SET(LIBRARY_OUTPUT_PATH ${C_LIBRARY_OUTPUT_PATH})

FILE(GLOB API_SRC *.c)
ADD_DEFINITIONS(-DLIB_EXPORTS=1)

ADD_LIBRARY( tinkercellapi
	STATIC
#    SHARED
	../TCstructs.c
	${API_SRC}
)

IF ( WIN32 )
  INSTALL(TARGETS tinkercellapi DESTINATION c)
ELSE ( WIN32 )
  INSTALL(TARGETS tinkercellapi ARCHIVE DESTINATION c)
ENDIF( WIN32 )

