# This file was automatically generated by SWIG (http://www.swig.org).
# Version 1.3.40
#
# Do not make changes to this file unless you know what you are doing--modify
# the SWIG interface file instead.

##   Generated via the command line invocation:
##	 swig -r tinkercell.i


#                         srun.swg                            #
#
# This is the basic code that is needed at run time within R to
# provide and define the relevant classes.  It is included
# automatically in the generated code by copying the contents of
# srun.swg into the newly created binding code.


# This could be provided as a separate run-time library but this
# approach allows the code to to be included directly into the
# generated bindings and so removes the need to have and install an
# additional library.  We may however end up with multiple copies of
# this and some confusion at run-time as to which class to use. This
# is an issue when we use NAMESPACES as we may need to export certain
# classes.

######################################################################

if(length(getClassDef("RSWIGStruct")) == 0) 
  setClass("RSWIGStruct", representation("VIRTUAL"))



if(length(getClassDef("ExternalReference")) == 0) 
# Should be virtual but this means it loses its slots currently
#representation("VIRTUAL")
  setClass("ExternalReference", representation( ref = "externalptr"))



if(length(getClassDef("NativeRoutinePointer")) == 0) 
  setClass("NativeRoutinePointer", 
              representation(parameterTypes = "character",
                             returnType = "character",
                             "VIRTUAL"), 
              contains = "ExternalReference")

if(length(getClassDef("CRoutinePointer")) == 0) 
  setClass("CRoutinePointer", contains = "NativeRoutinePointer")


if(length(getClassDef("EnumerationValue")) == 0) 
  setClass("EnumerationValue", contains = "integer")


if(!isGeneric("copyToR")) 
 setGeneric("copyToR",
            function(value, obj = new(gsub("Ref$", "", class(value)))) 
               standardGeneric("copyToR"
           ))

setGeneric("delete", function(obj) standardGeneric("delete"))


SWIG_createNewRef = 
function(className, ..., append = TRUE)
{
  f = get(paste("new", className, sep = "_"), mode = "function")

  f(...)
}

if(!isGeneric("copyToC")) 
 setGeneric("copyToC", 
             function(value, obj = RSWIG_createNewRef(class(value)))
              standardGeneric("copyToC"
            ))


# 
defineEnumeration =
function(name, .values, where = topenv(parent.frame()), suffix = "Value")
{
   # Mirror the class definitions via the E analogous to .__C__
  defName = paste(".__E__", name, sep = "")
  assign(defName,  .values,  envir = where)

  if(nchar(suffix))
    name = paste(name, suffix, sep = "")

  setClass(name, contains = "EnumerationValue", where = where)
}

enumToInteger <- function(name,type)
{
   if (is.character(name)) {
   ans <- as.integer(get(paste(".__E__", type, sep = ""))[name])
   if (is.na(ans)) {warning("enum not found ", name, " ", type)}
   ans
   } 
}

enumFromInteger =
function(i,type)
{
  itemlist <- get(paste(".__E__", type, sep=""))
  names(itemlist)[match(i, itemlist)]
}

coerceIfNotSubclass =
function(obj, type) 
{
    if(!is(obj, type)) {as(obj, type)} else obj
}


setClass("SWIGArray", representation(dims = "integer"), contains = "ExternalReference")

setMethod("length", "SWIGArray", function(x) x@dims[1])


defineEnumeration("SCopyReferences",
                   .values = c( "FALSE" = 0, "TRUE" = 1, "DEEP" = 2))

assert = 
function(condition, message = "")
{
  if(!condition)
    stop(message)

  TRUE
}


if(FALSE) {
print.SWIGFunction =
function(x, ...)
 {
 }
}


#######################################################################

R_SWIG_getCallbackFunctionStack =
function()
{
    # No PACKAGE argument as we don't know what the DLL is.
  .Call("R_SWIG_debug_getCallbackFunctionData")
}

R_SWIG_addCallbackFunctionStack =
function(fun, userData = NULL)
{
    # No PACKAGE argument as we don't know what the DLL is.
  .Call("R_SWIG_R_pushCallbackFunctionData", fun, userData)
}


#######################################################################


setClass('_p_tc_strings', contains = 'ExternalReference')
setClass("tc_strings",
    representation(
        length = "integer",
        strings = "character"),
        contains = "RSWIGStruct")


# End class tc_strings

setClass('_p_tc_items', contains = 'ExternalReference')
setClass("tc_items",
    representation(
        length = "integer"),
        contains = "RSWIGStruct")


# End class tc_items

setClass('_p_tc_matrix', contains = 'ExternalReference')
setClass("tc_matrix",
    representation(
        rows = "integer",
        cols = "integer"),
        contains = "RSWIGStruct")


# End class tc_matrix

setClass('_p_tc_table', contains = 'ExternalReference')
setClass("tc_table",
    representation(
        rows = "integer",
        cols = "integer",
        strings = "character"),
        contains = "RSWIGStruct")


# End class tc_table

setClass('_p_f_tc_matrix__void',
        prototype = list(parameterTypes = c('_tc_matrix'),
                        returnType = '_p_f_tc_matrix__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___tc_items',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__tc_items',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_tc_items__tc_items',
        prototype = list(parameterTypes = c('_p_char', '_tc_items'),
                        returnType = '_p_f_p_q_const__char_tc_items__tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__long',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__long'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_strings__tc_items',
        prototype = list(parameterTypes = c('_tc_strings'),
                        returnType = '_p_f_tc_strings__tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__void',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___void',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__p_char',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__p_char'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__void',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items__tc_strings',
        prototype = list(parameterTypes = c('_tc_items'),
                        returnType = '_p_f_tc_items__tc_strings'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__int',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__void',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__double',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__double'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items__tc_matrix',
        prototype = list(parameterTypes = c('_tc_items'),
                        returnType = '_p_f_tc_items__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_double_double__void',
        prototype = list(parameterTypes = c('_long', '_double', '_double'),
                        returnType = '_p_f_long_double_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items_tc_matrix__void',
        prototype = list(parameterTypes = c('_tc_items', '_tc_matrix'),
                        returnType = '_p_f_tc_items_tc_matrix__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_double_double__void',
        prototype = list(parameterTypes = c('_double', '_double'),
                        returnType = '_p_f_double_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___int',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___p_char',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___p_char'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_matrix_p_q_const__char_p_q_const__char__void',
        prototype = list(parameterTypes = c('_tc_matrix', '_p_char', '_p_char'),
                        returnType = '_p_f_tc_matrix_p_q_const__char_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_tc_matrix_p_q_const__char_p_f_tc_matrix__void__void',
        prototype = list(parameterTypes = c('_long', '_tc_matrix', '_p_char', '_p_f_tc_matrix__void'),
                        returnType = '_p_f_long_tc_matrix_p_q_const__char_p_f_tc_matrix__void__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_tc_matrix_p_f_tc_matrix__void__void',
        prototype = list(parameterTypes = c('_long', '_tc_matrix', '_p_f_tc_matrix__void'),
                        returnType = '_p_f_long_tc_matrix_p_f_tc_matrix__void__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_int_int_tc_strings__void',
        prototype = list(parameterTypes = c('_p_char', '_int', '_int', '_tc_strings'),
                        returnType = '_p_f_p_q_const__char_int_int_tc_strings__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_int_int__void',
        prototype = list(parameterTypes = c('_p_char', '_int', '_int'),
                        returnType = '_p_f_p_q_const__char_int_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__tc_items',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__long',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__long'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__tc_matrix',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char_tc_matrix__void',
        prototype = list(parameterTypes = c('_long', '_p_char', '_tc_matrix'),
                        returnType = '_p_f_long_p_q_const__char_tc_matrix__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__tc_table',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__tc_table'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char_tc_table__void',
        prototype = list(parameterTypes = c('_long', '_p_char', '_tc_table'),
                        returnType = '_p_f_long_p_q_const__char_tc_table__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long__tc_strings',
        prototype = list(parameterTypes = c('_long'),
                        returnType = '_p_f_long__tc_strings'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_double__void',
        prototype = list(parameterTypes = c('_double'),
                        returnType = '_p_f_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__p_char',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__p_char'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_tc_strings_p_q_const__char__int',
        prototype = list(parameterTypes = c('_p_char', '_tc_strings', '_p_char'),
                        returnType = '_p_f_p_q_const__char_tc_strings_p_q_const__char__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__double',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__double'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_strings_p_double__void',
        prototype = list(parameterTypes = c('_tc_strings', '_p_double'),
                        returnType = '_p_f_tc_strings_p_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__int',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_double_double_int__void',
        prototype = list(parameterTypes = c('_long', '_double', '_double', '_int'),
                        returnType = '_p_f_long_double_double_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_double_int__void',
        prototype = list(parameterTypes = c('_long', '_double', '_int'),
                        returnType = '_p_f_long_double_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char_int__void',
        prototype = list(parameterTypes = c('_long', '_p_char', '_int'),
                        returnType = '_p_f_long_p_q_const__char_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_double_double__void',
        prototype = list(parameterTypes = c('_p_char', '_double', '_double'),
                        returnType = '_p_f_p_q_const__char_double_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_double__void',
        prototype = list(parameterTypes = c('_p_char', '_double'),
                        returnType = '_p_f_p_q_const__char_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_table__void',
        prototype = list(parameterTypes = c('_tc_table'),
                        returnType = '_p_f_tc_table__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char__void',
        prototype = list(parameterTypes = c('_p_char', '_p_char'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_void__void',
        prototype = list(parameterTypes = c('_void'),
                        returnType = '_p_f_void__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_f_void__void__void',
        prototype = list(parameterTypes = c('_long', '_p_f_void__void'),
                        returnType = '_p_f_long_p_f_void__void__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_double__void',
        prototype = list(parameterTypes = c('_long', '_double'),
                        returnType = '_p_f_long_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__p_char',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__p_char'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items_tc_strings__tc_strings',
        prototype = list(parameterTypes = c('_tc_items', '_tc_strings'),
                        returnType = '_p_f_tc_items_tc_strings__tc_strings'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char_p_q_const__char__void',
        prototype = list(parameterTypes = c('_long', '_p_char', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__double',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__double'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items_tc_strings__tc_matrix',
        prototype = list(parameterTypes = c('_tc_items', '_tc_strings'),
                        returnType = '_p_f_tc_items_tc_strings__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char_double__void',
        prototype = list(parameterTypes = c('_long', '_p_char', '_double'),
                        returnType = '_p_f_long_p_q_const__char_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items_p_q_const__char_p_q_const__char__long',
        prototype = list(parameterTypes = c('_tc_items', '_p_char', '_p_char'),
                        returnType = '_p_f_tc_items_p_q_const__char_p_q_const__char__long'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_p_q_const__char__tc_items',
        prototype = list(parameterTypes = c('_long', '_p_char'),
                        returnType = '_p_f_long_p_q_const__char__tc_items'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_long_int__double',
        prototype = list(parameterTypes = c('_long', '_long', '_int'),
                        returnType = '_p_f_long_long_int__double'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_long_int_double_double__void',
        prototype = list(parameterTypes = c('_long', '_long', '_int', '_double', '_double'),
                        returnType = '_p_f_long_long_int_double_double__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_int__void',
        prototype = list(parameterTypes = c('_long', '_int'),
                        returnType = '_p_f_long_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_int__void',
        prototype = list(parameterTypes = c('_int'),
                        returnType = '_p_f_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items__void',
        prototype = list(parameterTypes = c('_tc_items'),
                        returnType = '_p_f_tc_items__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char__long',
        prototype = list(parameterTypes = c('_p_char', '_p_char'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char__long'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_items_tc_strings__void',
        prototype = list(parameterTypes = c('_tc_items', '_tc_strings'),
                        returnType = '_p_f_tc_items_tc_strings__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char__void',
        prototype = list(parameterTypes = c('_p_char', '_p_char', '_p_char', '_p_char', '_p_char'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char__int',
        prototype = list(parameterTypes = c('_p_char', '_p_char'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char_p_q_const__char__int',
        prototype = list(parameterTypes = c('_p_char', '_p_char', '_p_char'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char_p_q_const__char__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_p_q_const__char_p_q_const__char_tc_matrix__int',
        prototype = list(parameterTypes = c('_p_char', '_p_char', '_p_char', '_tc_matrix'),
                        returnType = '_p_f_p_q_const__char_p_q_const__char_p_q_const__char_tc_matrix__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_f___void_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_int_int_int__void',
        prototype = list(parameterTypes = c('_p_f___void', '_p_char', '_p_char', '_p_char', '_p_char', '_p_char', '_int', '_int', '_int'),
                        returnType = '_p_f_p_f___void_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_int_int_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_tc_matrix_p_q_const__char__void',
        prototype = list(parameterTypes = c('_tc_matrix', '_p_char'),
                        returnType = '_p_f_tc_matrix_p_q_const__char__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_int_int__void',
        prototype = list(parameterTypes = c('_int', '_int'),
                        returnType = '_p_f_int_int__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_int__tc_matrix',
        prototype = list(parameterTypes = c('_int'),
                        returnType = '_p_f_int__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_tc_items__int',
        prototype = list(parameterTypes = c('_p_char', '_tc_items'),
                        returnType = '_p_f_p_q_const__char_tc_items__int'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___tc_strings',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___tc_strings'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_long_tc_items__void',
        prototype = list(parameterTypes = c('_long', '_tc_items'),
                        returnType = '_p_f_long_tc_items__void'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_double_double_int__tc_matrix',
        prototype = list(parameterTypes = c('_double', '_double', '_int'),
                        returnType = '_p_f_double_double_int__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f___tc_matrix',
        prototype = list(parameterTypes = c(),
                        returnType = '_p_f___tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_double_double_int__tc_matrix',
        prototype = list(parameterTypes = c('_p_char', '_double', '_double', '_int'),
                        returnType = '_p_f_p_q_const__char_double_double_int__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char_double_double_int_p_q_const__char_double_double_int__tc_matrix',
        prototype = list(parameterTypes = c('_p_char', '_double', '_double', '_int', '_p_char', '_double', '_double', '_int'),
                        returnType = '_p_f_p_q_const__char_double_double_int_p_q_const__char_double_double_int__tc_matrix'),
        contains = 'CRoutinePointer')

##
setClass('_p_f_p_q_const__char__tc_matrix',
        prototype = list(parameterTypes = c('_p_char'),
                        returnType = '_p_f_p_q_const__char__tc_matrix'),
        contains = 'CRoutinePointer')

##



setMethod('[', "ExternalReference",
function(x,i,j, ..., drop=TRUE) 
if (!is.null(x$"__getitem__")) 
sapply(i, function(n) x$"__getitem__"(i=as.integer(n-1))))

setMethod('[<-' , "ExternalReference",
function(x,i,j, ..., value) 
if (!is.null(x$"__setitem__")) {
sapply(1:length(i), function(n) 
x$"__setitem__"(i=as.integer(i[n]-1), x=value[n]))
x
})

setAs('ExternalReference', 'character',
function(from) {if (!is.null(from$"__str__")) from$"__str__"()})

setMethod('print', 'ExternalReference',
function(x) {print(as(x, "character"))})

# Start of tc_strings_length_set

`tc_strings_length_set` = function(self, s_length)
{
  s_length = as.integer(s_length) 
  
  if(length(s_length) > 1) {
    warning("using only the first element of s_length")
  }
  
  .Call('R_swig_tc_strings_length_set', self, s_length, PACKAGE='tinkercell')
  
}

attr(`tc_strings_length_set`, 'returnType') = 'void'
attr(`tc_strings_length_set`, "inputTypes") = c('_p_tc_strings', 'integer')
class(`tc_strings_length_set`) = c("SWIGFunction", class('tc_strings_length_set'))

# Start of tc_strings_length_get

`tc_strings_length_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_strings_length_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_strings_length_get`, 'returnType') = 'integer'
attr(`tc_strings_length_get`, "inputTypes") = c('_p_tc_strings')
class(`tc_strings_length_get`) = c("SWIGFunction", class('tc_strings_length_get'))

# Start of tc_strings_strings_set

`tc_strings_strings_set` = function(self, s_strings)
{
  .Call('R_swig_tc_strings_strings_set', self, s_strings, PACKAGE='tinkercell')
  
}

attr(`tc_strings_strings_set`, 'returnType') = 'void'
attr(`tc_strings_strings_set`, "inputTypes") = c('_p_tc_strings', 'character')
class(`tc_strings_strings_set`) = c("SWIGFunction", class('tc_strings_strings_set'))

# Start of tc_strings_strings_get

`tc_strings_strings_get` = function(self)
{
  ans = .Call('R_swig_tc_strings_strings_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_p_char"
  
  ans
  
}

attr(`tc_strings_strings_get`, 'returnType') = 'character'
attr(`tc_strings_strings_get`, "inputTypes") = c('_p_tc_strings')
class(`tc_strings_strings_get`) = c("SWIGFunction", class('tc_strings_strings_get'))

# Start of new_tc_strings

`tc_strings` = function()
{
  ans = .Call('R_swig_new_tc_strings', PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_strings`, 'returnType') = '_p_tc_strings'
class(`tc_strings`) = c("SWIGFunction", class('tc_strings'))

# Start of delete_tc_strings

`delete_tc_strings` = function(self)
{
  .Call('R_swig_delete_tc_strings', self, PACKAGE='tinkercell')
  
}

attr(`delete_tc_strings`, 'returnType') = 'void'
attr(`delete_tc_strings`, "inputTypes") = c('_p_tc_strings')
class(`delete_tc_strings`) = c("SWIGFunction", class('delete_tc_strings'))

# Start of accessor method for tc_strings
setMethod('$', '_p_tc_strings', function(x, name)

{
  accessorFuns = list('length' = tc_strings_length_get, 'strings' = tc_strings_strings_get)
  vaccessors = c('length', 'strings')
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name))
  f = accessorFuns[[idx]]
  formals(f)[[1]] = x
  if (is.na(match(name, vaccessors))) f else f(x)
}


)
# end of accessor method for tc_strings
# Start of accessor method for tc_strings
setMethod('$<-', '_p_tc_strings', function(x, name, value)

{
  accessorFuns = list('length' = tc_strings_length_set, 'strings' = tc_strings_strings_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
setMethod('[[<-', c('_p_tc_strings', 'character'),function(x, i, j, ..., value)

{
  name = i
  accessorFuns = list('length' = tc_strings_length_set, 'strings' = tc_strings_strings_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
# end of accessor method for tc_strings
setMethod('delete', '_p_tc_strings', function(obj) {delete_tc_strings(obj)})
# Start definition of copy functions & methods for tc_strings
CopyToR_tc_strings = function(value, obj = new("tc_strings"))
{
  obj@length = value$length
  obj@strings = value$strings
  obj
}



CopyToC_tc_strings = function(value, obj)
{
  obj$length = value@length
  obj$strings = value@strings
  obj
}



# Start definition of copy methods for tc_strings
setMethod('copyToR', '_p_tc_strings', CopyToR_tc_strings)
setMethod('copyToC', 'tc_strings', CopyToC_tc_strings)

# End definition of copy methods for tc_strings
# End definition of copy functions & methods for tc_strings
# Start of tc_items_length_set

`tc_items_length_set` = function(self, s_length)
{
  s_length = as.integer(s_length) 
  
  if(length(s_length) > 1) {
    warning("using only the first element of s_length")
  }
  
  .Call('R_swig_tc_items_length_set', self, s_length, PACKAGE='tinkercell')
  
}

attr(`tc_items_length_set`, 'returnType') = 'void'
attr(`tc_items_length_set`, "inputTypes") = c('_p_tc_items', 'integer')
class(`tc_items_length_set`) = c("SWIGFunction", class('tc_items_length_set'))

# Start of tc_items_length_get

`tc_items_length_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_items_length_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_items_length_get`, 'returnType') = 'integer'
attr(`tc_items_length_get`, "inputTypes") = c('_p_tc_items')
class(`tc_items_length_get`) = c("SWIGFunction", class('tc_items_length_get'))

# Start of tc_items_items_set

`tc_items_items_set` = function(self, s_items)
{
  s_items = as.integer(s_items) 
  .Call('R_swig_tc_items_items_set', self, s_items, PACKAGE='tinkercell')
  
}

attr(`tc_items_items_set`, 'returnType') = 'void'
attr(`tc_items_items_set`, "inputTypes") = c('_p_tc_items', 'integer')
class(`tc_items_items_set`) = c("SWIGFunction", class('tc_items_items_set'))

# Start of tc_items_items_get

`tc_items_items_get` = function(self)
{
  ans = .Call('R_swig_tc_items_items_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_long"
  
  ans
  
}

attr(`tc_items_items_get`, 'returnType') = 'integer'
attr(`tc_items_items_get`, "inputTypes") = c('_p_tc_items')
class(`tc_items_items_get`) = c("SWIGFunction", class('tc_items_items_get'))

# Start of new_tc_items

`tc_items` = function()
{
  ans = .Call('R_swig_new_tc_items', PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_items`, 'returnType') = '_p_tc_items'
class(`tc_items`) = c("SWIGFunction", class('tc_items'))

# Start of delete_tc_items

`delete_tc_items` = function(self)
{
  .Call('R_swig_delete_tc_items', self, PACKAGE='tinkercell')
  
}

attr(`delete_tc_items`, 'returnType') = 'void'
attr(`delete_tc_items`, "inputTypes") = c('_p_tc_items')
class(`delete_tc_items`) = c("SWIGFunction", class('delete_tc_items'))

# Start of accessor method for tc_items
setMethod('$', '_p_tc_items', function(x, name)

{
  accessorFuns = list('length' = tc_items_length_get, 'items' = tc_items_items_get)
  vaccessors = c('length', 'items')
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name))
  f = accessorFuns[[idx]]
  formals(f)[[1]] = x
  if (is.na(match(name, vaccessors))) f else f(x)
}


)
# end of accessor method for tc_items
# Start of accessor method for tc_items
setMethod('$<-', '_p_tc_items', function(x, name, value)

{
  accessorFuns = list('length' = tc_items_length_set, 'items' = tc_items_items_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
setMethod('[[<-', c('_p_tc_items', 'character'),function(x, i, j, ..., value)

{
  name = i
  accessorFuns = list('length' = tc_items_length_set, 'items' = tc_items_items_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
# end of accessor method for tc_items
setMethod('delete', '_p_tc_items', function(obj) {delete_tc_items(obj)})
# Start definition of copy functions & methods for tc_items
CopyToR_tc_items = function(value, obj = new("tc_items"))
{
  obj@length = value$length
  obj
}



CopyToC_tc_items = function(value, obj)
{
  obj$length = value@length
  obj
}



# Start definition of copy methods for tc_items
setMethod('copyToR', '_p_tc_items', CopyToR_tc_items)
setMethod('copyToC', 'tc_items', CopyToC_tc_items)

# End definition of copy methods for tc_items
# End definition of copy functions & methods for tc_items
# Start of tc_matrix_rows_set

`tc_matrix_rows_set` = function(self, s_rows)
{
  s_rows = as.integer(s_rows) 
  
  if(length(s_rows) > 1) {
    warning("using only the first element of s_rows")
  }
  
  .Call('R_swig_tc_matrix_rows_set', self, s_rows, PACKAGE='tinkercell')
  
}

attr(`tc_matrix_rows_set`, 'returnType') = 'void'
attr(`tc_matrix_rows_set`, "inputTypes") = c('_p_tc_matrix', 'integer')
class(`tc_matrix_rows_set`) = c("SWIGFunction", class('tc_matrix_rows_set'))

# Start of tc_matrix_rows_get

`tc_matrix_rows_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_matrix_rows_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_matrix_rows_get`, 'returnType') = 'integer'
attr(`tc_matrix_rows_get`, "inputTypes") = c('_p_tc_matrix')
class(`tc_matrix_rows_get`) = c("SWIGFunction", class('tc_matrix_rows_get'))

# Start of tc_matrix_cols_set

`tc_matrix_cols_set` = function(self, s_cols)
{
  s_cols = as.integer(s_cols) 
  
  if(length(s_cols) > 1) {
    warning("using only the first element of s_cols")
  }
  
  .Call('R_swig_tc_matrix_cols_set', self, s_cols, PACKAGE='tinkercell')
  
}

attr(`tc_matrix_cols_set`, 'returnType') = 'void'
attr(`tc_matrix_cols_set`, "inputTypes") = c('_p_tc_matrix', 'integer')
class(`tc_matrix_cols_set`) = c("SWIGFunction", class('tc_matrix_cols_set'))

# Start of tc_matrix_cols_get

`tc_matrix_cols_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_matrix_cols_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_matrix_cols_get`, 'returnType') = 'integer'
attr(`tc_matrix_cols_get`, "inputTypes") = c('_p_tc_matrix')
class(`tc_matrix_cols_get`) = c("SWIGFunction", class('tc_matrix_cols_get'))

# Start of tc_matrix_values_set

`tc_matrix_values_set` = function(self, s_values)
{
  .Call('R_swig_tc_matrix_values_set', self, s_values, PACKAGE='tinkercell')
  
}

attr(`tc_matrix_values_set`, 'returnType') = 'void'
attr(`tc_matrix_values_set`, "inputTypes") = c('_p_tc_matrix', 'numeric')
class(`tc_matrix_values_set`) = c("SWIGFunction", class('tc_matrix_values_set'))

# Start of tc_matrix_values_get

`tc_matrix_values_get` = function(self)
{
  ans = .Call('R_swig_tc_matrix_values_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_double"
  
  ans
  
}

attr(`tc_matrix_values_get`, 'returnType') = 'numeric'
attr(`tc_matrix_values_get`, "inputTypes") = c('_p_tc_matrix')
class(`tc_matrix_values_get`) = c("SWIGFunction", class('tc_matrix_values_get'))

# Start of tc_matrix_rownames_set

`tc_matrix_rownames_set` = function(self, s_rownames)
{
  .Call('R_swig_tc_matrix_rownames_set', self, s_rownames, PACKAGE='tinkercell')
  
}

attr(`tc_matrix_rownames_set`, 'returnType') = 'void'
attr(`tc_matrix_rownames_set`, "inputTypes") = c('_p_tc_matrix', '_p_tc_strings')
class(`tc_matrix_rownames_set`) = c("SWIGFunction", class('tc_matrix_rownames_set'))

# Start of tc_matrix_rownames_get

`tc_matrix_rownames_get` = function(self)
{
  ans = .Call('R_swig_tc_matrix_rownames_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_matrix_rownames_get`, 'returnType') = '_p_tc_strings'
attr(`tc_matrix_rownames_get`, "inputTypes") = c('_p_tc_matrix')
class(`tc_matrix_rownames_get`) = c("SWIGFunction", class('tc_matrix_rownames_get'))

# Start of tc_matrix_colnames_set

`tc_matrix_colnames_set` = function(self, s_colnames)
{
  .Call('R_swig_tc_matrix_colnames_set', self, s_colnames, PACKAGE='tinkercell')
  
}

attr(`tc_matrix_colnames_set`, 'returnType') = 'void'
attr(`tc_matrix_colnames_set`, "inputTypes") = c('_p_tc_matrix', '_p_tc_strings')
class(`tc_matrix_colnames_set`) = c("SWIGFunction", class('tc_matrix_colnames_set'))

# Start of tc_matrix_colnames_get

`tc_matrix_colnames_get` = function(self)
{
  ans = .Call('R_swig_tc_matrix_colnames_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_matrix_colnames_get`, 'returnType') = '_p_tc_strings'
attr(`tc_matrix_colnames_get`, "inputTypes") = c('_p_tc_matrix')
class(`tc_matrix_colnames_get`) = c("SWIGFunction", class('tc_matrix_colnames_get'))

# Start of new_tc_matrix

`tc_matrix` = function()
{
  ans = .Call('R_swig_new_tc_matrix', PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_matrix`, 'returnType') = '_p_tc_matrix'
class(`tc_matrix`) = c("SWIGFunction", class('tc_matrix'))

# Start of delete_tc_matrix

`delete_tc_matrix` = function(self)
{
  .Call('R_swig_delete_tc_matrix', self, PACKAGE='tinkercell')
  
}

attr(`delete_tc_matrix`, 'returnType') = 'void'
attr(`delete_tc_matrix`, "inputTypes") = c('_p_tc_matrix')
class(`delete_tc_matrix`) = c("SWIGFunction", class('delete_tc_matrix'))

# Start of accessor method for tc_matrix
setMethod('$', '_p_tc_matrix', function(x, name)

{
  accessorFuns = list('rows' = tc_matrix_rows_get, 'cols' = tc_matrix_cols_get, 'values' = tc_matrix_values_get, 'rownames' = tc_matrix_rownames_get, 'colnames' = tc_matrix_colnames_get)
  vaccessors = c('rows', 'cols', 'values', 'rownames', 'colnames')
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name))
  f = accessorFuns[[idx]]
  formals(f)[[1]] = x
  if (is.na(match(name, vaccessors))) f else f(x)
}


)
# end of accessor method for tc_matrix
# Start of accessor method for tc_matrix
setMethod('$<-', '_p_tc_matrix', function(x, name, value)

{
  accessorFuns = list('rows' = tc_matrix_rows_set, 'cols' = tc_matrix_cols_set, 'values' = tc_matrix_values_set, 'rownames' = tc_matrix_rownames_set, 'colnames' = tc_matrix_colnames_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
setMethod('[[<-', c('_p_tc_matrix', 'character'),function(x, i, j, ..., value)

{
  name = i
  accessorFuns = list('rows' = tc_matrix_rows_set, 'cols' = tc_matrix_cols_set, 'values' = tc_matrix_values_set, 'rownames' = tc_matrix_rownames_set, 'colnames' = tc_matrix_colnames_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
# end of accessor method for tc_matrix
setMethod('delete', '_p_tc_matrix', function(obj) {delete_tc_matrix(obj)})
# Start definition of copy functions & methods for tc_matrix
CopyToR_tc_matrix = function(value, obj = new("tc_matrix"))
{
  obj@rows = value$rows
  obj@cols = value$cols
  obj
}



CopyToC_tc_matrix = function(value, obj)
{
  obj$rows = value@rows
  obj$cols = value@cols
  obj
}



# Start definition of copy methods for tc_matrix
setMethod('copyToR', '_p_tc_matrix', CopyToR_tc_matrix)
setMethod('copyToC', 'tc_matrix', CopyToC_tc_matrix)

# End definition of copy methods for tc_matrix
# End definition of copy functions & methods for tc_matrix
# Start of tc_table_rows_set

`tc_table_rows_set` = function(self, s_rows)
{
  s_rows = as.integer(s_rows) 
  
  if(length(s_rows) > 1) {
    warning("using only the first element of s_rows")
  }
  
  .Call('R_swig_tc_table_rows_set', self, s_rows, PACKAGE='tinkercell')
  
}

attr(`tc_table_rows_set`, 'returnType') = 'void'
attr(`tc_table_rows_set`, "inputTypes") = c('_p_tc_table', 'integer')
class(`tc_table_rows_set`) = c("SWIGFunction", class('tc_table_rows_set'))

# Start of tc_table_rows_get

`tc_table_rows_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_table_rows_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_table_rows_get`, 'returnType') = 'integer'
attr(`tc_table_rows_get`, "inputTypes") = c('_p_tc_table')
class(`tc_table_rows_get`) = c("SWIGFunction", class('tc_table_rows_get'))

# Start of tc_table_cols_set

`tc_table_cols_set` = function(self, s_cols)
{
  s_cols = as.integer(s_cols) 
  
  if(length(s_cols) > 1) {
    warning("using only the first element of s_cols")
  }
  
  .Call('R_swig_tc_table_cols_set', self, s_cols, PACKAGE='tinkercell')
  
}

attr(`tc_table_cols_set`, 'returnType') = 'void'
attr(`tc_table_cols_set`, "inputTypes") = c('_p_tc_table', 'integer')
class(`tc_table_cols_set`) = c("SWIGFunction", class('tc_table_cols_set'))

# Start of tc_table_cols_get

`tc_table_cols_get` = function(self, .copy = FALSE)
{
  .Call('R_swig_tc_table_cols_get', self, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_table_cols_get`, 'returnType') = 'integer'
attr(`tc_table_cols_get`, "inputTypes") = c('_p_tc_table')
class(`tc_table_cols_get`) = c("SWIGFunction", class('tc_table_cols_get'))

# Start of tc_table_strings_set

`tc_table_strings_set` = function(self, s_strings)
{
  .Call('R_swig_tc_table_strings_set', self, s_strings, PACKAGE='tinkercell')
  
}

attr(`tc_table_strings_set`, 'returnType') = 'void'
attr(`tc_table_strings_set`, "inputTypes") = c('_p_tc_table', 'character')
class(`tc_table_strings_set`) = c("SWIGFunction", class('tc_table_strings_set'))

# Start of tc_table_strings_get

`tc_table_strings_get` = function(self)
{
  ans = .Call('R_swig_tc_table_strings_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_p_char"
  
  ans
  
}

attr(`tc_table_strings_get`, 'returnType') = 'character'
attr(`tc_table_strings_get`, "inputTypes") = c('_p_tc_table')
class(`tc_table_strings_get`) = c("SWIGFunction", class('tc_table_strings_get'))

# Start of tc_table_rownames_set

`tc_table_rownames_set` = function(self, s_rownames)
{
  .Call('R_swig_tc_table_rownames_set', self, s_rownames, PACKAGE='tinkercell')
  
}

attr(`tc_table_rownames_set`, 'returnType') = 'void'
attr(`tc_table_rownames_set`, "inputTypes") = c('_p_tc_table', '_p_tc_strings')
class(`tc_table_rownames_set`) = c("SWIGFunction", class('tc_table_rownames_set'))

# Start of tc_table_rownames_get

`tc_table_rownames_get` = function(self)
{
  ans = .Call('R_swig_tc_table_rownames_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_table_rownames_get`, 'returnType') = '_p_tc_strings'
attr(`tc_table_rownames_get`, "inputTypes") = c('_p_tc_table')
class(`tc_table_rownames_get`) = c("SWIGFunction", class('tc_table_rownames_get'))

# Start of tc_table_colnames_set

`tc_table_colnames_set` = function(self, s_colnames)
{
  .Call('R_swig_tc_table_colnames_set', self, s_colnames, PACKAGE='tinkercell')
  
}

attr(`tc_table_colnames_set`, 'returnType') = 'void'
attr(`tc_table_colnames_set`, "inputTypes") = c('_p_tc_table', '_p_tc_strings')
class(`tc_table_colnames_set`) = c("SWIGFunction", class('tc_table_colnames_set'))

# Start of tc_table_colnames_get

`tc_table_colnames_get` = function(self)
{
  ans = .Call('R_swig_tc_table_colnames_get', self, PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_table_colnames_get`, 'returnType') = '_p_tc_strings'
attr(`tc_table_colnames_get`, "inputTypes") = c('_p_tc_table')
class(`tc_table_colnames_get`) = c("SWIGFunction", class('tc_table_colnames_get'))

# Start of new_tc_table

`tc_table` = function()
{
  ans = .Call('R_swig_new_tc_table', PACKAGE='tinkercell')
  class(ans) <- "_p_tc_table"
  
  ans
  
}

attr(`tc_table`, 'returnType') = '_p_tc_table'
class(`tc_table`) = c("SWIGFunction", class('tc_table'))

# Start of delete_tc_table

`delete_tc_table` = function(self)
{
  .Call('R_swig_delete_tc_table', self, PACKAGE='tinkercell')
  
}

attr(`delete_tc_table`, 'returnType') = 'void'
attr(`delete_tc_table`, "inputTypes") = c('_p_tc_table')
class(`delete_tc_table`) = c("SWIGFunction", class('delete_tc_table'))

# Start of accessor method for tc_table
setMethod('$', '_p_tc_table', function(x, name)

{
  accessorFuns = list('rows' = tc_table_rows_get, 'cols' = tc_table_cols_get, 'strings' = tc_table_strings_get, 'rownames' = tc_table_rownames_get, 'colnames' = tc_table_colnames_get)
  vaccessors = c('rows', 'cols', 'strings', 'rownames', 'colnames')
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name))
  f = accessorFuns[[idx]]
  formals(f)[[1]] = x
  if (is.na(match(name, vaccessors))) f else f(x)
}


)
# end of accessor method for tc_table
# Start of accessor method for tc_table
setMethod('$<-', '_p_tc_table', function(x, name, value)

{
  accessorFuns = list('rows' = tc_table_rows_set, 'cols' = tc_table_cols_set, 'strings' = tc_table_strings_set, 'rownames' = tc_table_rownames_set, 'colnames' = tc_table_colnames_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
setMethod('[[<-', c('_p_tc_table', 'character'),function(x, i, j, ..., value)

{
  name = i
  accessorFuns = list('rows' = tc_table_rows_set, 'cols' = tc_table_cols_set, 'strings' = tc_table_strings_set, 'rownames' = tc_table_rownames_set, 'colnames' = tc_table_colnames_set)
  idx = pmatch(name, names(accessorFuns))
  if(is.na(idx)) 
  return(callNextMethod(x, name, value))
  f = accessorFuns[[idx]]
  f(x, value)
  x
}


)
# end of accessor method for tc_table
setMethod('delete', '_p_tc_table', function(obj) {delete_tc_table(obj)})
# Start definition of copy functions & methods for tc_table
CopyToR_tc_table = function(value, obj = new("tc_table"))
{
  obj@rows = value$rows
  obj@cols = value$cols
  obj@strings = value$strings
  obj
}



CopyToC_tc_table = function(value, obj)
{
  obj$rows = value@rows
  obj$cols = value@cols
  obj$strings = value@strings
  obj
}



# Start definition of copy methods for tc_table
setMethod('copyToR', '_p_tc_table', CopyToR_tc_table)
setMethod('copyToC', 'tc_table', CopyToC_tc_table)

# End definition of copy methods for tc_table
# End definition of copy functions & methods for tc_table
# Start of tc_createMatrix

`tc_createMatrix` = function(rows, cols, .copy = FALSE)
{
  rows = as.integer(rows) 
  
  if(length(rows) > 1) {
    warning("using only the first element of rows")
  }
  
  cols = as.integer(cols) 
  
  if(length(cols) > 1) {
    warning("using only the first element of cols")
  }
  
  ans = .Call('R_swig_tc_createMatrix', rows, cols, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_createMatrix`, 'returnType') = '_p_tc_matrix'
attr(`tc_createMatrix`, "inputTypes") = c('integer', 'integer')
class(`tc_createMatrix`) = c("SWIGFunction", class('tc_createMatrix'))

# Start of tc_createTable

`tc_createTable` = function(rows, cols, .copy = FALSE)
{
  rows = as.integer(rows) 
  
  if(length(rows) > 1) {
    warning("using only the first element of rows")
  }
  
  cols = as.integer(cols) 
  
  if(length(cols) > 1) {
    warning("using only the first element of cols")
  }
  
  ans = .Call('R_swig_tc_createTable', rows, cols, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_table"
  
  ans
  
}

attr(`tc_createTable`, 'returnType') = '_p_tc_table'
attr(`tc_createTable`, "inputTypes") = c('integer', 'integer')
class(`tc_createTable`) = c("SWIGFunction", class('tc_createTable'))

# Start of tc_createStringsArray

`tc_createStringsArray` = function(len, .copy = FALSE)
{
  len = as.integer(len) 
  
  if(length(len) > 1) {
    warning("using only the first element of len")
  }
  
  ans = .Call('R_swig_tc_createStringsArray', len, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_createStringsArray`, 'returnType') = '_p_tc_strings'
attr(`tc_createStringsArray`, "inputTypes") = c('integer')
class(`tc_createStringsArray`) = c("SWIGFunction", class('tc_createStringsArray'))

# Start of tc_createItemsArray

`tc_createItemsArray` = function(len, .copy = FALSE)
{
  len = as.integer(len) 
  
  if(length(len) > 1) {
    warning("using only the first element of len")
  }
  
  ans = .Call('R_swig_tc_createItemsArray', len, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_createItemsArray`, 'returnType') = '_p_tc_items'
attr(`tc_createItemsArray`, "inputTypes") = c('integer')
class(`tc_createItemsArray`) = c("SWIGFunction", class('tc_createItemsArray'))

# Start of tc_getMatrixValue

`tc_getMatrixValue` = function(M, i, j, .copy = FALSE)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  .Call('R_swig_tc_getMatrixValue', M, i, j, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getMatrixValue`, 'returnType') = 'numeric'
attr(`tc_getMatrixValue`, "inputTypes") = c('_p_tc_matrix', 'integer', 'integer')
class(`tc_getMatrixValue`) = c("SWIGFunction", class('tc_getMatrixValue'))

# Start of tc_setMatrixValue

`tc_setMatrixValue` = function(M, i, j, d)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  
  .Call('R_swig_tc_setMatrixValue', M, i, j, d, PACKAGE='tinkercell')
  
}

attr(`tc_setMatrixValue`, 'returnType') = 'void'
attr(`tc_setMatrixValue`, "inputTypes") = c('_p_tc_matrix', 'integer', 'integer', 'numeric')
class(`tc_setMatrixValue`) = c("SWIGFunction", class('tc_setMatrixValue'))

# Start of tc_getRowName

`tc_getRowName` = function(M, i)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  .Call('R_swig_tc_getRowName', M, i, PACKAGE='tinkercell')
  
}

attr(`tc_getRowName`, 'returnType') = 'character'
attr(`tc_getRowName`, "inputTypes") = c('_p_tc_matrix', 'integer')
class(`tc_getRowName`) = c("SWIGFunction", class('tc_getRowName'))

# Start of tc_setRowName

`tc_setRowName` = function(M, i, s)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  s = as(s, "character") 
  .Call('R_swig_tc_setRowName', M, i, s, PACKAGE='tinkercell')
  
}

attr(`tc_setRowName`, 'returnType') = 'void'
attr(`tc_setRowName`, "inputTypes") = c('_p_tc_matrix', 'integer', 'character')
class(`tc_setRowName`) = c("SWIGFunction", class('tc_setRowName'))

# Start of tc_getColumnName

`tc_getColumnName` = function(M, j)
{
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  .Call('R_swig_tc_getColumnName', M, j, PACKAGE='tinkercell')
  
}

attr(`tc_getColumnName`, 'returnType') = 'character'
attr(`tc_getColumnName`, "inputTypes") = c('_p_tc_matrix', 'integer')
class(`tc_getColumnName`) = c("SWIGFunction", class('tc_getColumnName'))

# Start of tc_setColumnName

`tc_setColumnName` = function(M, j, s)
{
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  s = as(s, "character") 
  .Call('R_swig_tc_setColumnName', M, j, s, PACKAGE='tinkercell')
  
}

attr(`tc_setColumnName`, 'returnType') = 'void'
attr(`tc_setColumnName`, "inputTypes") = c('_p_tc_matrix', 'integer', 'character')
class(`tc_setColumnName`) = c("SWIGFunction", class('tc_setColumnName'))

# Start of tc_getTableValue

`tc_getTableValue` = function(S, i, j)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  .Call('R_swig_tc_getTableValue', S, i, j, PACKAGE='tinkercell')
  
}

attr(`tc_getTableValue`, 'returnType') = 'character'
attr(`tc_getTableValue`, "inputTypes") = c('_p_tc_table', 'integer', 'integer')
class(`tc_getTableValue`) = c("SWIGFunction", class('tc_getTableValue'))

# Start of tc_setTableValue

`tc_setTableValue` = function(S, i, j, s)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  s = as(s, "character") 
  .Call('R_swig_tc_setTableValue', S, i, j, s, PACKAGE='tinkercell')
  
}

attr(`tc_setTableValue`, 'returnType') = 'void'
attr(`tc_setTableValue`, "inputTypes") = c('_p_tc_table', 'integer', 'integer', 'character')
class(`tc_setTableValue`) = c("SWIGFunction", class('tc_setTableValue'))

# Start of tc_getString

`tc_getString` = function(S, i)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  .Call('R_swig_tc_getString', S, i, PACKAGE='tinkercell')
  
}

attr(`tc_getString`, 'returnType') = 'character'
attr(`tc_getString`, "inputTypes") = c('_p_tc_strings', 'integer')
class(`tc_getString`) = c("SWIGFunction", class('tc_getString'))

# Start of tc_setString

`tc_setString` = function(S, i, c)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  c = as(c, "character") 
  .Call('R_swig_tc_setString', S, i, c, PACKAGE='tinkercell')
  
}

attr(`tc_setString`, 'returnType') = 'void'
attr(`tc_setString`, "inputTypes") = c('_p_tc_strings', 'integer', 'character')
class(`tc_setString`) = c("SWIGFunction", class('tc_setString'))

# Start of tc_getItem

`tc_getItem` = function(A, i, .copy = FALSE)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  .Call('R_swig_tc_getItem', A, i, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getItem`, 'returnType') = 'integer'
attr(`tc_getItem`, "inputTypes") = c('_p_tc_items', 'integer')
class(`tc_getItem`) = c("SWIGFunction", class('tc_getItem'))

# Start of tc_setItem

`tc_setItem` = function(A, i, o)
{
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  .Call('R_swig_tc_setItem', A, i, o, PACKAGE='tinkercell')
  
}

attr(`tc_setItem`, 'returnType') = 'void'
attr(`tc_setItem`, "inputTypes") = c('_p_tc_items', 'integer', 'integer')
class(`tc_setItem`) = c("SWIGFunction", class('tc_setItem'))

# Start of tc_getStringIndex

`tc_getStringIndex` = function(A, s, .copy = FALSE)
{
  s = as(s, "character") 
  .Call('R_swig_tc_getStringIndex', A, s, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getStringIndex`, 'returnType') = 'integer'
attr(`tc_getStringIndex`, "inputTypes") = c('_p_tc_strings', 'character')
class(`tc_getStringIndex`) = c("SWIGFunction", class('tc_getStringIndex'))

# Start of tc_getRowIndex

`tc_getRowIndex` = function(s_arg1, s, .copy = FALSE)
{
  s = as(s, "character") 
  .Call('R_swig_tc_getRowIndex', s_arg1, s, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getRowIndex`, 'returnType') = 'integer'
attr(`tc_getRowIndex`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_getRowIndex`) = c("SWIGFunction", class('tc_getRowIndex'))

# Start of tc_getColumnIndex

`tc_getColumnIndex` = function(s_arg1, s, .copy = FALSE)
{
  s = as(s, "character") 
  .Call('R_swig_tc_getColumnIndex', s_arg1, s, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getColumnIndex`, 'returnType') = 'integer'
attr(`tc_getColumnIndex`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_getColumnIndex`) = c("SWIGFunction", class('tc_getColumnIndex'))

# Start of tc_deleteMatrix

`tc_deleteMatrix` = function(M)
{
  .Call('R_swig_tc_deleteMatrix', M, PACKAGE='tinkercell')
  
}

attr(`tc_deleteMatrix`, 'returnType') = 'void'
attr(`tc_deleteMatrix`, "inputTypes") = c('_p_tc_matrix')
class(`tc_deleteMatrix`) = c("SWIGFunction", class('tc_deleteMatrix'))

# Start of tc_deleteTable

`tc_deleteTable` = function(M)
{
  .Call('R_swig_tc_deleteTable', M, PACKAGE='tinkercell')
  
}

attr(`tc_deleteTable`, 'returnType') = 'void'
attr(`tc_deleteTable`, "inputTypes") = c('_p_tc_table')
class(`tc_deleteTable`) = c("SWIGFunction", class('tc_deleteTable'))

# Start of tc_deleteItemsArray

`tc_deleteItemsArray` = function(A)
{
  .Call('R_swig_tc_deleteItemsArray', A, PACKAGE='tinkercell')
  
}

attr(`tc_deleteItemsArray`, 'returnType') = 'void'
attr(`tc_deleteItemsArray`, "inputTypes") = c('_p_tc_items')
class(`tc_deleteItemsArray`) = c("SWIGFunction", class('tc_deleteItemsArray'))

# Start of tc_deleteStringsArray

`tc_deleteStringsArray` = function(C)
{
  .Call('R_swig_tc_deleteStringsArray', C, PACKAGE='tinkercell')
  
}

attr(`tc_deleteStringsArray`, 'returnType') = 'void'
attr(`tc_deleteStringsArray`, "inputTypes") = c('_p_tc_strings')
class(`tc_deleteStringsArray`) = c("SWIGFunction", class('tc_deleteStringsArray'))

# Start of tc_appendColumns

`tc_appendColumns` = function(A, B, .copy = FALSE)
{
  ans = .Call('R_swig_tc_appendColumns', A, B, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_appendColumns`, 'returnType') = '_p_tc_matrix'
attr(`tc_appendColumns`, "inputTypes") = c('_p_tc_matrix', '_p_tc_matrix')
class(`tc_appendColumns`) = c("SWIGFunction", class('tc_appendColumns'))

# Start of tc_appendRows

`tc_appendRows` = function(A, B, .copy = FALSE)
{
  ans = .Call('R_swig_tc_appendRows', A, B, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_appendRows`, 'returnType') = '_p_tc_matrix'
attr(`tc_appendRows`, "inputTypes") = c('_p_tc_matrix', '_p_tc_matrix')
class(`tc_appendRows`) = c("SWIGFunction", class('tc_appendRows'))

# Start of tc_printMatrixToFile

`tc_printMatrixToFile` = function(file, M)
{
  file = as(file, "character") 
  
  .Call('R_swig_tc_printMatrixToFile', file, M, PACKAGE='tinkercell')
  
}

attr(`tc_printMatrixToFile`, 'returnType') = 'void'
attr(`tc_printMatrixToFile`, "inputTypes") = c('character', '_p_tc_matrix')
class(`tc_printMatrixToFile`) = c("SWIGFunction", class('tc_printMatrixToFile'))

# Start of tc_printOutMatrix

`tc_printOutMatrix` = function(M)
{
  .Call('R_swig_tc_printOutMatrix', M, PACKAGE='tinkercell')
  
}

attr(`tc_printOutMatrix`, 'returnType') = 'void'
attr(`tc_printOutMatrix`, "inputTypes") = c('_p_tc_matrix')
class(`tc_printOutMatrix`) = c("SWIGFunction", class('tc_printOutMatrix'))

# Start of tc_printTableToFile

`tc_printTableToFile` = function(file, M)
{
  file = as(file, "character") 
  
  .Call('R_swig_tc_printTableToFile', file, M, PACKAGE='tinkercell')
  
}

attr(`tc_printTableToFile`, 'returnType') = 'void'
attr(`tc_printTableToFile`, "inputTypes") = c('character', '_p_tc_table')
class(`tc_printTableToFile`) = c("SWIGFunction", class('tc_printTableToFile'))

# Start of tc_printOutTable

`tc_printOutTable` = function(M)
{
  .Call('R_swig_tc_printOutTable', M, PACKAGE='tinkercell')
  
}

attr(`tc_printOutTable`, 'returnType') = 'void'
attr(`tc_printOutTable`, "inputTypes") = c('_p_tc_table')
class(`tc_printOutTable`) = c("SWIGFunction", class('tc_printOutTable'))

# Start of tc_allItems

`tc_allItems` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_allItems', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_allItems`, 'returnType') = '_p_tc_items'
class(`tc_allItems`) = c("SWIGFunction", class('tc_allItems'))

# Start of tc_selectedItems

`tc_selectedItems` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_selectedItems', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_selectedItems`, 'returnType') = '_p_tc_items'
class(`tc_selectedItems`) = c("SWIGFunction", class('tc_selectedItems'))

# Start of tc_itemsOfFamily

`tc_itemsOfFamily` = function(family, .copy = FALSE)
{
  family = as(family, "character") 
  ans = .Call('R_swig_tc_itemsOfFamily', family, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_itemsOfFamily`, 'returnType') = '_p_tc_items'
attr(`tc_itemsOfFamily`, "inputTypes") = c('character')
class(`tc_itemsOfFamily`) = c("SWIGFunction", class('tc_itemsOfFamily'))

# Start of tc_itemsOfFamilyFrom

`tc_itemsOfFamilyFrom` = function(family, itemsToSelectFrom, .copy = FALSE)
{
  family = as(family, "character") 
  
  ans = .Call('R_swig_tc_itemsOfFamilyFrom', family, itemsToSelectFrom, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_itemsOfFamilyFrom`, 'returnType') = '_p_tc_items'
attr(`tc_itemsOfFamilyFrom`, "inputTypes") = c('character', '_p_tc_items')
class(`tc_itemsOfFamilyFrom`) = c("SWIGFunction", class('tc_itemsOfFamilyFrom'))

# Start of tc_find

`tc_find` = function(name, .copy = FALSE)
{
  name = as(name, "character") 
  .Call('R_swig_tc_find', name, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_find`, 'returnType') = 'integer'
attr(`tc_find`, "inputTypes") = c('character')
class(`tc_find`) = c("SWIGFunction", class('tc_find'))

# Start of tc_findItems

`tc_findItems` = function(names, .copy = FALSE)
{
  ans = .Call('R_swig_tc_findItems', names, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_findItems`, 'returnType') = '_p_tc_items'
attr(`tc_findItems`, "inputTypes") = c('_p_tc_strings')
class(`tc_findItems`) = c("SWIGFunction", class('tc_findItems'))

# Start of tc_select

`tc_select` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_select', item, PACKAGE='tinkercell')
  
}

attr(`tc_select`, 'returnType') = 'void'
attr(`tc_select`, "inputTypes") = c('integer')
class(`tc_select`) = c("SWIGFunction", class('tc_select'))

# Start of tc_deselect

`tc_deselect` = function()
{
  .Call('R_swig_tc_deselect', PACKAGE='tinkercell')
  
}

attr(`tc_deselect`, 'returnType') = 'void'
class(`tc_deselect`) = c("SWIGFunction", class('tc_deselect'))

# Start of tc_getName

`tc_getName` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getName', item, PACKAGE='tinkercell')
  
}

attr(`tc_getName`, 'returnType') = 'character'
attr(`tc_getName`, "inputTypes") = c('integer')
class(`tc_getName`) = c("SWIGFunction", class('tc_getName'))

# Start of tc_getUniqueName

`tc_getUniqueName` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getUniqueName', item, PACKAGE='tinkercell')
  
}

attr(`tc_getUniqueName`, 'returnType') = 'character'
attr(`tc_getUniqueName`, "inputTypes") = c('integer')
class(`tc_getUniqueName`) = c("SWIGFunction", class('tc_getUniqueName'))

# Start of tc_rename

`tc_rename` = function(item, name)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  name = as(name, "character") 
  .Call('R_swig_tc_rename', item, name, PACKAGE='tinkercell')
  
}

attr(`tc_rename`, 'returnType') = 'void'
attr(`tc_rename`, "inputTypes") = c('integer', 'character')
class(`tc_rename`) = c("SWIGFunction", class('tc_rename'))

# Start of tc_getNames

`tc_getNames` = function(items, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getNames', items, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getNames`, 'returnType') = '_p_tc_strings'
attr(`tc_getNames`, "inputTypes") = c('_p_tc_items')
class(`tc_getNames`) = c("SWIGFunction", class('tc_getNames'))

# Start of tc_getUniqueNames

`tc_getUniqueNames` = function(items, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getUniqueNames', items, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getUniqueNames`, 'returnType') = '_p_tc_strings'
attr(`tc_getUniqueNames`, "inputTypes") = c('_p_tc_items')
class(`tc_getUniqueNames`) = c("SWIGFunction", class('tc_getUniqueNames'))

# Start of tc_getFamily

`tc_getFamily` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getFamily', item, PACKAGE='tinkercell')
  
}

attr(`tc_getFamily`, 'returnType') = 'character'
attr(`tc_getFamily`, "inputTypes") = c('integer')
class(`tc_getFamily`) = c("SWIGFunction", class('tc_getFamily'))

# Start of tc_isA

`tc_isA` = function(item, family, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  family = as(family, "character") 
  .Call('R_swig_tc_isA', item, family, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_isA`, 'returnType') = 'integer'
attr(`tc_isA`, "inputTypes") = c('integer', 'character')
class(`tc_isA`) = c("SWIGFunction", class('tc_isA'))

# Start of tc_print

`tc_print` = function(text)
{
  text = as(text, "character") 
  .Call('R_swig_tc_print', text, PACKAGE='tinkercell')
  
}

attr(`tc_print`, 'returnType') = 'void'
attr(`tc_print`, "inputTypes") = c('character')
class(`tc_print`) = c("SWIGFunction", class('tc_print'))

# Start of tc_openUrl

`tc_openUrl` = function(url)
{
  url = as(url, "character") 
  .Call('R_swig_tc_openUrl', url, PACKAGE='tinkercell')
  
}

attr(`tc_openUrl`, 'returnType') = 'void'
attr(`tc_openUrl`, "inputTypes") = c('character')
class(`tc_openUrl`) = c("SWIGFunction", class('tc_openUrl'))

# Start of tc_errorReport

`tc_errorReport` = function(text)
{
  text = as(text, "character") 
  .Call('R_swig_tc_errorReport', text, PACKAGE='tinkercell')
  
}

attr(`tc_errorReport`, 'returnType') = 'void'
attr(`tc_errorReport`, "inputTypes") = c('character')
class(`tc_errorReport`) = c("SWIGFunction", class('tc_errorReport'))

# Start of tc_printMatrix

`tc_printMatrix` = function(data)
{
  .Call('R_swig_tc_printMatrix', data, PACKAGE='tinkercell')
  
}

attr(`tc_printMatrix`, 'returnType') = 'void'
attr(`tc_printMatrix`, "inputTypes") = c('_p_tc_matrix')
class(`tc_printMatrix`) = c("SWIGFunction", class('tc_printMatrix'))

# Start of tc_printFile

`tc_printFile` = function(filename)
{
  filename = as(filename, "character") 
  .Call('R_swig_tc_printFile', filename, PACKAGE='tinkercell')
  
}

attr(`tc_printFile`, 'returnType') = 'void'
attr(`tc_printFile`, "inputTypes") = c('character')
class(`tc_printFile`) = c("SWIGFunction", class('tc_printFile'))

# Start of tc_clear

`tc_clear` = function()
{
  .Call('R_swig_tc_clear', PACKAGE='tinkercell')
  
}

attr(`tc_clear`, 'returnType') = 'void'
class(`tc_clear`) = c("SWIGFunction", class('tc_clear'))

# Start of tc_remove

`tc_remove` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_remove', item, PACKAGE='tinkercell')
  
}

attr(`tc_remove`, 'returnType') = 'void'
attr(`tc_remove`, "inputTypes") = c('integer')
class(`tc_remove`) = c("SWIGFunction", class('tc_remove'))

# Start of tc_getY

`tc_getY` = function(item, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getY', item, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getY`, 'returnType') = 'numeric'
attr(`tc_getY`, "inputTypes") = c('integer')
class(`tc_getY`) = c("SWIGFunction", class('tc_getY'))

# Start of tc_getX

`tc_getX` = function(item, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getX', item, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getX`, 'returnType') = 'numeric'
attr(`tc_getX`, "inputTypes") = c('integer')
class(`tc_getX`) = c("SWIGFunction", class('tc_getX'))

# Start of tc_getPos

`tc_getPos` = function(items, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getPos', items, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getPos`, 'returnType') = '_p_tc_matrix'
attr(`tc_getPos`, "inputTypes") = c('_p_tc_items')
class(`tc_getPos`) = c("SWIGFunction", class('tc_getPos'))

# Start of tc_setPos

`tc_setPos` = function(item, x, y)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  
  .Call('R_swig_tc_setPos', item, x, y, PACKAGE='tinkercell')
  
}

attr(`tc_setPos`, 'returnType') = 'void'
attr(`tc_setPos`, "inputTypes") = c('integer', 'numeric', 'numeric')
class(`tc_setPos`) = c("SWIGFunction", class('tc_setPos'))

# Start of tc_setPosMulti

`tc_setPosMulti` = function(items, positions)
{
  .Call('R_swig_tc_setPosMulti', items, positions, PACKAGE='tinkercell')
  
}

attr(`tc_setPosMulti`, 'returnType') = 'void'
attr(`tc_setPosMulti`, "inputTypes") = c('_p_tc_items', '_p_tc_matrix')
class(`tc_setPosMulti`) = c("SWIGFunction", class('tc_setPosMulti'))

# Start of tc_moveSelected

`tc_moveSelected` = function(dx, dy)
{
  .Call('R_swig_tc_moveSelected', dx, dy, PACKAGE='tinkercell')
  
}

attr(`tc_moveSelected`, 'returnType') = 'void'
attr(`tc_moveSelected`, "inputTypes") = c('numeric', 'numeric')
class(`tc_moveSelected`) = c("SWIGFunction", class('tc_moveSelected'))

# Start of tc_isWindows

`tc_isWindows` = function(.copy = FALSE)
{
  .Call('R_swig_tc_isWindows', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_isWindows`, 'returnType') = 'integer'
class(`tc_isWindows`) = c("SWIGFunction", class('tc_isWindows'))

# Start of tc_isMac

`tc_isMac` = function(.copy = FALSE)
{
  .Call('R_swig_tc_isMac', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_isMac`, 'returnType') = 'integer'
class(`tc_isMac`) = c("SWIGFunction", class('tc_isMac'))

# Start of tc_isLinux

`tc_isLinux` = function(.copy = FALSE)
{
  .Call('R_swig_tc_isLinux', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_isLinux`, 'returnType') = 'integer'
class(`tc_isLinux`) = c("SWIGFunction", class('tc_isLinux'))

# Start of tc_appDir

`tc_appDir` = function()
{
  .Call('R_swig_tc_appDir', PACKAGE='tinkercell')
  
}

attr(`tc_appDir`, 'returnType') = 'character'
class(`tc_appDir`) = c("SWIGFunction", class('tc_appDir'))

# Start of tc_homeDir

`tc_homeDir` = function()
{
  .Call('R_swig_tc_homeDir', PACKAGE='tinkercell')
  
}

attr(`tc_homeDir`, 'returnType') = 'character'
class(`tc_homeDir`) = c("SWIGFunction", class('tc_homeDir'))

# Start of tc_createInputWindowForScript

`tc_createInputWindowForScript` = function(input, title, functionname)
{
  title = as(title, "character") 
  functionname = as(functionname, "character") 
  .Call('R_swig_tc_createInputWindowForScript', input, title, functionname, PACKAGE='tinkercell')
  
}

attr(`tc_createInputWindowForScript`, 'returnType') = 'void'
attr(`tc_createInputWindowForScript`, "inputTypes") = c('_p_tc_matrix', 'character', 'character')
class(`tc_createInputWindowForScript`) = c("SWIGFunction", class('tc_createInputWindowForScript'))

# Start of tc_createInputWindow

`tc_createInputWindow` = function(input, title, f)
{
  title = as(title, "character") 
  if(is.function(f)) {
    assert('...' %in% names(formals(f)) || length(formals(f)) >= 1)
  } else {
    if(is.character(f)) {
      f = getNativeSymbolInfo(f)
    }
    if(is(f, "NativeSymbolInfo")) {
      f = f$address
    }
  }
  .Call('R_swig_tc_createInputWindow', input, title, f, PACKAGE='tinkercell')
  
}

attr(`tc_createInputWindow`, 'returnType') = 'void'
attr(`tc_createInputWindow`, "inputTypes") = c('_p_tc_matrix', 'character', '_p_f_tc_matrix__void')
class(`tc_createInputWindow`) = c("SWIGFunction", class('tc_createInputWindow'))

# Start of tc_addInputWindowOptions

`tc_addInputWindowOptions` = function(title, i, j, options)
{
  title = as(title, "character") 
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  
  .Call('R_swig_tc_addInputWindowOptions', title, i, j, options, PACKAGE='tinkercell')
  
}

attr(`tc_addInputWindowOptions`, 'returnType') = 'void'
attr(`tc_addInputWindowOptions`, "inputTypes") = c('character', 'integer', 'integer', '_p_tc_strings')
class(`tc_addInputWindowOptions`) = c("SWIGFunction", class('tc_addInputWindowOptions'))

# Start of tc_addInputWindowCheckbox

`tc_addInputWindowCheckbox` = function(title, i, j)
{
  title = as(title, "character") 
  i = as.integer(i) 
  
  if(length(i) > 1) {
    warning("using only the first element of i")
  }
  
  j = as.integer(j) 
  
  if(length(j) > 1) {
    warning("using only the first element of j")
  }
  
  .Call('R_swig_tc_addInputWindowCheckbox', title, i, j, PACKAGE='tinkercell')
  
}

attr(`tc_addInputWindowCheckbox`, 'returnType') = 'void'
attr(`tc_addInputWindowCheckbox`, "inputTypes") = c('character', 'integer', 'integer')
class(`tc_addInputWindowCheckbox`) = c("SWIGFunction", class('tc_addInputWindowCheckbox'))

# Start of tc_openNewWindow

`tc_openNewWindow` = function(title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_openNewWindow', title, PACKAGE='tinkercell')
  
}

attr(`tc_openNewWindow`, 'returnType') = 'void'
attr(`tc_openNewWindow`, "inputTypes") = c('character')
class(`tc_openNewWindow`) = c("SWIGFunction", class('tc_openNewWindow'))

# Start of tc_getChildren

`tc_getChildren` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_getChildren', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_getChildren`, 'returnType') = '_p_tc_items'
attr(`tc_getChildren`, "inputTypes") = c('integer')
class(`tc_getChildren`) = c("SWIGFunction", class('tc_getChildren'))

# Start of tc_getParent

`tc_getParent` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  .Call('R_swig_tc_getParent', o, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getParent`, 'returnType') = 'integer'
attr(`tc_getParent`, "inputTypes") = c('integer')
class(`tc_getParent`) = c("SWIGFunction", class('tc_getParent'))

# Start of tc_getNumericalData

`tc_getNumericalData` = function(item, data, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  data = as(data, "character") 
  ans = .Call('R_swig_tc_getNumericalData', item, data, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getNumericalData`, 'returnType') = '_p_tc_matrix'
attr(`tc_getNumericalData`, "inputTypes") = c('integer', 'character')
class(`tc_getNumericalData`) = c("SWIGFunction", class('tc_getNumericalData'))

# Start of tc_setNumericalData

`tc_setNumericalData` = function(o, title, data)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  title = as(title, "character") 
  
  .Call('R_swig_tc_setNumericalData', o, title, data, PACKAGE='tinkercell')
  
}

attr(`tc_setNumericalData`, 'returnType') = 'void'
attr(`tc_setNumericalData`, "inputTypes") = c('integer', 'character', '_p_tc_matrix')
class(`tc_setNumericalData`) = c("SWIGFunction", class('tc_setNumericalData'))

# Start of tc_setNumericalValues

`tc_setNumericalValues` = function(data)
{
  .Call('R_swig_tc_setNumericalValues', data, PACKAGE='tinkercell')
  
}

attr(`tc_setNumericalValues`, 'returnType') = 'void'
attr(`tc_setNumericalValues`, "inputTypes") = c('_p_tc_matrix')
class(`tc_setNumericalValues`) = c("SWIGFunction", class('tc_setNumericalValues'))

# Start of tc_setNumericalValue

`tc_setNumericalValue` = function(name, value)
{
  name = as(name, "character") 
  
  .Call('R_swig_tc_setNumericalValue', name, value, PACKAGE='tinkercell')
  
}

attr(`tc_setNumericalValue`, 'returnType') = 'void'
attr(`tc_setNumericalValue`, "inputTypes") = c('character', 'numeric')
class(`tc_setNumericalValue`) = c("SWIGFunction", class('tc_setNumericalValue'))

# Start of tc_getTextData

`tc_getTextData` = function(item, data, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  data = as(data, "character") 
  ans = .Call('R_swig_tc_getTextData', item, data, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_table"
  
  ans
  
}

attr(`tc_getTextData`, 'returnType') = '_p_tc_table'
attr(`tc_getTextData`, "inputTypes") = c('integer', 'character')
class(`tc_getTextData`) = c("SWIGFunction", class('tc_getTextData'))

# Start of tc_setTextData

`tc_setTextData` = function(o, title, data)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  title = as(title, "character") 
  
  .Call('R_swig_tc_setTextData', o, title, data, PACKAGE='tinkercell')
  
}

attr(`tc_setTextData`, 'returnType') = 'void'
attr(`tc_setTextData`, "inputTypes") = c('integer', 'character', '_p_tc_table')
class(`tc_setTextData`) = c("SWIGFunction", class('tc_setTextData'))

# Start of tc_setTextValues

`tc_setTextValues` = function(data)
{
  .Call('R_swig_tc_setTextValues', data, PACKAGE='tinkercell')
  
}

attr(`tc_setTextValues`, 'returnType') = 'void'
attr(`tc_setTextValues`, "inputTypes") = c('_p_tc_table')
class(`tc_setTextValues`) = c("SWIGFunction", class('tc_setTextValues'))

# Start of tc_getNumericalValue

`tc_getNumericalValue` = function(name, .copy = FALSE)
{
  name = as(name, "character") 
  .Call('R_swig_tc_getNumericalValue', name, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getNumericalValue`, 'returnType') = 'numeric'
attr(`tc_getNumericalValue`, "inputTypes") = c('character')
class(`tc_getNumericalValue`) = c("SWIGFunction", class('tc_getNumericalValue'))

# Start of tc_getTextValue

`tc_getTextValue` = function(name)
{
  name = as(name, "character") 
  .Call('R_swig_tc_getTextValue', name, PACKAGE='tinkercell')
  
}

attr(`tc_getTextValue`, 'returnType') = 'character'
attr(`tc_getTextValue`, "inputTypes") = c('character')
class(`tc_getTextValue`) = c("SWIGFunction", class('tc_getTextValue'))

# Start of tc_setTextValue

`tc_setTextValue` = function(name, value)
{
  name = as(name, "character") 
  value = as(value, "character") 
  .Call('R_swig_tc_setTextValue', name, value, PACKAGE='tinkercell')
  
}

attr(`tc_setTextValue`, 'returnType') = 'void'
attr(`tc_setTextValue`, "inputTypes") = c('character', 'character')
class(`tc_setTextValue`) = c("SWIGFunction", class('tc_setTextValue'))

# Start of tc_getNumericalDataNames

`tc_getNumericalDataNames` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_getNumericalDataNames', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getNumericalDataNames`, 'returnType') = '_p_tc_strings'
attr(`tc_getNumericalDataNames`, "inputTypes") = c('integer')
class(`tc_getNumericalDataNames`) = c("SWIGFunction", class('tc_getNumericalDataNames'))

# Start of tc_getTextDataNames

`tc_getTextDataNames` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_getTextDataNames', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getTextDataNames`, 'returnType') = '_p_tc_strings'
attr(`tc_getTextDataNames`, "inputTypes") = c('integer')
class(`tc_getTextDataNames`) = c("SWIGFunction", class('tc_getTextDataNames'))

# Start of tc_zoom

`tc_zoom` = function(factor)
{
  .Call('R_swig_tc_zoom', factor, PACKAGE='tinkercell')
  
}

attr(`tc_zoom`, 'returnType') = 'void'
attr(`tc_zoom`, "inputTypes") = c('numeric')
class(`tc_zoom`) = c("SWIGFunction", class('tc_zoom'))

# Start of tc_getStringDialog

`tc_getStringDialog` = function(title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_getStringDialog', title, PACKAGE='tinkercell')
  
}

attr(`tc_getStringDialog`, 'returnType') = 'character'
attr(`tc_getStringDialog`, "inputTypes") = c('character')
class(`tc_getStringDialog`) = c("SWIGFunction", class('tc_getStringDialog'))

# Start of tc_getFilename

`tc_getFilename` = function()
{
  .Call('R_swig_tc_getFilename', PACKAGE='tinkercell')
  
}

attr(`tc_getFilename`, 'returnType') = 'character'
class(`tc_getFilename`) = c("SWIGFunction", class('tc_getFilename'))

# Start of tc_getStringFromList

`tc_getStringFromList` = function(title, list, selectedString, .copy = FALSE)
{
  title = as(title, "character") 
  
  selectedString = as(selectedString, "character") 
  .Call('R_swig_tc_getStringFromList', title, list, selectedString, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getStringFromList`, 'returnType') = 'integer'
attr(`tc_getStringFromList`, "inputTypes") = c('character', '_p_tc_strings', 'character')
class(`tc_getStringFromList`) = c("SWIGFunction", class('tc_getStringFromList'))

# Start of tc_getNumber

`tc_getNumber` = function(title, .copy = FALSE)
{
  title = as(title, "character") 
  .Call('R_swig_tc_getNumber', title, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getNumber`, 'returnType') = 'numeric'
attr(`tc_getNumber`, "inputTypes") = c('character')
class(`tc_getNumber`) = c("SWIGFunction", class('tc_getNumber'))

# Start of tc_getNumbers

`tc_getNumbers` = function(labels, result)
{
  .Call('R_swig_tc_getNumbers', labels, result, PACKAGE='tinkercell')
  
}

attr(`tc_getNumbers`, 'returnType') = 'void'
attr(`tc_getNumbers`, "inputTypes") = c('_p_tc_strings', 'numeric')
class(`tc_getNumbers`) = c("SWIGFunction", class('tc_getNumbers'))

# Start of tc_askQuestion

`tc_askQuestion` = function(message, .copy = FALSE)
{
  message = as(message, "character") 
  .Call('R_swig_tc_askQuestion', message, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_askQuestion`, 'returnType') = 'integer'
attr(`tc_askQuestion`, "inputTypes") = c('character')
class(`tc_askQuestion`) = c("SWIGFunction", class('tc_askQuestion'))

# Start of tc_messageDialog

`tc_messageDialog` = function(message)
{
  message = as(message, "character") 
  .Call('R_swig_tc_messageDialog', message, PACKAGE='tinkercell')
  
}

attr(`tc_messageDialog`, 'returnType') = 'void'
attr(`tc_messageDialog`, "inputTypes") = c('character')
class(`tc_messageDialog`) = c("SWIGFunction", class('tc_messageDialog'))

# Start of tc_openFile

`tc_openFile` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_openFile', file, PACKAGE='tinkercell')
  
}

attr(`tc_openFile`, 'returnType') = 'void'
attr(`tc_openFile`, "inputTypes") = c('character')
class(`tc_openFile`) = c("SWIGFunction", class('tc_openFile'))

# Start of tc_saveToFile

`tc_saveToFile` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_saveToFile', file, PACKAGE='tinkercell')
  
}

attr(`tc_saveToFile`, 'returnType') = 'void'
attr(`tc_saveToFile`, "inputTypes") = c('character')
class(`tc_saveToFile`) = c("SWIGFunction", class('tc_saveToFile'))

# Start of tc_thisThread

`tc_thisThread` = function(.copy = FALSE)
{
  .Call('R_swig_tc_thisThread', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_thisThread`, 'returnType') = 'integer'
class(`tc_thisThread`) = c("SWIGFunction", class('tc_thisThread'))

# Start of tc_createSliders

`tc_createSliders` = function(input, f)
{
  if(is.function(f)) {
    assert('...' %in% names(formals(f)) || length(formals(f)) >= 0)
  } else {
    if(is.character(f)) {
      f = getNativeSymbolInfo(f)
    }
    if(is(f, "NativeSymbolInfo")) {
      f = f$address
    }
  }
  .Call('R_swig_tc_createSliders', input, f, PACKAGE='tinkercell')
  
}

attr(`tc_createSliders`, 'returnType') = 'void'
attr(`tc_createSliders`, "inputTypes") = c('_p_tc_matrix', '_p_f_tc_matrix__void')
class(`tc_createSliders`) = c("SWIGFunction", class('tc_createSliders'))

# Start of tc_getColor

`tc_getColor` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getColor', item, PACKAGE='tinkercell')
  
}

attr(`tc_getColor`, 'returnType') = 'character'
attr(`tc_getColor`, "inputTypes") = c('integer')
class(`tc_getColor`) = c("SWIGFunction", class('tc_getColor'))

# Start of tc_setColor

`tc_setColor` = function(item, name, permanent)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  name = as(name, "character") 
  permanent = as.integer(permanent) 
  
  if(length(permanent) > 1) {
    warning("using only the first element of permanent")
  }
  
  .Call('R_swig_tc_setColor', item, name, permanent, PACKAGE='tinkercell')
  
}

attr(`tc_setColor`, 'returnType') = 'void'
attr(`tc_setColor`, "inputTypes") = c('integer', 'character', 'integer')
class(`tc_setColor`) = c("SWIGFunction", class('tc_setColor'))

# Start of tc_changeNodeImage

`tc_changeNodeImage` = function(item, filename)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  filename = as(filename, "character") 
  .Call('R_swig_tc_changeNodeImage', item, filename, PACKAGE='tinkercell')
  
}

attr(`tc_changeNodeImage`, 'returnType') = 'void'
attr(`tc_changeNodeImage`, "inputTypes") = c('integer', 'character')
class(`tc_changeNodeImage`) = c("SWIGFunction", class('tc_changeNodeImage'))

# Start of tc_changeArrowHead

`tc_changeArrowHead` = function(connection, filename)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  filename = as(filename, "character") 
  .Call('R_swig_tc_changeArrowHead', connection, filename, PACKAGE='tinkercell')
  
}

attr(`tc_changeArrowHead`, 'returnType') = 'void'
attr(`tc_changeArrowHead`, "inputTypes") = c('integer', 'character')
class(`tc_changeArrowHead`) = c("SWIGFunction", class('tc_changeArrowHead'))

# Start of tc_setSize

`tc_setSize` = function(item, width, height)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  
  .Call('R_swig_tc_setSize', item, width, height, PACKAGE='tinkercell')
  
}

attr(`tc_setSize`, 'returnType') = 'void'
attr(`tc_setSize`, "inputTypes") = c('integer', 'numeric', 'numeric')
class(`tc_setSize`) = c("SWIGFunction", class('tc_setSize'))

# Start of tc_getWidth

`tc_getWidth` = function(item, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getWidth', item, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getWidth`, 'returnType') = 'numeric'
attr(`tc_getWidth`, "inputTypes") = c('integer')
class(`tc_getWidth`) = c("SWIGFunction", class('tc_getWidth'))

# Start of tc_getHeight

`tc_getHeight` = function(item, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_getHeight', item, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getHeight`, 'returnType') = 'numeric'
attr(`tc_getHeight`, "inputTypes") = c('integer')
class(`tc_getHeight`) = c("SWIGFunction", class('tc_getHeight'))

# Start of tc_rotate

`tc_rotate` = function(item, t)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  .Call('R_swig_tc_rotate', item, t, PACKAGE='tinkercell')
  
}

attr(`tc_rotate`, 'returnType') = 'void'
attr(`tc_rotate`, "inputTypes") = c('integer', 'numeric')
class(`tc_rotate`) = c("SWIGFunction", class('tc_rotate'))

# Start of tc_screenshot

`tc_screenshot` = function(filename, width, height)
{
  filename = as(filename, "character") 
  width = as.integer(width) 
  
  if(length(width) > 1) {
    warning("using only the first element of width")
  }
  
  height = as.integer(height) 
  
  if(length(height) > 1) {
    warning("using only the first element of height")
  }
  
  .Call('R_swig_tc_screenshot', filename, width, height, PACKAGE='tinkercell')
  
}

attr(`tc_screenshot`, 'returnType') = 'void'
attr(`tc_screenshot`, "inputTypes") = c('character', 'integer', 'integer')
class(`tc_screenshot`) = c("SWIGFunction", class('tc_screenshot'))

# Start of tc_screenWidth

`tc_screenWidth` = function(.copy = FALSE)
{
  .Call('R_swig_tc_screenWidth', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_screenWidth`, 'returnType') = 'integer'
class(`tc_screenWidth`) = c("SWIGFunction", class('tc_screenWidth'))

# Start of tc_screenHeight

`tc_screenHeight` = function(.copy = FALSE)
{
  .Call('R_swig_tc_screenHeight', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_screenHeight`, 'returnType') = 'integer'
class(`tc_screenHeight`) = c("SWIGFunction", class('tc_screenHeight'))

# Start of tc_screenX

`tc_screenX` = function(.copy = FALSE)
{
  .Call('R_swig_tc_screenX', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_screenX`, 'returnType') = 'integer'
class(`tc_screenX`) = c("SWIGFunction", class('tc_screenX'))

# Start of tc_screenY

`tc_screenY` = function(.copy = FALSE)
{
  .Call('R_swig_tc_screenY', as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_screenY`, 'returnType') = 'integer'
class(`tc_screenY`) = c("SWIGFunction", class('tc_screenY'))

# Start of tc_annotations

`tc_annotations` = function()
{
  .Call('R_swig_tc_annotations', PACKAGE='tinkercell')
  
}

attr(`tc_annotations`, 'returnType') = 'character'
class(`tc_annotations`) = c("SWIGFunction", class('tc_annotations'))

# Start of tc_insertAnnotations

`tc_insertAnnotations` = function(s_arg1, s_arg2, s_arg3)
{
  s_arg1 = as(s_arg1, "character") 
  
  
  .Call('R_swig_tc_insertAnnotations', s_arg1, s_arg2, s_arg3, PACKAGE='tinkercell')
  
}

attr(`tc_insertAnnotations`, 'returnType') = 'void'
attr(`tc_insertAnnotations`, "inputTypes") = c('character', 'numeric', 'numeric')
class(`tc_insertAnnotations`) = c("SWIGFunction", class('tc_insertAnnotations'))

# Start of tc_Main_api_initialize

`tc_Main_api_initialize` = function(tc_allItems0, tc_selectedItems0, tc_itemsOfFamily0, tc_itemsOfFamily1, tc_find0, tc_findItems0, tc_select0, tc_deselect0, tc_getName0, tc_getUniqueName0, tc_setName0, tc_getNames0, tc_getUniqueNames0, tc_getFamily0, tc_isA0, tc_clearText, tc_outputText0, tc_errorReport0, tc_outputTable0, tc_printFile0, tc_removeItem0, tc_getY0, tc_getX0, tc_getPos0, tc_setPos0, tc_setPos1, tc_moveSelected0, tc_isWindows0, tc_isMac0, tc_isLinux0, tc_appDir0, tc_homeDir0, tc_createInputWindow0, tc_createInputWindow1, createSliders, tc_addInputWindowOptions0, tc_addInputWindowCheckbox0, tc_openNewWindow0, tc_getChildren0, tc_getParent0, tc_getNumericalData0, tc_setNumericalData0, tc_getTextData0, tc_setTextData0, tc_getNumericalDataNames0, tc_getTextDataNames0, tc_zoom0, tc_getStringDialog0, getSelectedString, getNumber, getNumbers, getFilename, askQuestion, messageDialog, openFile, saveToFile, setSize0, getWidth0, getHeight0, setAngle0, getColor, setColor0, changeGraphics0, changeArrowHead0, screenshot, screenHeight, screenWidth, screenX, screenY, annotations, insertAnnotations, setNumericalValues, setNumericalValue, setTextValues, setTextValue, getNumericalValue, getTextValue, openUrl)
{
  if(is.function(tc_allItems0)) {
    assert('...' %in% names(formals(tc_allItems0)) || length(formals(tc_allItems0)) >= 0)
  } else {
    if(is.character(tc_allItems0)) {
      tc_allItems0 = getNativeSymbolInfo(tc_allItems0)
    }
    if(is(tc_allItems0, "NativeSymbolInfo")) {
      tc_allItems0 = tc_allItems0$address
    }
  }
  if(is.function(tc_selectedItems0)) {
    assert('...' %in% names(formals(tc_selectedItems0)) || length(formals(tc_selectedItems0)) >= 0)
  } else {
    if(is.character(tc_selectedItems0)) {
      tc_selectedItems0 = getNativeSymbolInfo(tc_selectedItems0)
    }
    if(is(tc_selectedItems0, "NativeSymbolInfo")) {
      tc_selectedItems0 = tc_selectedItems0$address
    }
  }
  if(is.function(tc_itemsOfFamily0)) {
    assert('...' %in% names(formals(tc_itemsOfFamily0)) || length(formals(tc_itemsOfFamily0)) >= 1)
  } else {
    if(is.character(tc_itemsOfFamily0)) {
      tc_itemsOfFamily0 = getNativeSymbolInfo(tc_itemsOfFamily0)
    }
    if(is(tc_itemsOfFamily0, "NativeSymbolInfo")) {
      tc_itemsOfFamily0 = tc_itemsOfFamily0$address
    }
  }
  if(is.function(tc_itemsOfFamily1)) {
    assert('...' %in% names(formals(tc_itemsOfFamily1)) || length(formals(tc_itemsOfFamily1)) >= 2)
  } else {
    if(is.character(tc_itemsOfFamily1)) {
      tc_itemsOfFamily1 = getNativeSymbolInfo(tc_itemsOfFamily1)
    }
    if(is(tc_itemsOfFamily1, "NativeSymbolInfo")) {
      tc_itemsOfFamily1 = tc_itemsOfFamily1$address
    }
  }
  if(is.function(tc_find0)) {
    assert('...' %in% names(formals(tc_find0)) || length(formals(tc_find0)) >= 1)
  } else {
    if(is.character(tc_find0)) {
      tc_find0 = getNativeSymbolInfo(tc_find0)
    }
    if(is(tc_find0, "NativeSymbolInfo")) {
      tc_find0 = tc_find0$address
    }
  }
  if(is.function(tc_findItems0)) {
    assert('...' %in% names(formals(tc_findItems0)) || length(formals(tc_findItems0)) >= 1)
  } else {
    if(is.character(tc_findItems0)) {
      tc_findItems0 = getNativeSymbolInfo(tc_findItems0)
    }
    if(is(tc_findItems0, "NativeSymbolInfo")) {
      tc_findItems0 = tc_findItems0$address
    }
  }
  if(is.function(tc_select0)) {
    assert('...' %in% names(formals(tc_select0)) || length(formals(tc_select0)) >= 1)
  } else {
    if(is.character(tc_select0)) {
      tc_select0 = getNativeSymbolInfo(tc_select0)
    }
    if(is(tc_select0, "NativeSymbolInfo")) {
      tc_select0 = tc_select0$address
    }
  }
  if(is.function(tc_deselect0)) {
    assert('...' %in% names(formals(tc_deselect0)) || length(formals(tc_deselect0)) >= 0)
  } else {
    if(is.character(tc_deselect0)) {
      tc_deselect0 = getNativeSymbolInfo(tc_deselect0)
    }
    if(is(tc_deselect0, "NativeSymbolInfo")) {
      tc_deselect0 = tc_deselect0$address
    }
  }
  if(is.function(tc_getName0)) {
    assert('...' %in% names(formals(tc_getName0)) || length(formals(tc_getName0)) >= 1)
  } else {
    if(is.character(tc_getName0)) {
      tc_getName0 = getNativeSymbolInfo(tc_getName0)
    }
    if(is(tc_getName0, "NativeSymbolInfo")) {
      tc_getName0 = tc_getName0$address
    }
  }
  if(is.function(tc_getUniqueName0)) {
    assert('...' %in% names(formals(tc_getUniqueName0)) || length(formals(tc_getUniqueName0)) >= 0)
  } else {
    if(is.character(tc_getUniqueName0)) {
      tc_getUniqueName0 = getNativeSymbolInfo(tc_getUniqueName0)
    }
    if(is(tc_getUniqueName0, "NativeSymbolInfo")) {
      tc_getUniqueName0 = tc_getUniqueName0$address
    }
  }
  if(is.function(tc_setName0)) {
    assert('...' %in% names(formals(tc_setName0)) || length(formals(tc_setName0)) >= 2)
  } else {
    if(is.character(tc_setName0)) {
      tc_setName0 = getNativeSymbolInfo(tc_setName0)
    }
    if(is(tc_setName0, "NativeSymbolInfo")) {
      tc_setName0 = tc_setName0$address
    }
  }
  if(is.function(tc_getNames0)) {
    assert('...' %in% names(formals(tc_getNames0)) || length(formals(tc_getNames0)) >= 1)
  } else {
    if(is.character(tc_getNames0)) {
      tc_getNames0 = getNativeSymbolInfo(tc_getNames0)
    }
    if(is(tc_getNames0, "NativeSymbolInfo")) {
      tc_getNames0 = tc_getNames0$address
    }
  }
  if(is.function(tc_getUniqueNames0)) {
    assert('...' %in% names(formals(tc_getUniqueNames0)) || length(formals(tc_getUniqueNames0)) >= 0)
  } else {
    if(is.character(tc_getUniqueNames0)) {
      tc_getUniqueNames0 = getNativeSymbolInfo(tc_getUniqueNames0)
    }
    if(is(tc_getUniqueNames0, "NativeSymbolInfo")) {
      tc_getUniqueNames0 = tc_getUniqueNames0$address
    }
  }
  if(is.function(tc_getFamily0)) {
    assert('...' %in% names(formals(tc_getFamily0)) || length(formals(tc_getFamily0)) >= 0)
  } else {
    if(is.character(tc_getFamily0)) {
      tc_getFamily0 = getNativeSymbolInfo(tc_getFamily0)
    }
    if(is(tc_getFamily0, "NativeSymbolInfo")) {
      tc_getFamily0 = tc_getFamily0$address
    }
  }
  if(is.function(tc_isA0)) {
    assert('...' %in% names(formals(tc_isA0)) || length(formals(tc_isA0)) >= 2)
  } else {
    if(is.character(tc_isA0)) {
      tc_isA0 = getNativeSymbolInfo(tc_isA0)
    }
    if(is(tc_isA0, "NativeSymbolInfo")) {
      tc_isA0 = tc_isA0$address
    }
  }
  if(is.function(tc_clearText)) {
    assert('...' %in% names(formals(tc_clearText)) || length(formals(tc_clearText)) >= 0)
  } else {
    if(is.character(tc_clearText)) {
      tc_clearText = getNativeSymbolInfo(tc_clearText)
    }
    if(is(tc_clearText, "NativeSymbolInfo")) {
      tc_clearText = tc_clearText$address
    }
  }
  if(is.function(tc_outputText0)) {
    assert('...' %in% names(formals(tc_outputText0)) || length(formals(tc_outputText0)) >= 1)
  } else {
    if(is.character(tc_outputText0)) {
      tc_outputText0 = getNativeSymbolInfo(tc_outputText0)
    }
    if(is(tc_outputText0, "NativeSymbolInfo")) {
      tc_outputText0 = tc_outputText0$address
    }
  }
  if(is.function(tc_errorReport0)) {
    assert('...' %in% names(formals(tc_errorReport0)) || length(formals(tc_errorReport0)) >= 0)
  } else {
    if(is.character(tc_errorReport0)) {
      tc_errorReport0 = getNativeSymbolInfo(tc_errorReport0)
    }
    if(is(tc_errorReport0, "NativeSymbolInfo")) {
      tc_errorReport0 = tc_errorReport0$address
    }
  }
  if(is.function(tc_outputTable0)) {
    assert('...' %in% names(formals(tc_outputTable0)) || length(formals(tc_outputTable0)) >= 0)
  } else {
    if(is.character(tc_outputTable0)) {
      tc_outputTable0 = getNativeSymbolInfo(tc_outputTable0)
    }
    if(is(tc_outputTable0, "NativeSymbolInfo")) {
      tc_outputTable0 = tc_outputTable0$address
    }
  }
  if(is.function(tc_printFile0)) {
    assert('...' %in% names(formals(tc_printFile0)) || length(formals(tc_printFile0)) >= 0)
  } else {
    if(is.character(tc_printFile0)) {
      tc_printFile0 = getNativeSymbolInfo(tc_printFile0)
    }
    if(is(tc_printFile0, "NativeSymbolInfo")) {
      tc_printFile0 = tc_printFile0$address
    }
  }
  if(is.function(tc_removeItem0)) {
    assert('...' %in% names(formals(tc_removeItem0)) || length(formals(tc_removeItem0)) >= 0)
  } else {
    if(is.character(tc_removeItem0)) {
      tc_removeItem0 = getNativeSymbolInfo(tc_removeItem0)
    }
    if(is(tc_removeItem0, "NativeSymbolInfo")) {
      tc_removeItem0 = tc_removeItem0$address
    }
  }
  if(is.function(tc_getY0)) {
    assert('...' %in% names(formals(tc_getY0)) || length(formals(tc_getY0)) >= 1)
  } else {
    if(is.character(tc_getY0)) {
      tc_getY0 = getNativeSymbolInfo(tc_getY0)
    }
    if(is(tc_getY0, "NativeSymbolInfo")) {
      tc_getY0 = tc_getY0$address
    }
  }
  if(is.function(tc_getX0)) {
    assert('...' %in% names(formals(tc_getX0)) || length(formals(tc_getX0)) >= 0)
  } else {
    if(is.character(tc_getX0)) {
      tc_getX0 = getNativeSymbolInfo(tc_getX0)
    }
    if(is(tc_getX0, "NativeSymbolInfo")) {
      tc_getX0 = tc_getX0$address
    }
  }
  if(is.function(tc_getPos0)) {
    assert('...' %in% names(formals(tc_getPos0)) || length(formals(tc_getPos0)) >= 1)
  } else {
    if(is.character(tc_getPos0)) {
      tc_getPos0 = getNativeSymbolInfo(tc_getPos0)
    }
    if(is(tc_getPos0, "NativeSymbolInfo")) {
      tc_getPos0 = tc_getPos0$address
    }
  }
  if(is.function(tc_setPos0)) {
    assert('...' %in% names(formals(tc_setPos0)) || length(formals(tc_setPos0)) >= 3)
  } else {
    if(is.character(tc_setPos0)) {
      tc_setPos0 = getNativeSymbolInfo(tc_setPos0)
    }
    if(is(tc_setPos0, "NativeSymbolInfo")) {
      tc_setPos0 = tc_setPos0$address
    }
  }
  if(is.function(tc_setPos1)) {
    assert('...' %in% names(formals(tc_setPos1)) || length(formals(tc_setPos1)) >= 2)
  } else {
    if(is.character(tc_setPos1)) {
      tc_setPos1 = getNativeSymbolInfo(tc_setPos1)
    }
    if(is(tc_setPos1, "NativeSymbolInfo")) {
      tc_setPos1 = tc_setPos1$address
    }
  }
  if(is.function(tc_moveSelected0)) {
    assert('...' %in% names(formals(tc_moveSelected0)) || length(formals(tc_moveSelected0)) >= 2)
  } else {
    if(is.character(tc_moveSelected0)) {
      tc_moveSelected0 = getNativeSymbolInfo(tc_moveSelected0)
    }
    if(is(tc_moveSelected0, "NativeSymbolInfo")) {
      tc_moveSelected0 = tc_moveSelected0$address
    }
  }
  if(is.function(tc_isWindows0)) {
    assert('...' %in% names(formals(tc_isWindows0)) || length(formals(tc_isWindows0)) >= 0)
  } else {
    if(is.character(tc_isWindows0)) {
      tc_isWindows0 = getNativeSymbolInfo(tc_isWindows0)
    }
    if(is(tc_isWindows0, "NativeSymbolInfo")) {
      tc_isWindows0 = tc_isWindows0$address
    }
  }
  if(is.function(tc_isMac0)) {
    assert('...' %in% names(formals(tc_isMac0)) || length(formals(tc_isMac0)) >= 0)
  } else {
    if(is.character(tc_isMac0)) {
      tc_isMac0 = getNativeSymbolInfo(tc_isMac0)
    }
    if(is(tc_isMac0, "NativeSymbolInfo")) {
      tc_isMac0 = tc_isMac0$address
    }
  }
  if(is.function(tc_isLinux0)) {
    assert('...' %in% names(formals(tc_isLinux0)) || length(formals(tc_isLinux0)) >= 0)
  } else {
    if(is.character(tc_isLinux0)) {
      tc_isLinux0 = getNativeSymbolInfo(tc_isLinux0)
    }
    if(is(tc_isLinux0, "NativeSymbolInfo")) {
      tc_isLinux0 = tc_isLinux0$address
    }
  }
  if(is.function(tc_appDir0)) {
    assert('...' %in% names(formals(tc_appDir0)) || length(formals(tc_appDir0)) >= 0)
  } else {
    if(is.character(tc_appDir0)) {
      tc_appDir0 = getNativeSymbolInfo(tc_appDir0)
    }
    if(is(tc_appDir0, "NativeSymbolInfo")) {
      tc_appDir0 = tc_appDir0$address
    }
  }
  if(is.function(tc_homeDir0)) {
    assert('...' %in% names(formals(tc_homeDir0)) || length(formals(tc_homeDir0)) >= 0)
  } else {
    if(is.character(tc_homeDir0)) {
      tc_homeDir0 = getNativeSymbolInfo(tc_homeDir0)
    }
    if(is(tc_homeDir0, "NativeSymbolInfo")) {
      tc_homeDir0 = tc_homeDir0$address
    }
  }
  if(is.function(tc_createInputWindow0)) {
    assert('...' %in% names(formals(tc_createInputWindow0)) || length(formals(tc_createInputWindow0)) >= 3)
  } else {
    if(is.character(tc_createInputWindow0)) {
      tc_createInputWindow0 = getNativeSymbolInfo(tc_createInputWindow0)
    }
    if(is(tc_createInputWindow0, "NativeSymbolInfo")) {
      tc_createInputWindow0 = tc_createInputWindow0$address
    }
  }
  if(is.function(tc_createInputWindow1)) {
    assert('...' %in% names(formals(tc_createInputWindow1)) || length(formals(tc_createInputWindow1)) >= 4)
  } else {
    if(is.character(tc_createInputWindow1)) {
      tc_createInputWindow1 = getNativeSymbolInfo(tc_createInputWindow1)
    }
    if(is(tc_createInputWindow1, "NativeSymbolInfo")) {
      tc_createInputWindow1 = tc_createInputWindow1$address
    }
  }
  if(is.function(createSliders)) {
    assert('...' %in% names(formals(createSliders)) || length(formals(createSliders)) >= 3)
  } else {
    if(is.character(createSliders)) {
      createSliders = getNativeSymbolInfo(createSliders)
    }
    if(is(createSliders, "NativeSymbolInfo")) {
      createSliders = createSliders$address
    }
  }
  if(is.function(tc_addInputWindowOptions0)) {
    assert('...' %in% names(formals(tc_addInputWindowOptions0)) || length(formals(tc_addInputWindowOptions0)) >= 4)
  } else {
    if(is.character(tc_addInputWindowOptions0)) {
      tc_addInputWindowOptions0 = getNativeSymbolInfo(tc_addInputWindowOptions0)
    }
    if(is(tc_addInputWindowOptions0, "NativeSymbolInfo")) {
      tc_addInputWindowOptions0 = tc_addInputWindowOptions0$address
    }
  }
  if(is.function(tc_addInputWindowCheckbox0)) {
    assert('...' %in% names(formals(tc_addInputWindowCheckbox0)) || length(formals(tc_addInputWindowCheckbox0)) >= 3)
  } else {
    if(is.character(tc_addInputWindowCheckbox0)) {
      tc_addInputWindowCheckbox0 = getNativeSymbolInfo(tc_addInputWindowCheckbox0)
    }
    if(is(tc_addInputWindowCheckbox0, "NativeSymbolInfo")) {
      tc_addInputWindowCheckbox0 = tc_addInputWindowCheckbox0$address
    }
  }
  if(is.function(tc_openNewWindow0)) {
    assert('...' %in% names(formals(tc_openNewWindow0)) || length(formals(tc_openNewWindow0)) >= 0)
  } else {
    if(is.character(tc_openNewWindow0)) {
      tc_openNewWindow0 = getNativeSymbolInfo(tc_openNewWindow0)
    }
    if(is(tc_openNewWindow0, "NativeSymbolInfo")) {
      tc_openNewWindow0 = tc_openNewWindow0$address
    }
  }
  if(is.function(tc_getChildren0)) {
    assert('...' %in% names(formals(tc_getChildren0)) || length(formals(tc_getChildren0)) >= 1)
  } else {
    if(is.character(tc_getChildren0)) {
      tc_getChildren0 = getNativeSymbolInfo(tc_getChildren0)
    }
    if(is(tc_getChildren0, "NativeSymbolInfo")) {
      tc_getChildren0 = tc_getChildren0$address
    }
  }
  if(is.function(tc_getParent0)) {
    assert('...' %in% names(formals(tc_getParent0)) || length(formals(tc_getParent0)) >= 1)
  } else {
    if(is.character(tc_getParent0)) {
      tc_getParent0 = getNativeSymbolInfo(tc_getParent0)
    }
    if(is(tc_getParent0, "NativeSymbolInfo")) {
      tc_getParent0 = tc_getParent0$address
    }
  }
  if(is.function(tc_getNumericalData0)) {
    assert('...' %in% names(formals(tc_getNumericalData0)) || length(formals(tc_getNumericalData0)) >= 2)
  } else {
    if(is.character(tc_getNumericalData0)) {
      tc_getNumericalData0 = getNativeSymbolInfo(tc_getNumericalData0)
    }
    if(is(tc_getNumericalData0, "NativeSymbolInfo")) {
      tc_getNumericalData0 = tc_getNumericalData0$address
    }
  }
  if(is.function(tc_setNumericalData0)) {
    assert('...' %in% names(formals(tc_setNumericalData0)) || length(formals(tc_setNumericalData0)) >= 3)
  } else {
    if(is.character(tc_setNumericalData0)) {
      tc_setNumericalData0 = getNativeSymbolInfo(tc_setNumericalData0)
    }
    if(is(tc_setNumericalData0, "NativeSymbolInfo")) {
      tc_setNumericalData0 = tc_setNumericalData0$address
    }
  }
  if(is.function(tc_getTextData0)) {
    assert('...' %in% names(formals(tc_getTextData0)) || length(formals(tc_getTextData0)) >= 2)
  } else {
    if(is.character(tc_getTextData0)) {
      tc_getTextData0 = getNativeSymbolInfo(tc_getTextData0)
    }
    if(is(tc_getTextData0, "NativeSymbolInfo")) {
      tc_getTextData0 = tc_getTextData0$address
    }
  }
  if(is.function(tc_setTextData0)) {
    assert('...' %in% names(formals(tc_setTextData0)) || length(formals(tc_setTextData0)) >= 3)
  } else {
    if(is.character(tc_setTextData0)) {
      tc_setTextData0 = getNativeSymbolInfo(tc_setTextData0)
    }
    if(is(tc_setTextData0, "NativeSymbolInfo")) {
      tc_setTextData0 = tc_setTextData0$address
    }
  }
  if(is.function(tc_getNumericalDataNames0)) {
    assert('...' %in% names(formals(tc_getNumericalDataNames0)) || length(formals(tc_getNumericalDataNames0)) >= 1)
  } else {
    if(is.character(tc_getNumericalDataNames0)) {
      tc_getNumericalDataNames0 = getNativeSymbolInfo(tc_getNumericalDataNames0)
    }
    if(is(tc_getNumericalDataNames0, "NativeSymbolInfo")) {
      tc_getNumericalDataNames0 = tc_getNumericalDataNames0$address
    }
  }
  if(is.function(tc_getTextDataNames0)) {
    assert('...' %in% names(formals(tc_getTextDataNames0)) || length(formals(tc_getTextDataNames0)) >= 0)
  } else {
    if(is.character(tc_getTextDataNames0)) {
      tc_getTextDataNames0 = getNativeSymbolInfo(tc_getTextDataNames0)
    }
    if(is(tc_getTextDataNames0, "NativeSymbolInfo")) {
      tc_getTextDataNames0 = tc_getTextDataNames0$address
    }
  }
  if(is.function(tc_zoom0)) {
    assert('...' %in% names(formals(tc_zoom0)) || length(formals(tc_zoom0)) >= 1)
  } else {
    if(is.character(tc_zoom0)) {
      tc_zoom0 = getNativeSymbolInfo(tc_zoom0)
    }
    if(is(tc_zoom0, "NativeSymbolInfo")) {
      tc_zoom0 = tc_zoom0$address
    }
  }
  if(is.function(tc_getStringDialog0)) {
    assert('...' %in% names(formals(tc_getStringDialog0)) || length(formals(tc_getStringDialog0)) >= 1)
  } else {
    if(is.character(tc_getStringDialog0)) {
      tc_getStringDialog0 = getNativeSymbolInfo(tc_getStringDialog0)
    }
    if(is(tc_getStringDialog0, "NativeSymbolInfo")) {
      tc_getStringDialog0 = tc_getStringDialog0$address
    }
  }
  if(is.function(getSelectedString)) {
    assert('...' %in% names(formals(getSelectedString)) || length(formals(getSelectedString)) >= 3)
  } else {
    if(is.character(getSelectedString)) {
      getSelectedString = getNativeSymbolInfo(getSelectedString)
    }
    if(is(getSelectedString, "NativeSymbolInfo")) {
      getSelectedString = getSelectedString$address
    }
  }
  if(is.function(getNumber)) {
    assert('...' %in% names(formals(getNumber)) || length(formals(getNumber)) >= 1)
  } else {
    if(is.character(getNumber)) {
      getNumber = getNativeSymbolInfo(getNumber)
    }
    if(is(getNumber, "NativeSymbolInfo")) {
      getNumber = getNumber$address
    }
  }
  if(is.function(getNumbers)) {
    assert('...' %in% names(formals(getNumbers)) || length(formals(getNumbers)) >= 2)
  } else {
    if(is.character(getNumbers)) {
      getNumbers = getNativeSymbolInfo(getNumbers)
    }
    if(is(getNumbers, "NativeSymbolInfo")) {
      getNumbers = getNumbers$address
    }
  }
  if(is.function(getFilename)) {
    assert('...' %in% names(formals(getFilename)) || length(formals(getFilename)) >= 0)
  } else {
    if(is.character(getFilename)) {
      getFilename = getNativeSymbolInfo(getFilename)
    }
    if(is(getFilename, "NativeSymbolInfo")) {
      getFilename = getFilename$address
    }
  }
  if(is.function(askQuestion)) {
    assert('...' %in% names(formals(askQuestion)) || length(formals(askQuestion)) >= 1)
  } else {
    if(is.character(askQuestion)) {
      askQuestion = getNativeSymbolInfo(askQuestion)
    }
    if(is(askQuestion, "NativeSymbolInfo")) {
      askQuestion = askQuestion$address
    }
  }
  if(is.function(messageDialog)) {
    assert('...' %in% names(formals(messageDialog)) || length(formals(messageDialog)) >= 0)
  } else {
    if(is.character(messageDialog)) {
      messageDialog = getNativeSymbolInfo(messageDialog)
    }
    if(is(messageDialog, "NativeSymbolInfo")) {
      messageDialog = messageDialog$address
    }
  }
  if(is.function(openFile)) {
    assert('...' %in% names(formals(openFile)) || length(formals(openFile)) >= 0)
  } else {
    if(is.character(openFile)) {
      openFile = getNativeSymbolInfo(openFile)
    }
    if(is(openFile, "NativeSymbolInfo")) {
      openFile = openFile$address
    }
  }
  if(is.function(saveToFile)) {
    assert('...' %in% names(formals(saveToFile)) || length(formals(saveToFile)) >= 0)
  } else {
    if(is.character(saveToFile)) {
      saveToFile = getNativeSymbolInfo(saveToFile)
    }
    if(is(saveToFile, "NativeSymbolInfo")) {
      saveToFile = saveToFile$address
    }
  }
  if(is.function(setSize0)) {
    assert('...' %in% names(formals(setSize0)) || length(formals(setSize0)) >= 4)
  } else {
    if(is.character(setSize0)) {
      setSize0 = getNativeSymbolInfo(setSize0)
    }
    if(is(setSize0, "NativeSymbolInfo")) {
      setSize0 = setSize0$address
    }
  }
  if(is.function(getWidth0)) {
    assert('...' %in% names(formals(getWidth0)) || length(formals(getWidth0)) >= 0)
  } else {
    if(is.character(getWidth0)) {
      getWidth0 = getNativeSymbolInfo(getWidth0)
    }
    if(is(getWidth0, "NativeSymbolInfo")) {
      getWidth0 = getWidth0$address
    }
  }
  if(is.function(getHeight0)) {
    assert('...' %in% names(formals(getHeight0)) || length(formals(getHeight0)) >= 0)
  } else {
    if(is.character(getHeight0)) {
      getHeight0 = getNativeSymbolInfo(getHeight0)
    }
    if(is(getHeight0, "NativeSymbolInfo")) {
      getHeight0 = getHeight0$address
    }
  }
  if(is.function(setAngle0)) {
    assert('...' %in% names(formals(setAngle0)) || length(formals(setAngle0)) >= 3)
  } else {
    if(is.character(setAngle0)) {
      setAngle0 = getNativeSymbolInfo(setAngle0)
    }
    if(is(setAngle0, "NativeSymbolInfo")) {
      setAngle0 = setAngle0$address
    }
  }
  if(is.function(getColor)) {
    assert('...' %in% names(formals(getColor)) || length(formals(getColor)) >= 0)
  } else {
    if(is.character(getColor)) {
      getColor = getNativeSymbolInfo(getColor)
    }
    if(is(getColor, "NativeSymbolInfo")) {
      getColor = getColor$address
    }
  }
  if(is.function(setColor0)) {
    assert('...' %in% names(formals(setColor0)) || length(formals(setColor0)) >= 3)
  } else {
    if(is.character(setColor0)) {
      setColor0 = getNativeSymbolInfo(setColor0)
    }
    if(is(setColor0, "NativeSymbolInfo")) {
      setColor0 = setColor0$address
    }
  }
  if(is.function(changeGraphics0)) {
    assert('...' %in% names(formals(changeGraphics0)) || length(formals(changeGraphics0)) >= 0)
  } else {
    if(is.character(changeGraphics0)) {
      changeGraphics0 = getNativeSymbolInfo(changeGraphics0)
    }
    if(is(changeGraphics0, "NativeSymbolInfo")) {
      changeGraphics0 = changeGraphics0$address
    }
  }
  if(is.function(changeArrowHead0)) {
    assert('...' %in% names(formals(changeArrowHead0)) || length(formals(changeArrowHead0)) >= 0)
  } else {
    if(is.character(changeArrowHead0)) {
      changeArrowHead0 = getNativeSymbolInfo(changeArrowHead0)
    }
    if(is(changeArrowHead0, "NativeSymbolInfo")) {
      changeArrowHead0 = changeArrowHead0$address
    }
  }
  if(is.function(screenshot)) {
    assert('...' %in% names(formals(screenshot)) || length(formals(screenshot)) >= 0)
  } else {
    if(is.character(screenshot)) {
      screenshot = getNativeSymbolInfo(screenshot)
    }
    if(is(screenshot, "NativeSymbolInfo")) {
      screenshot = screenshot$address
    }
  }
  if(is.function(screenHeight)) {
    assert('...' %in% names(formals(screenHeight)) || length(formals(screenHeight)) >= 0)
  } else {
    if(is.character(screenHeight)) {
      screenHeight = getNativeSymbolInfo(screenHeight)
    }
    if(is(screenHeight, "NativeSymbolInfo")) {
      screenHeight = screenHeight$address
    }
  }
  if(is.function(screenWidth)) {
    assert('...' %in% names(formals(screenWidth)) || length(formals(screenWidth)) >= 0)
  } else {
    if(is.character(screenWidth)) {
      screenWidth = getNativeSymbolInfo(screenWidth)
    }
    if(is(screenWidth, "NativeSymbolInfo")) {
      screenWidth = screenWidth$address
    }
  }
  if(is.function(screenX)) {
    assert('...' %in% names(formals(screenX)) || length(formals(screenX)) >= 0)
  } else {
    if(is.character(screenX)) {
      screenX = getNativeSymbolInfo(screenX)
    }
    if(is(screenX, "NativeSymbolInfo")) {
      screenX = screenX$address
    }
  }
  if(is.function(screenY)) {
    assert('...' %in% names(formals(screenY)) || length(formals(screenY)) >= 0)
  } else {
    if(is.character(screenY)) {
      screenY = getNativeSymbolInfo(screenY)
    }
    if(is(screenY, "NativeSymbolInfo")) {
      screenY = screenY$address
    }
  }
  if(is.function(annotations)) {
    assert('...' %in% names(formals(annotations)) || length(formals(annotations)) >= 0)
  } else {
    if(is.character(annotations)) {
      annotations = getNativeSymbolInfo(annotations)
    }
    if(is(annotations, "NativeSymbolInfo")) {
      annotations = annotations$address
    }
  }
  if(is.function(insertAnnotations)) {
    assert('...' %in% names(formals(insertAnnotations)) || length(formals(insertAnnotations)) >= 3)
  } else {
    if(is.character(insertAnnotations)) {
      insertAnnotations = getNativeSymbolInfo(insertAnnotations)
    }
    if(is(insertAnnotations, "NativeSymbolInfo")) {
      insertAnnotations = insertAnnotations$address
    }
  }
  if(is.function(setNumericalValues)) {
    assert('...' %in% names(formals(setNumericalValues)) || length(formals(setNumericalValues)) >= 0)
  } else {
    if(is.character(setNumericalValues)) {
      setNumericalValues = getNativeSymbolInfo(setNumericalValues)
    }
    if(is(setNumericalValues, "NativeSymbolInfo")) {
      setNumericalValues = setNumericalValues$address
    }
  }
  if(is.function(setNumericalValue)) {
    assert('...' %in% names(formals(setNumericalValue)) || length(formals(setNumericalValue)) >= 2)
  } else {
    if(is.character(setNumericalValue)) {
      setNumericalValue = getNativeSymbolInfo(setNumericalValue)
    }
    if(is(setNumericalValue, "NativeSymbolInfo")) {
      setNumericalValue = setNumericalValue$address
    }
  }
  if(is.function(setTextValues)) {
    assert('...' %in% names(formals(setTextValues)) || length(formals(setTextValues)) >= 1)
  } else {
    if(is.character(setTextValues)) {
      setTextValues = getNativeSymbolInfo(setTextValues)
    }
    if(is(setTextValues, "NativeSymbolInfo")) {
      setTextValues = setTextValues$address
    }
  }
  if(is.function(setTextValue)) {
    assert('...' %in% names(formals(setTextValue)) || length(formals(setTextValue)) >= 2)
  } else {
    if(is.character(setTextValue)) {
      setTextValue = getNativeSymbolInfo(setTextValue)
    }
    if(is(setTextValue, "NativeSymbolInfo")) {
      setTextValue = setTextValue$address
    }
  }
  if(is.function(getNumericalValue)) {
    assert('...' %in% names(formals(getNumericalValue)) || length(formals(getNumericalValue)) >= 0)
  } else {
    if(is.character(getNumericalValue)) {
      getNumericalValue = getNativeSymbolInfo(getNumericalValue)
    }
    if(is(getNumericalValue, "NativeSymbolInfo")) {
      getNumericalValue = getNumericalValue$address
    }
  }
  if(is.function(getTextValue)) {
    assert('...' %in% names(formals(getTextValue)) || length(formals(getTextValue)) >= 0)
  } else {
    if(is.character(getTextValue)) {
      getTextValue = getNativeSymbolInfo(getTextValue)
    }
    if(is(getTextValue, "NativeSymbolInfo")) {
      getTextValue = getTextValue$address
    }
  }
  if(is.function(openUrl)) {
    assert('...' %in% names(formals(openUrl)) || length(formals(openUrl)) >= 0)
  } else {
    if(is.character(openUrl)) {
      openUrl = getNativeSymbolInfo(openUrl)
    }
    if(is(openUrl, "NativeSymbolInfo")) {
      openUrl = openUrl$address
    }
  }
  .Call('R_swig_tc_Main_api_initialize', tc_allItems0, tc_selectedItems0, tc_itemsOfFamily0, tc_itemsOfFamily1, tc_find0, tc_findItems0, tc_select0, tc_deselect0, tc_getName0, tc_getUniqueName0, tc_setName0, tc_getNames0, tc_getUniqueNames0, tc_getFamily0, tc_isA0, tc_clearText, tc_outputText0, tc_errorReport0, tc_outputTable0, tc_printFile0, tc_removeItem0, tc_getY0, tc_getX0, tc_getPos0, tc_setPos0, tc_setPos1, tc_moveSelected0, tc_isWindows0, tc_isMac0, tc_isLinux0, tc_appDir0, tc_homeDir0, tc_createInputWindow0, tc_createInputWindow1, createSliders, tc_addInputWindowOptions0, tc_addInputWindowCheckbox0, tc_openNewWindow0, tc_getChildren0, tc_getParent0, tc_getNumericalData0, tc_setNumericalData0, tc_getTextData0, tc_setTextData0, tc_getNumericalDataNames0, tc_getTextDataNames0, tc_zoom0, tc_getStringDialog0, getSelectedString, getNumber, getNumbers, getFilename, askQuestion, messageDialog, openFile, saveToFile, setSize0, getWidth0, getHeight0, setAngle0, getColor, setColor0, changeGraphics0, changeArrowHead0, screenshot, screenHeight, screenWidth, screenX, screenY, annotations, insertAnnotations, setNumericalValues, setNumericalValue, setTextValues, setTextValue, getNumericalValue, getTextValue, openUrl, PACKAGE='tinkercell')
  
}

attr(`tc_Main_api_initialize`, 'returnType') = 'void'
attr(`tc_Main_api_initialize`, "inputTypes") = c('_p_f___tc_items', '_p_f___tc_items', '_p_f_p_q_const__char__tc_items', '_p_f_p_q_const__char_tc_items__tc_items', '_p_f_p_q_const__char__long', '_p_f_tc_strings__tc_items', '_p_f_long__void', '_p_f___void', '_p_f_long__p_char', '_p_f_long__p_char', '_p_f_long_p_q_const__char__void', '_p_f_tc_items__tc_strings', '_p_f_tc_items__tc_strings', '_p_f_long__p_char', '_p_f_long_p_q_const__char__int', '_p_f___void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_tc_matrix__void', '_p_f_p_q_const__char__void', '_p_f_long__void', '_p_f_long__double', '_p_f_long__double', '_p_f_tc_items__tc_matrix', '_p_f_long_double_double__void', '_p_f_tc_items_tc_matrix__void', '_p_f_double_double__void', '_p_f___int', '_p_f___int', '_p_f___int', '_p_f___p_char', '_p_f___p_char', '_p_f_tc_matrix_p_q_const__char_p_q_const__char__void', '_p_f_long_tc_matrix_p_q_const__char_p_f_tc_matrix__void__void', '_p_f_long_tc_matrix_p_f_tc_matrix__void__void', '_p_f_p_q_const__char_int_int_tc_strings__void', '_p_f_p_q_const__char_int_int__void', '_p_f_p_q_const__char__void', '_p_f_long__tc_items', '_p_f_long__long', '_p_f_long_p_q_const__char__tc_matrix', '_p_f_long_p_q_const__char_tc_matrix__void', '_p_f_long_p_q_const__char__tc_table', '_p_f_long_p_q_const__char_tc_table__void', '_p_f_long__tc_strings', '_p_f_long__tc_strings', '_p_f_double__void', '_p_f_p_q_const__char__p_char', '_p_f_p_q_const__char_tc_strings_p_q_const__char__int', '_p_f_p_q_const__char__double', '_p_f_tc_strings_p_double__void', '_p_f___p_char', '_p_f_p_q_const__char__int', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_long_double_double_int__void', '_p_f_long__double', '_p_f_long__double', '_p_f_long_double_int__void', '_p_f_long__p_char', '_p_f_long_p_q_const__char_int__void', '_p_f_long_p_q_const__char__void', '_p_f_long_p_q_const__char__void', '_p_f_p_q_const__char_int_int__void', '_p_f___int', '_p_f___int', '_p_f___int', '_p_f___int', '_p_f___p_char', '_p_f_p_q_const__char_double_double__void', '_p_f_tc_matrix__void', '_p_f_p_q_const__char_double__void', '_p_f_tc_table__void', '_p_f_p_q_const__char_p_q_const__char__void', '_p_f_p_q_const__char__double', '_p_f_p_q_const__char__p_char', '_p_f___void')
class(`tc_Main_api_initialize`) = c("SWIGFunction", class('tc_Main_api_initialize'))

# Start of tc_showProgress

`tc_showProgress` = function(title, progress)
{
  title = as(title, "character") 
  progress = as.integer(progress) 
  
  if(length(progress) > 1) {
    warning("using only the first element of progress")
  }
  
  .Call('R_swig_tc_showProgress', title, progress, PACKAGE='tinkercell')
  
}

attr(`tc_showProgress`, 'returnType') = 'void'
attr(`tc_showProgress`, "inputTypes") = c('character', 'integer')
class(`tc_showProgress`) = c("SWIGFunction", class('tc_showProgress'))

# Start of tc_callback

`tc_callback` = function(f)
{
  if(is.function(f)) {
    assert('...' %in% names(formals(f)) || length(formals(f)) >= 1)
  } else {
    if(is.character(f)) {
      f = getNativeSymbolInfo(f)
    }
    if(is(f, "NativeSymbolInfo")) {
      f = f$address
    }
  }
  .Call('R_swig_tc_callback', f, PACKAGE='tinkercell')
  
}

attr(`tc_callback`, 'returnType') = 'void'
attr(`tc_callback`, "inputTypes") = c('_p_f_void__void')
class(`tc_callback`) = c("SWIGFunction", class('tc_callback'))

# Start of tc_callWhenExiting

`tc_callWhenExiting` = function(f)
{
  if(is.function(f)) {
    assert('...' %in% names(formals(f)) || length(formals(f)) >= 0)
  } else {
    if(is.character(f)) {
      f = getNativeSymbolInfo(f)
    }
    if(is(f, "NativeSymbolInfo")) {
      f = f$address
    }
  }
  .Call('R_swig_tc_callWhenExiting', f, PACKAGE='tinkercell')
  
}

attr(`tc_callWhenExiting`, 'returnType') = 'void'
attr(`tc_callWhenExiting`, "inputTypes") = c('_p_f_void__void')
class(`tc_callWhenExiting`) = c("SWIGFunction", class('tc_callWhenExiting'))

# Start of tc_CThread_api_initialize

`tc_CThread_api_initialize` = function(cthread, callback, callWhenExiting, showProgress)
{
  cthread = as.integer(cthread) 
  
  if(length(cthread) > 1) {
    warning("using only the first element of cthread")
  }
  
  if(is.function(callback)) {
    assert('...' %in% names(formals(callback)) || length(formals(callback)) >= 2)
  } else {
    if(is.character(callback)) {
      callback = getNativeSymbolInfo(callback)
    }
    if(is(callback, "NativeSymbolInfo")) {
      callback = callback$address
    }
  }
  if(is.function(callWhenExiting)) {
    assert('...' %in% names(formals(callWhenExiting)) || length(formals(callWhenExiting)) >= 0)
  } else {
    if(is.character(callWhenExiting)) {
      callWhenExiting = getNativeSymbolInfo(callWhenExiting)
    }
    if(is(callWhenExiting, "NativeSymbolInfo")) {
      callWhenExiting = callWhenExiting$address
    }
  }
  if(is.function(showProgress)) {
    assert('...' %in% names(formals(showProgress)) || length(formals(showProgress)) >= 0)
  } else {
    if(is.character(showProgress)) {
      showProgress = getNativeSymbolInfo(showProgress)
    }
    if(is(showProgress, "NativeSymbolInfo")) {
      showProgress = showProgress$address
    }
  }
  .Call('R_swig_tc_CThread_api_initialize', cthread, callback, callWhenExiting, showProgress, PACKAGE='tinkercell')
  
}

attr(`tc_CThread_api_initialize`, 'returnType') = 'void'
attr(`tc_CThread_api_initialize`, "inputTypes") = c('integer', '_p_f_long_p_f_void__void__void', '_p_f_long_p_f_void__void__void', '_p_f_long_p_q_const__char_int__void')
class(`tc_CThread_api_initialize`) = c("SWIGFunction", class('tc_CThread_api_initialize'))

# Start of tc_displayText

`tc_displayText` = function(item, text)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  text = as(text, "character") 
  .Call('R_swig_tc_displayText', item, text, PACKAGE='tinkercell')
  
}

attr(`tc_displayText`, 'returnType') = 'void'
attr(`tc_displayText`, "inputTypes") = c('integer', 'character')
class(`tc_displayText`) = c("SWIGFunction", class('tc_displayText'))

# Start of tc_displayNumber

`tc_displayNumber` = function(item, number)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  .Call('R_swig_tc_displayNumber', item, number, PACKAGE='tinkercell')
  
}

attr(`tc_displayNumber`, 'returnType') = 'void'
attr(`tc_displayNumber`, "inputTypes") = c('integer', 'numeric')
class(`tc_displayNumber`) = c("SWIGFunction", class('tc_displayNumber'))

# Start of tc_setDisplayLabelColor

`tc_setDisplayLabelColor` = function(color1, color2)
{
  color1 = as(color1, "character") 
  color2 = as(color2, "character") 
  .Call('R_swig_tc_setDisplayLabelColor', color1, color2, PACKAGE='tinkercell')
  
}

attr(`tc_setDisplayLabelColor`, 'returnType') = 'void'
attr(`tc_setDisplayLabelColor`, "inputTypes") = c('character', 'character')
class(`tc_setDisplayLabelColor`) = c("SWIGFunction", class('tc_setDisplayLabelColor'))

# Start of tc_highlight

`tc_highlight` = function(item, color)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  color = as(color, "character") 
  .Call('R_swig_tc_highlight', item, color, PACKAGE='tinkercell')
  
}

attr(`tc_highlight`, 'returnType') = 'void'
attr(`tc_highlight`, "inputTypes") = c('integer', 'character')
class(`tc_highlight`) = c("SWIGFunction", class('tc_highlight'))

# Start of tc_burn

`tc_burn` = function(item, intensity)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  .Call('R_swig_tc_burn', item, intensity, PACKAGE='tinkercell')
  
}

attr(`tc_burn`, 'returnType') = 'void'
attr(`tc_burn`, "inputTypes") = c('integer', 'numeric')
class(`tc_burn`) = c("SWIGFunction", class('tc_burn'))

# Start of tc_LabelingTool_api

`tc_LabelingTool_api` = function(displayText, displayNumber, setDisplayLabelColor, highlight, burn)
{
  if(is.function(displayText)) {
    assert('...' %in% names(formals(displayText)) || length(formals(displayText)) >= 0)
  } else {
    if(is.character(displayText)) {
      displayText = getNativeSymbolInfo(displayText)
    }
    if(is(displayText, "NativeSymbolInfo")) {
      displayText = displayText$address
    }
  }
  if(is.function(displayNumber)) {
    assert('...' %in% names(formals(displayNumber)) || length(formals(displayNumber)) >= 2)
  } else {
    if(is.character(displayNumber)) {
      displayNumber = getNativeSymbolInfo(displayNumber)
    }
    if(is(displayNumber, "NativeSymbolInfo")) {
      displayNumber = displayNumber$address
    }
  }
  if(is.function(setDisplayLabelColor)) {
    assert('...' %in% names(formals(setDisplayLabelColor)) || length(formals(setDisplayLabelColor)) >= 0)
  } else {
    if(is.character(setDisplayLabelColor)) {
      setDisplayLabelColor = getNativeSymbolInfo(setDisplayLabelColor)
    }
    if(is(setDisplayLabelColor, "NativeSymbolInfo")) {
      setDisplayLabelColor = setDisplayLabelColor$address
    }
  }
  if(is.function(highlight)) {
    assert('...' %in% names(formals(highlight)) || length(formals(highlight)) >= 0)
  } else {
    if(is.character(highlight)) {
      highlight = getNativeSymbolInfo(highlight)
    }
    if(is(highlight, "NativeSymbolInfo")) {
      highlight = highlight$address
    }
  }
  if(is.function(burn)) {
    assert('...' %in% names(formals(burn)) || length(formals(burn)) >= 0)
  } else {
    if(is.character(burn)) {
      burn = getNativeSymbolInfo(burn)
    }
    if(is(burn, "NativeSymbolInfo")) {
      burn = burn$address
    }
  }
  .Call('R_swig_tc_LabelingTool_api', displayText, displayNumber, setDisplayLabelColor, highlight, burn, PACKAGE='tinkercell')
  
}

attr(`tc_LabelingTool_api`, 'returnType') = 'void'
attr(`tc_LabelingTool_api`, "inputTypes") = c('_p_f_long_p_q_const__char__void', '_p_f_long_double__void', '_p_f_p_q_const__char_p_q_const__char__void', '_p_f_long_p_q_const__char__void', '_p_f_long_double__void')
class(`tc_LabelingTool_api`) = c("SWIGFunction", class('tc_LabelingTool_api'))

# Start of tc_getParameters

`tc_getParameters` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getParameters', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getParameters`, 'returnType') = '_p_tc_matrix'
attr(`tc_getParameters`, "inputTypes") = c('_p_tc_items')
class(`tc_getParameters`) = c("SWIGFunction", class('tc_getParameters'))

# Start of tc_getInitialValues

`tc_getInitialValues` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getInitialValues', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getInitialValues`, 'returnType') = '_p_tc_matrix'
attr(`tc_getInitialValues`, "inputTypes") = c('_p_tc_items')
class(`tc_getInitialValues`) = c("SWIGFunction", class('tc_getInitialValues'))

# Start of tc_setInitialValues

`tc_setInitialValues` = function(items, values)
{
  .Call('R_swig_tc_setInitialValues', items, values, PACKAGE='tinkercell')
  
}

attr(`tc_setInitialValues`, 'returnType') = 'void'
attr(`tc_setInitialValues`, "inputTypes") = c('_p_tc_items', '_p_tc_matrix')
class(`tc_setInitialValues`) = c("SWIGFunction", class('tc_setInitialValues'))

# Start of tc_getFixedVariables

`tc_getFixedVariables` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getFixedVariables', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getFixedVariables`, 'returnType') = '_p_tc_matrix'
attr(`tc_getFixedVariables`, "inputTypes") = c('_p_tc_items')
class(`tc_getFixedVariables`) = c("SWIGFunction", class('tc_getFixedVariables'))

# Start of tc_getParametersAndFixedVariables

`tc_getParametersAndFixedVariables` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getParametersAndFixedVariables', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getParametersAndFixedVariables`, 'returnType') = '_p_tc_matrix'
attr(`tc_getParametersAndFixedVariables`, "inputTypes") = c('_p_tc_items')
class(`tc_getParametersAndFixedVariables`) = c("SWIGFunction", class('tc_getParametersAndFixedVariables'))

# Start of tc_getTextAttribute

`tc_getTextAttribute` = function(item, attribute)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  attribute = as(attribute, "character") 
  .Call('R_swig_tc_getTextAttribute', item, attribute, PACKAGE='tinkercell')
  
}

attr(`tc_getTextAttribute`, 'returnType') = 'character'
attr(`tc_getTextAttribute`, "inputTypes") = c('integer', 'character')
class(`tc_getTextAttribute`) = c("SWIGFunction", class('tc_getTextAttribute'))

# Start of tc_getParameter

`tc_getParameter` = function(item, attribute, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  attribute = as(attribute, "character") 
  .Call('R_swig_tc_getParameter', item, attribute, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getParameter`, 'returnType') = 'numeric'
attr(`tc_getParameter`, "inputTypes") = c('integer', 'character')
class(`tc_getParameter`) = c("SWIGFunction", class('tc_getParameter'))

# Start of tc_getParametersNamed

`tc_getParametersNamed` = function(a, attibutes, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getParametersNamed', a, attibutes, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getParametersNamed`, 'returnType') = '_p_tc_matrix'
attr(`tc_getParametersNamed`, "inputTypes") = c('_p_tc_items', '_p_tc_strings')
class(`tc_getParametersNamed`) = c("SWIGFunction", class('tc_getParametersNamed'))

# Start of tc_getParametersExcept

`tc_getParametersExcept` = function(a, attributes, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getParametersExcept', a, attributes, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getParametersExcept`, 'returnType') = '_p_tc_matrix'
attr(`tc_getParametersExcept`, "inputTypes") = c('_p_tc_items', '_p_tc_strings')
class(`tc_getParametersExcept`) = c("SWIGFunction", class('tc_getParametersExcept'))

# Start of tc_getAllTextNamed

`tc_getAllTextNamed` = function(a, attributes, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getAllTextNamed', a, attributes, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getAllTextNamed`, 'returnType') = '_p_tc_strings'
attr(`tc_getAllTextNamed`, "inputTypes") = c('_p_tc_items', '_p_tc_strings')
class(`tc_getAllTextNamed`) = c("SWIGFunction", class('tc_getAllTextNamed'))

# Start of tc_setTextAttribute

`tc_setTextAttribute` = function(item, attribute, value)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  attribute = as(attribute, "character") 
  value = as(value, "character") 
  .Call('R_swig_tc_setTextAttribute', item, attribute, value, PACKAGE='tinkercell')
  
}

attr(`tc_setTextAttribute`, 'returnType') = 'void'
attr(`tc_setTextAttribute`, "inputTypes") = c('integer', 'character', 'character')
class(`tc_setTextAttribute`) = c("SWIGFunction", class('tc_setTextAttribute'))

# Start of tc_setParameter

`tc_setParameter` = function(item, attribute, value)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  attribute = as(attribute, "character") 
  
  .Call('R_swig_tc_setParameter', item, attribute, value, PACKAGE='tinkercell')
  
}

attr(`tc_setParameter`, 'returnType') = 'void'
attr(`tc_setParameter`, "inputTypes") = c('integer', 'character', 'numeric')
class(`tc_setParameter`) = c("SWIGFunction", class('tc_setParameter'))

# Start of tc_setTextAttributeByName

`tc_setTextAttributeByName` = function(attribute, value)
{
  attribute = as(attribute, "character") 
  value = as(value, "character") 
  .Call('R_swig_tc_setTextAttributeByName', attribute, value, PACKAGE='tinkercell')
  
}

attr(`tc_setTextAttributeByName`, 'returnType') = 'void'
attr(`tc_setTextAttributeByName`, "inputTypes") = c('character', 'character')
class(`tc_setTextAttributeByName`) = c("SWIGFunction", class('tc_setTextAttributeByName'))

# Start of tc_setParameterByName

`tc_setParameterByName` = function(attribute, value)
{
  attribute = as(attribute, "character") 
  
  .Call('R_swig_tc_setParameterByName', attribute, value, PACKAGE='tinkercell')
  
}

attr(`tc_setParameterByName`, 'returnType') = 'void'
attr(`tc_setParameterByName`, "inputTypes") = c('character', 'numeric')
class(`tc_setParameterByName`) = c("SWIGFunction", class('tc_setParameterByName'))

# Start of tc_setTextAttributes

`tc_setTextAttributes` = function(s_arg1)
{
  .Call('R_swig_tc_setTextAttributes', s_arg1, PACKAGE='tinkercell')
  
}

attr(`tc_setTextAttributes`, 'returnType') = 'void'
attr(`tc_setTextAttributes`, "inputTypes") = c('_p_tc_table')
class(`tc_setTextAttributes`) = c("SWIGFunction", class('tc_setTextAttributes'))

# Start of tc_setParameters

`tc_setParameters` = function(s_arg1, s_arg2)
{
  s_arg2 = as.integer(s_arg2) 
  
  if(length(s_arg2) > 1) {
    warning("using only the first element of s_arg2")
  }
  
  .Call('R_swig_tc_setParameters', s_arg1, s_arg2, PACKAGE='tinkercell')
  
}

attr(`tc_setParameters`, 'returnType') = 'void'
attr(`tc_setParameters`, "inputTypes") = c('_p_tc_matrix', 'integer')
class(`tc_setParameters`) = c("SWIGFunction", class('tc_setParameters'))

# Start of tc_BasicInformationTool_Text_api

`tc_BasicInformationTool_Text_api` = function(getTextData, getAllTextDataNamed, setTextData)
{
  if(is.function(getTextData)) {
    assert('...' %in% names(formals(getTextData)) || length(formals(getTextData)) >= 2)
  } else {
    if(is.character(getTextData)) {
      getTextData = getNativeSymbolInfo(getTextData)
    }
    if(is(getTextData, "NativeSymbolInfo")) {
      getTextData = getTextData$address
    }
  }
  if(is.function(getAllTextDataNamed)) {
    assert('...' %in% names(formals(getAllTextDataNamed)) || length(formals(getAllTextDataNamed)) >= 2)
  } else {
    if(is.character(getAllTextDataNamed)) {
      getAllTextDataNamed = getNativeSymbolInfo(getAllTextDataNamed)
    }
    if(is(getAllTextDataNamed, "NativeSymbolInfo")) {
      getAllTextDataNamed = getAllTextDataNamed$address
    }
  }
  if(is.function(setTextData)) {
    assert('...' %in% names(formals(setTextData)) || length(formals(setTextData)) >= 3)
  } else {
    if(is.character(setTextData)) {
      setTextData = getNativeSymbolInfo(setTextData)
    }
    if(is(setTextData, "NativeSymbolInfo")) {
      setTextData = setTextData$address
    }
  }
  .Call('R_swig_tc_BasicInformationTool_Text_api', getTextData, getAllTextDataNamed, setTextData, PACKAGE='tinkercell')
  
}

attr(`tc_BasicInformationTool_Text_api`, 'returnType') = 'void'
attr(`tc_BasicInformationTool_Text_api`, "inputTypes") = c('_p_f_long_p_q_const__char__p_char', '_p_f_tc_items_tc_strings__tc_strings', '_p_f_long_p_q_const__char_p_q_const__char__void')
class(`tc_BasicInformationTool_Text_api`) = c("SWIGFunction", class('tc_BasicInformationTool_Text_api'))

# Start of tc_BasicInformationTool_Numeric_api

`tc_BasicInformationTool_Numeric_api` = function(getInitialValues, setInitialValues, getParameters, getFixedVariabes, getParametersAndFixedVariabes, getNumericalData, getParametersNamed, getParametersExcept, setNumericalData)
{
  if(is.function(getInitialValues)) {
    assert('...' %in% names(formals(getInitialValues)) || length(formals(getInitialValues)) >= 0)
  } else {
    if(is.character(getInitialValues)) {
      getInitialValues = getNativeSymbolInfo(getInitialValues)
    }
    if(is(getInitialValues, "NativeSymbolInfo")) {
      getInitialValues = getInitialValues$address
    }
  }
  if(is.function(setInitialValues)) {
    assert('...' %in% names(formals(setInitialValues)) || length(formals(setInitialValues)) >= 0)
  } else {
    if(is.character(setInitialValues)) {
      setInitialValues = getNativeSymbolInfo(setInitialValues)
    }
    if(is(setInitialValues, "NativeSymbolInfo")) {
      setInitialValues = setInitialValues$address
    }
  }
  if(is.function(getParameters)) {
    assert('...' %in% names(formals(getParameters)) || length(formals(getParameters)) >= 0)
  } else {
    if(is.character(getParameters)) {
      getParameters = getNativeSymbolInfo(getParameters)
    }
    if(is(getParameters, "NativeSymbolInfo")) {
      getParameters = getParameters$address
    }
  }
  if(is.function(getFixedVariabes)) {
    assert('...' %in% names(formals(getFixedVariabes)) || length(formals(getFixedVariabes)) >= 0)
  } else {
    if(is.character(getFixedVariabes)) {
      getFixedVariabes = getNativeSymbolInfo(getFixedVariabes)
    }
    if(is(getFixedVariabes, "NativeSymbolInfo")) {
      getFixedVariabes = getFixedVariabes$address
    }
  }
  if(is.function(getParametersAndFixedVariabes)) {
    assert('...' %in% names(formals(getParametersAndFixedVariabes)) || length(formals(getParametersAndFixedVariabes)) >= 0)
  } else {
    if(is.character(getParametersAndFixedVariabes)) {
      getParametersAndFixedVariabes = getNativeSymbolInfo(getParametersAndFixedVariabes)
    }
    if(is(getParametersAndFixedVariabes, "NativeSymbolInfo")) {
      getParametersAndFixedVariabes = getParametersAndFixedVariabes$address
    }
  }
  if(is.function(getNumericalData)) {
    assert('...' %in% names(formals(getNumericalData)) || length(formals(getNumericalData)) >= 2)
  } else {
    if(is.character(getNumericalData)) {
      getNumericalData = getNativeSymbolInfo(getNumericalData)
    }
    if(is(getNumericalData, "NativeSymbolInfo")) {
      getNumericalData = getNumericalData$address
    }
  }
  if(is.function(getParametersNamed)) {
    assert('...' %in% names(formals(getParametersNamed)) || length(formals(getParametersNamed)) >= 2)
  } else {
    if(is.character(getParametersNamed)) {
      getParametersNamed = getNativeSymbolInfo(getParametersNamed)
    }
    if(is(getParametersNamed, "NativeSymbolInfo")) {
      getParametersNamed = getParametersNamed$address
    }
  }
  if(is.function(getParametersExcept)) {
    assert('...' %in% names(formals(getParametersExcept)) || length(formals(getParametersExcept)) >= 0)
  } else {
    if(is.character(getParametersExcept)) {
      getParametersExcept = getNativeSymbolInfo(getParametersExcept)
    }
    if(is(getParametersExcept, "NativeSymbolInfo")) {
      getParametersExcept = getParametersExcept$address
    }
  }
  if(is.function(setNumericalData)) {
    assert('...' %in% names(formals(setNumericalData)) || length(formals(setNumericalData)) >= 3)
  } else {
    if(is.character(setNumericalData)) {
      setNumericalData = getNativeSymbolInfo(setNumericalData)
    }
    if(is(setNumericalData, "NativeSymbolInfo")) {
      setNumericalData = setNumericalData$address
    }
  }
  .Call('R_swig_tc_BasicInformationTool_Numeric_api', getInitialValues, setInitialValues, getParameters, getFixedVariabes, getParametersAndFixedVariabes, getNumericalData, getParametersNamed, getParametersExcept, setNumericalData, PACKAGE='tinkercell')
  
}

attr(`tc_BasicInformationTool_Numeric_api`, 'returnType') = 'void'
attr(`tc_BasicInformationTool_Numeric_api`, "inputTypes") = c('_p_f_tc_items__tc_matrix', '_p_f_tc_items_tc_matrix__void', '_p_f_tc_items__tc_matrix', '_p_f_tc_items__tc_matrix', '_p_f_tc_items__tc_matrix', '_p_f_long_p_q_const__char__double', '_p_f_tc_items_tc_strings__tc_matrix', '_p_f_tc_items_tc_strings__tc_matrix', '_p_f_long_p_q_const__char_double__void')
class(`tc_BasicInformationTool_Numeric_api`) = c("SWIGFunction", class('tc_BasicInformationTool_Numeric_api'))

# Start of tc_insertConnection

`tc_insertConnection` = function(parts, name, family, .copy = FALSE)
{
  name = as(name, "character") 
  family = as(family, "character") 
  .Call('R_swig_tc_insertConnection', parts, name, family, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_insertConnection`, 'returnType') = 'integer'
attr(`tc_insertConnection`, "inputTypes") = c('_p_tc_items', 'character', 'character')
class(`tc_insertConnection`) = c("SWIGFunction", class('tc_insertConnection'))

# Start of tc_getConnectedNodes

`tc_getConnectedNodes` = function(connection, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  ans = .Call('R_swig_tc_getConnectedNodes', connection, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_getConnectedNodes`, 'returnType') = '_p_tc_items'
attr(`tc_getConnectedNodes`, "inputTypes") = c('integer')
class(`tc_getConnectedNodes`) = c("SWIGFunction", class('tc_getConnectedNodes'))

# Start of tc_getConnectedNodesWithRole

`tc_getConnectedNodesWithRole` = function(connection, role, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  role = as(role, "character") 
  ans = .Call('R_swig_tc_getConnectedNodesWithRole', connection, role, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_getConnectedNodesWithRole`, 'returnType') = '_p_tc_items'
attr(`tc_getConnectedNodesWithRole`, "inputTypes") = c('integer', 'character')
class(`tc_getConnectedNodesWithRole`) = c("SWIGFunction", class('tc_getConnectedNodesWithRole'))

# Start of tc_getConnections

`tc_getConnections` = function(part, .copy = FALSE)
{
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  ans = .Call('R_swig_tc_getConnections', part, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_getConnections`, 'returnType') = '_p_tc_items'
attr(`tc_getConnections`, "inputTypes") = c('integer')
class(`tc_getConnections`) = c("SWIGFunction", class('tc_getConnections'))

# Start of tc_getConnectionsWithRole

`tc_getConnectionsWithRole` = function(part, role, .copy = FALSE)
{
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  role = as(role, "character") 
  ans = .Call('R_swig_tc_getConnectionsWithRole', part, role, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_getConnectionsWithRole`, 'returnType') = '_p_tc_items'
attr(`tc_getConnectionsWithRole`, "inputTypes") = c('integer', 'character')
class(`tc_getConnectionsWithRole`) = c("SWIGFunction", class('tc_getConnectionsWithRole'))

# Start of tc_ConnectionInsertion_api

`tc_ConnectionInsertion_api` = function(insertConnection, getConnectedParts, getConnectedPartsWithRole, getConnections, getConnectionsWithRole)
{
  if(is.function(insertConnection)) {
    assert('...' %in% names(formals(insertConnection)) || length(formals(insertConnection)) >= 3)
  } else {
    if(is.character(insertConnection)) {
      insertConnection = getNativeSymbolInfo(insertConnection)
    }
    if(is(insertConnection, "NativeSymbolInfo")) {
      insertConnection = insertConnection$address
    }
  }
  if(is.function(getConnectedParts)) {
    assert('...' %in% names(formals(getConnectedParts)) || length(formals(getConnectedParts)) >= 0)
  } else {
    if(is.character(getConnectedParts)) {
      getConnectedParts = getNativeSymbolInfo(getConnectedParts)
    }
    if(is(getConnectedParts, "NativeSymbolInfo")) {
      getConnectedParts = getConnectedParts$address
    }
  }
  if(is.function(getConnectedPartsWithRole)) {
    assert('...' %in% names(formals(getConnectedPartsWithRole)) || length(formals(getConnectedPartsWithRole)) >= 2)
  } else {
    if(is.character(getConnectedPartsWithRole)) {
      getConnectedPartsWithRole = getNativeSymbolInfo(getConnectedPartsWithRole)
    }
    if(is(getConnectedPartsWithRole, "NativeSymbolInfo")) {
      getConnectedPartsWithRole = getConnectedPartsWithRole$address
    }
  }
  if(is.function(getConnections)) {
    assert('...' %in% names(formals(getConnections)) || length(formals(getConnections)) >= 0)
  } else {
    if(is.character(getConnections)) {
      getConnections = getNativeSymbolInfo(getConnections)
    }
    if(is(getConnections, "NativeSymbolInfo")) {
      getConnections = getConnections$address
    }
  }
  if(is.function(getConnectionsWithRole)) {
    assert('...' %in% names(formals(getConnectionsWithRole)) || length(formals(getConnectionsWithRole)) >= 0)
  } else {
    if(is.character(getConnectionsWithRole)) {
      getConnectionsWithRole = getNativeSymbolInfo(getConnectionsWithRole)
    }
    if(is(getConnectionsWithRole, "NativeSymbolInfo")) {
      getConnectionsWithRole = getConnectionsWithRole$address
    }
  }
  .Call('R_swig_tc_ConnectionInsertion_api', insertConnection, getConnectedParts, getConnectedPartsWithRole, getConnections, getConnectionsWithRole, PACKAGE='tinkercell')
  
}

attr(`tc_ConnectionInsertion_api`, 'returnType') = 'void'
attr(`tc_ConnectionInsertion_api`, "inputTypes") = c('_p_f_tc_items_p_q_const__char_p_q_const__char__long', '_p_f_long__tc_items', '_p_f_long_p_q_const__char__tc_items', '_p_f_long__tc_items', '_p_f_long_p_q_const__char__tc_items')
class(`tc_ConnectionInsertion_api`) = c("SWIGFunction", class('tc_ConnectionInsertion_api'))

# Start of tc_getControlPointX

`tc_getControlPointX` = function(connection, part, whichPoint, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  whichPoint = as.integer(whichPoint) 
  
  if(length(whichPoint) > 1) {
    warning("using only the first element of whichPoint")
  }
  
  .Call('R_swig_tc_getControlPointX', connection, part, whichPoint, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getControlPointX`, 'returnType') = 'numeric'
attr(`tc_getControlPointX`, "inputTypes") = c('integer', 'integer', 'integer')
class(`tc_getControlPointX`) = c("SWIGFunction", class('tc_getControlPointX'))

# Start of tc_getControlPointY

`tc_getControlPointY` = function(connection, part, whichPoint, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  whichPoint = as.integer(whichPoint) 
  
  if(length(whichPoint) > 1) {
    warning("using only the first element of whichPoint")
  }
  
  .Call('R_swig_tc_getControlPointY', connection, part, whichPoint, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getControlPointY`, 'returnType') = 'numeric'
attr(`tc_getControlPointY`, "inputTypes") = c('integer', 'integer', 'integer')
class(`tc_getControlPointY`) = c("SWIGFunction", class('tc_getControlPointY'))

# Start of tc_setControlPoint

`tc_setControlPoint` = function(connection, part, whichPoint, x, y)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  whichPoint = as.integer(whichPoint) 
  
  if(length(whichPoint) > 1) {
    warning("using only the first element of whichPoint")
  }
  
  
  
  .Call('R_swig_tc_setControlPoint', connection, part, whichPoint, x, y, PACKAGE='tinkercell')
  
}

attr(`tc_setControlPoint`, 'returnType') = 'void'
attr(`tc_setControlPoint`, "inputTypes") = c('integer', 'integer', 'integer', 'numeric', 'numeric')
class(`tc_setControlPoint`) = c("SWIGFunction", class('tc_setControlPoint'))

# Start of tc_setCenterPoint

`tc_setCenterPoint` = function(connection, y, x)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  
  
  .Call('R_swig_tc_setCenterPoint', connection, y, x, PACKAGE='tinkercell')
  
}

attr(`tc_setCenterPoint`, 'returnType') = 'void'
attr(`tc_setCenterPoint`, "inputTypes") = c('integer', 'numeric', 'numeric')
class(`tc_setCenterPoint`) = c("SWIGFunction", class('tc_setCenterPoint'))

# Start of tc_getCenterPointX

`tc_getCenterPointX` = function(connection, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  .Call('R_swig_tc_getCenterPointX', connection, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getCenterPointX`, 'returnType') = 'numeric'
attr(`tc_getCenterPointX`, "inputTypes") = c('integer')
class(`tc_getCenterPointX`) = c("SWIGFunction", class('tc_getCenterPointX'))

# Start of tc_getCenterPointY

`tc_getCenterPointY` = function(connection, .copy = FALSE)
{
  connection = as.integer(connection) 
  
  if(length(connection) > 1) {
    warning("using only the first element of connection")
  }
  
  .Call('R_swig_tc_getCenterPointY', connection, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_getCenterPointY`, 'returnType') = 'numeric'
attr(`tc_getCenterPointY`, "inputTypes") = c('integer')
class(`tc_getCenterPointY`) = c("SWIGFunction", class('tc_getCenterPointY'))

# Start of tc_setStraight

`tc_setStraight` = function(item, straight)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  straight = as.integer(straight) 
  
  if(length(straight) > 1) {
    warning("using only the first element of straight")
  }
  
  .Call('R_swig_tc_setStraight', item, straight, PACKAGE='tinkercell')
  
}

attr(`tc_setStraight`, 'returnType') = 'void'
attr(`tc_setStraight`, "inputTypes") = c('integer', 'integer')
class(`tc_setStraight`) = c("SWIGFunction", class('tc_setStraight'))

# Start of tc_setAllStraight

`tc_setAllStraight` = function(straight)
{
  straight = as.integer(straight) 
  
  if(length(straight) > 1) {
    warning("using only the first element of straight")
  }
  
  .Call('R_swig_tc_setAllStraight', straight, PACKAGE='tinkercell')
  
}

attr(`tc_setAllStraight`, 'returnType') = 'void'
attr(`tc_setAllStraight`, "inputTypes") = c('integer')
class(`tc_setAllStraight`) = c("SWIGFunction", class('tc_setAllStraight'))

# Start of tc_setLineWidth

`tc_setLineWidth` = function(item, width, permanent)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  
  permanent = as.integer(permanent) 
  
  if(length(permanent) > 1) {
    warning("using only the first element of permanent")
  }
  
  .Call('R_swig_tc_setLineWidth', item, width, permanent, PACKAGE='tinkercell')
  
}

attr(`tc_setLineWidth`, 'returnType') = 'void'
attr(`tc_setLineWidth`, "inputTypes") = c('integer', 'numeric', 'integer')
class(`tc_setLineWidth`) = c("SWIGFunction", class('tc_setLineWidth'))

# Start of tc_ConnectionSelection_api

`tc_ConnectionSelection_api` = function(getControlPointX, getControlPointY, setControlPoint, setCenterPoint, getCenterPointX, getCenterPointY, setStraight, setAllStraight, setLineWidth)
{
  if(is.function(getControlPointX)) {
    assert('...' %in% names(formals(getControlPointX)) || length(formals(getControlPointX)) >= 3)
  } else {
    if(is.character(getControlPointX)) {
      getControlPointX = getNativeSymbolInfo(getControlPointX)
    }
    if(is(getControlPointX, "NativeSymbolInfo")) {
      getControlPointX = getControlPointX$address
    }
  }
  if(is.function(getControlPointY)) {
    assert('...' %in% names(formals(getControlPointY)) || length(formals(getControlPointY)) >= 0)
  } else {
    if(is.character(getControlPointY)) {
      getControlPointY = getNativeSymbolInfo(getControlPointY)
    }
    if(is(getControlPointY, "NativeSymbolInfo")) {
      getControlPointY = getControlPointY$address
    }
  }
  if(is.function(setControlPoint)) {
    assert('...' %in% names(formals(setControlPoint)) || length(formals(setControlPoint)) >= 5)
  } else {
    if(is.character(setControlPoint)) {
      setControlPoint = getNativeSymbolInfo(setControlPoint)
    }
    if(is(setControlPoint, "NativeSymbolInfo")) {
      setControlPoint = setControlPoint$address
    }
  }
  if(is.function(setCenterPoint)) {
    assert('...' %in% names(formals(setCenterPoint)) || length(formals(setCenterPoint)) >= 0)
  } else {
    if(is.character(setCenterPoint)) {
      setCenterPoint = getNativeSymbolInfo(setCenterPoint)
    }
    if(is(setCenterPoint, "NativeSymbolInfo")) {
      setCenterPoint = setCenterPoint$address
    }
  }
  if(is.function(getCenterPointX)) {
    assert('...' %in% names(formals(getCenterPointX)) || length(formals(getCenterPointX)) >= 0)
  } else {
    if(is.character(getCenterPointX)) {
      getCenterPointX = getNativeSymbolInfo(getCenterPointX)
    }
    if(is(getCenterPointX, "NativeSymbolInfo")) {
      getCenterPointX = getCenterPointX$address
    }
  }
  if(is.function(getCenterPointY)) {
    assert('...' %in% names(formals(getCenterPointY)) || length(formals(getCenterPointY)) >= 0)
  } else {
    if(is.character(getCenterPointY)) {
      getCenterPointY = getNativeSymbolInfo(getCenterPointY)
    }
    if(is(getCenterPointY, "NativeSymbolInfo")) {
      getCenterPointY = getCenterPointY$address
    }
  }
  if(is.function(setStraight)) {
    assert('...' %in% names(formals(setStraight)) || length(formals(setStraight)) >= 2)
  } else {
    if(is.character(setStraight)) {
      setStraight = getNativeSymbolInfo(setStraight)
    }
    if(is(setStraight, "NativeSymbolInfo")) {
      setStraight = setStraight$address
    }
  }
  if(is.function(setAllStraight)) {
    assert('...' %in% names(formals(setAllStraight)) || length(formals(setAllStraight)) >= 1)
  } else {
    if(is.character(setAllStraight)) {
      setAllStraight = getNativeSymbolInfo(setAllStraight)
    }
    if(is(setAllStraight, "NativeSymbolInfo")) {
      setAllStraight = setAllStraight$address
    }
  }
  if(is.function(setLineWidth)) {
    assert('...' %in% names(formals(setLineWidth)) || length(formals(setLineWidth)) >= 0)
  } else {
    if(is.character(setLineWidth)) {
      setLineWidth = getNativeSymbolInfo(setLineWidth)
    }
    if(is(setLineWidth, "NativeSymbolInfo")) {
      setLineWidth = setLineWidth$address
    }
  }
  .Call('R_swig_tc_ConnectionSelection_api', getControlPointX, getControlPointY, setControlPoint, setCenterPoint, getCenterPointX, getCenterPointY, setStraight, setAllStraight, setLineWidth, PACKAGE='tinkercell')
  
}

attr(`tc_ConnectionSelection_api`, 'returnType') = 'void'
attr(`tc_ConnectionSelection_api`, "inputTypes") = c('_p_f_long_long_int__double', '_p_f_long_long_int__double', '_p_f_long_long_int_double_double__void', '_p_f_long_double_double__void', '_p_f_long__double', '_p_f_long__double', '_p_f_long_int__void', '_p_f_int__void', '_p_f_long_double_int__void')
class(`tc_ConnectionSelection_api`) = c("SWIGFunction", class('tc_ConnectionSelection_api'))

# Start of tc_merge

`tc_merge` = function(parts)
{
  .Call('R_swig_tc_merge', parts, PACKAGE='tinkercell')
  
}

attr(`tc_merge`, 'returnType') = 'void'
attr(`tc_merge`, "inputTypes") = c('_p_tc_items')
class(`tc_merge`) = c("SWIGFunction", class('tc_merge'))

# Start of tc_separate

`tc_separate` = function(part)
{
  part = as.integer(part) 
  
  if(length(part) > 1) {
    warning("using only the first element of part")
  }
  
  .Call('R_swig_tc_separate', part, PACKAGE='tinkercell')
  
}

attr(`tc_separate`, 'returnType') = 'void'
attr(`tc_separate`, "inputTypes") = c('integer')
class(`tc_separate`) = c("SWIGFunction", class('tc_separate'))

# Start of tc_GroupHandlerTool_api

`tc_GroupHandlerTool_api` = function(merge, separate)
{
  if(is.function(merge)) {
    assert('...' %in% names(formals(merge)) || length(formals(merge)) >= 1)
  } else {
    if(is.character(merge)) {
      merge = getNativeSymbolInfo(merge)
    }
    if(is(merge, "NativeSymbolInfo")) {
      merge = merge$address
    }
  }
  if(is.function(separate)) {
    assert('...' %in% names(formals(separate)) || length(formals(separate)) >= 0)
  } else {
    if(is.character(separate)) {
      separate = getNativeSymbolInfo(separate)
    }
    if(is(separate, "NativeSymbolInfo")) {
      separate = separate$address
    }
  }
  .Call('R_swig_tc_GroupHandlerTool_api', merge, separate, PACKAGE='tinkercell')
  
}

attr(`tc_GroupHandlerTool_api`, 'returnType') = 'void'
attr(`tc_GroupHandlerTool_api`, "inputTypes") = c('_p_f_tc_items__void', '_p_f_long__void')
class(`tc_GroupHandlerTool_api`) = c("SWIGFunction", class('tc_GroupHandlerTool_api'))

# Start of tc_insert

`tc_insert` = function(name, family, .copy = FALSE)
{
  name = as(name, "character") 
  family = as(family, "character") 
  .Call('R_swig_tc_insert', name, family, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_insert`, 'returnType') = 'integer'
attr(`tc_insert`, "inputTypes") = c('character', 'character')
class(`tc_insert`) = c("SWIGFunction", class('tc_insert'))

# Start of tc_NodeInsertion_api

`tc_NodeInsertion_api` = function(insertItem)
{
  if(is.function(insertItem)) {
    assert('...' %in% names(formals(insertItem)) || length(formals(insertItem)) >= 2)
  } else {
    if(is.character(insertItem)) {
      insertItem = getNativeSymbolInfo(insertItem)
    }
    if(is(insertItem, "NativeSymbolInfo")) {
      insertItem = insertItem$address
    }
  }
  .Call('R_swig_tc_NodeInsertion_api', insertItem, PACKAGE='tinkercell')
  
}

attr(`tc_NodeInsertion_api`, 'returnType') = 'void'
attr(`tc_NodeInsertion_api`, "inputTypes") = c('_p_f_p_q_const__char_p_q_const__char__long')
class(`tc_NodeInsertion_api`) = c("SWIGFunction", class('tc_NodeInsertion_api'))

# Start of tc_getStoichiometry

`tc_getStoichiometry` = function(A, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getStoichiometry', A, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getStoichiometry`, 'returnType') = '_p_tc_matrix'
attr(`tc_getStoichiometry`, "inputTypes") = c('_p_tc_items')
class(`tc_getStoichiometry`) = c("SWIGFunction", class('tc_getStoichiometry'))

# Start of tc_setStoichiometry

`tc_setStoichiometry` = function(A, N)
{
  .Call('R_swig_tc_setStoichiometry', A, N, PACKAGE='tinkercell')
  
}

attr(`tc_setStoichiometry`, 'returnType') = 'void'
attr(`tc_setStoichiometry`, "inputTypes") = c('_p_tc_items', '_p_tc_matrix')
class(`tc_setStoichiometry`) = c("SWIGFunction", class('tc_setStoichiometry'))

# Start of tc_getRates

`tc_getRates` = function(A, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getRates', A, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getRates`, 'returnType') = '_p_tc_strings'
attr(`tc_getRates`, "inputTypes") = c('_p_tc_items')
class(`tc_getRates`) = c("SWIGFunction", class('tc_getRates'))

# Start of tc_setRates

`tc_setRates` = function(A, rates)
{
  .Call('R_swig_tc_setRates', A, rates, PACKAGE='tinkercell')
  
}

attr(`tc_setRates`, 'returnType') = 'void'
attr(`tc_setRates`, "inputTypes") = c('_p_tc_items', '_p_tc_strings')
class(`tc_setRates`) = c("SWIGFunction", class('tc_setRates'))

# Start of tc_getStoichiometryFor

`tc_getStoichiometryFor` = function(x, .copy = FALSE)
{
  x = as.integer(x) 
  
  if(length(x) > 1) {
    warning("using only the first element of x")
  }
  
  ans = .Call('R_swig_tc_getStoichiometryFor', x, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getStoichiometryFor`, 'returnType') = '_p_tc_matrix'
attr(`tc_getStoichiometryFor`, "inputTypes") = c('integer')
class(`tc_getStoichiometryFor`) = c("SWIGFunction", class('tc_getStoichiometryFor'))

# Start of tc_getRate

`tc_getRate` = function(x)
{
  x = as.integer(x) 
  
  if(length(x) > 1) {
    warning("using only the first element of x")
  }
  
  .Call('R_swig_tc_getRate', x, PACKAGE='tinkercell')
  
}

attr(`tc_getRate`, 'returnType') = 'character'
attr(`tc_getRate`, "inputTypes") = c('integer')
class(`tc_getRate`) = c("SWIGFunction", class('tc_getRate'))

# Start of tc_setRate

`tc_setRate` = function(x, r)
{
  x = as.integer(x) 
  
  if(length(x) > 1) {
    warning("using only the first element of x")
  }
  
  r = as(r, "character") 
  .Call('R_swig_tc_setRate', x, r, PACKAGE='tinkercell')
  
}

attr(`tc_setRate`, 'returnType') = 'void'
attr(`tc_setRate`, "inputTypes") = c('integer', 'character')
class(`tc_setRate`) = c("SWIGFunction", class('tc_setRate'))

# Start of tc_setStoichiometryFor

`tc_setStoichiometryFor` = function(x, N)
{
  x = as.integer(x) 
  
  if(length(x) > 1) {
    warning("using only the first element of x")
  }
  
  
  .Call('R_swig_tc_setStoichiometryFor', x, N, PACKAGE='tinkercell')
  
}

attr(`tc_setStoichiometryFor`, 'returnType') = 'void'
attr(`tc_setStoichiometryFor`, "inputTypes") = c('integer', '_p_tc_matrix')
class(`tc_setStoichiometryFor`) = c("SWIGFunction", class('tc_setStoichiometryFor'))

# Start of tc_StoichiometryTool_api

`tc_StoichiometryTool_api` = function(getStoichiometry, setStoichiometry, getRates, setRates)
{
  if(is.function(getStoichiometry)) {
    assert('...' %in% names(formals(getStoichiometry)) || length(formals(getStoichiometry)) >= 0)
  } else {
    if(is.character(getStoichiometry)) {
      getStoichiometry = getNativeSymbolInfo(getStoichiometry)
    }
    if(is(getStoichiometry, "NativeSymbolInfo")) {
      getStoichiometry = getStoichiometry$address
    }
  }
  if(is.function(setStoichiometry)) {
    assert('...' %in% names(formals(setStoichiometry)) || length(formals(setStoichiometry)) >= 0)
  } else {
    if(is.character(setStoichiometry)) {
      setStoichiometry = getNativeSymbolInfo(setStoichiometry)
    }
    if(is(setStoichiometry, "NativeSymbolInfo")) {
      setStoichiometry = setStoichiometry$address
    }
  }
  if(is.function(getRates)) {
    assert('...' %in% names(formals(getRates)) || length(formals(getRates)) >= 0)
  } else {
    if(is.character(getRates)) {
      getRates = getNativeSymbolInfo(getRates)
    }
    if(is(getRates, "NativeSymbolInfo")) {
      getRates = getRates$address
    }
  }
  if(is.function(setRates)) {
    assert('...' %in% names(formals(setRates)) || length(formals(setRates)) >= 2)
  } else {
    if(is.character(setRates)) {
      setRates = getNativeSymbolInfo(setRates)
    }
    if(is(setRates, "NativeSymbolInfo")) {
      setRates = setRates$address
    }
  }
  .Call('R_swig_tc_StoichiometryTool_api', getStoichiometry, setStoichiometry, getRates, setRates, PACKAGE='tinkercell')
  
}

attr(`tc_StoichiometryTool_api`, 'returnType') = 'void'
attr(`tc_StoichiometryTool_api`, "inputTypes") = c('_p_f_tc_items__tc_matrix', '_p_f_tc_items_tc_matrix__void', '_p_f_tc_items__tc_strings', '_p_f_tc_items_tc_strings__void')
class(`tc_StoichiometryTool_api`) = c("SWIGFunction", class('tc_StoichiometryTool_api'))

# Start of tc_compileAndRun

`tc_compileAndRun` = function(command, s_args, .copy = FALSE)
{
  command = as(command, "character") 
  s_args = as(s_args, "character") 
  .Call('R_swig_tc_compileAndRun', command, s_args, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_compileAndRun`, 'returnType') = 'integer'
attr(`tc_compileAndRun`, "inputTypes") = c('character', 'character')
class(`tc_compileAndRun`) = c("SWIGFunction", class('tc_compileAndRun'))

# Start of tc_compileBuildLoad

`tc_compileBuildLoad` = function(filename, s_arg2, title, .copy = FALSE)
{
  filename = as(filename, "character") 
  s_arg2 = as(s_arg2, "character") 
  title = as(title, "character") 
  .Call('R_swig_tc_compileBuildLoad', filename, s_arg2, title, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_compileBuildLoad`, 'returnType') = 'integer'
attr(`tc_compileBuildLoad`, "inputTypes") = c('character', 'character', 'character')
class(`tc_compileBuildLoad`) = c("SWIGFunction", class('tc_compileBuildLoad'))

# Start of tc_compileBuildLoadSliders

`tc_compileBuildLoadSliders` = function(filename, s_arg2, title, inputs, .copy = FALSE)
{
  filename = as(filename, "character") 
  s_arg2 = as(s_arg2, "character") 
  title = as(title, "character") 
  
  .Call('R_swig_tc_compileBuildLoadSliders', filename, s_arg2, title, inputs, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_compileBuildLoadSliders`, 'returnType') = 'integer'
attr(`tc_compileBuildLoadSliders`, "inputTypes") = c('character', 'character', 'character', '_p_tc_matrix')
class(`tc_compileBuildLoadSliders`) = c("SWIGFunction", class('tc_compileBuildLoadSliders'))

# Start of tc_runPythonCode

`tc_runPythonCode` = function(code)
{
  code = as(code, "character") 
  .Call('R_swig_tc_runPythonCode', code, PACKAGE='tinkercell')
  
}

attr(`tc_runPythonCode`, 'returnType') = 'void'
attr(`tc_runPythonCode`, "inputTypes") = c('character')
class(`tc_runPythonCode`) = c("SWIGFunction", class('tc_runPythonCode'))

# Start of tc_runPythonFile

`tc_runPythonFile` = function(filename)
{
  filename = as(filename, "character") 
  .Call('R_swig_tc_runPythonFile', filename, PACKAGE='tinkercell')
  
}

attr(`tc_runPythonFile`, 'returnType') = 'void'
attr(`tc_runPythonFile`, "inputTypes") = c('character')
class(`tc_runPythonFile`) = c("SWIGFunction", class('tc_runPythonFile'))

# Start of tc_addPythonPlugin

`tc_addPythonPlugin` = function(file, name, description, category, icon)
{
  file = as(file, "character") 
  name = as(name, "character") 
  description = as(description, "character") 
  category = as(category, "character") 
  icon = as(icon, "character") 
  .Call('R_swig_tc_addPythonPlugin', file, name, description, category, icon, PACKAGE='tinkercell')
  
}

attr(`tc_addPythonPlugin`, 'returnType') = 'void'
attr(`tc_addPythonPlugin`, "inputTypes") = c('character', 'character', 'character', 'character', 'character')
class(`tc_addPythonPlugin`) = c("SWIGFunction", class('tc_addPythonPlugin'))

# Start of tc_callFunction

`tc_callFunction` = function(functionTitle)
{
  functionTitle = as(functionTitle, "character") 
  .Call('R_swig_tc_callFunction', functionTitle, PACKAGE='tinkercell')
  
}

attr(`tc_callFunction`, 'returnType') = 'void'
attr(`tc_callFunction`, "inputTypes") = c('character')
class(`tc_callFunction`) = c("SWIGFunction", class('tc_callFunction'))

# Start of tc_loadLibrary

`tc_loadLibrary` = function(filename)
{
  filename = as(filename, "character") 
  .Call('R_swig_tc_loadLibrary', filename, PACKAGE='tinkercell')
  
}

attr(`tc_loadLibrary`, 'returnType') = 'void'
attr(`tc_loadLibrary`, "inputTypes") = c('character')
class(`tc_loadLibrary`) = c("SWIGFunction", class('tc_loadLibrary'))

# Start of tc_OctaveTool_api

`tc_OctaveTool_api` = function(runOctaveCode, runOctaveFile, addOctavePlugin)
{
  if(is.function(runOctaveCode)) {
    assert('...' %in% names(formals(runOctaveCode)) || length(formals(runOctaveCode)) >= 0)
  } else {
    if(is.character(runOctaveCode)) {
      runOctaveCode = getNativeSymbolInfo(runOctaveCode)
    }
    if(is(runOctaveCode, "NativeSymbolInfo")) {
      runOctaveCode = runOctaveCode$address
    }
  }
  if(is.function(runOctaveFile)) {
    assert('...' %in% names(formals(runOctaveFile)) || length(formals(runOctaveFile)) >= 0)
  } else {
    if(is.character(runOctaveFile)) {
      runOctaveFile = getNativeSymbolInfo(runOctaveFile)
    }
    if(is(runOctaveFile, "NativeSymbolInfo")) {
      runOctaveFile = runOctaveFile$address
    }
  }
  if(is.function(addOctavePlugin)) {
    assert('...' %in% names(formals(addOctavePlugin)) || length(formals(addOctavePlugin)) >= 5)
  } else {
    if(is.character(addOctavePlugin)) {
      addOctavePlugin = getNativeSymbolInfo(addOctavePlugin)
    }
    if(is(addOctavePlugin, "NativeSymbolInfo")) {
      addOctavePlugin = addOctavePlugin$address
    }
  }
  .Call('R_swig_tc_OctaveTool_api', runOctaveCode, runOctaveFile, addOctavePlugin, PACKAGE='tinkercell')
  
}

attr(`tc_OctaveTool_api`, 'returnType') = 'void'
attr(`tc_OctaveTool_api`, "inputTypes") = c('_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char__void')
class(`tc_OctaveTool_api`) = c("SWIGFunction", class('tc_OctaveTool_api'))

# Start of tc_addFunction

`tc_addFunction` = function(f, title, description, category, iconFile, target_family, show_menu, in_tool_menu, make_default)
{
  if(is.function(f)) {
    assert('...' %in% names(formals(f)) || length(formals(f)) >= 0)
  } else {
    if(is.character(f)) {
      f = getNativeSymbolInfo(f)
    }
    if(is(f, "NativeSymbolInfo")) {
      f = f$address
    }
  }
  title = as(title, "character") 
  description = as(description, "character") 
  category = as(category, "character") 
  iconFile = as(iconFile, "character") 
  target_family = as(target_family, "character") 
  show_menu = as.integer(show_menu) 
  
  if(length(show_menu) > 1) {
    warning("using only the first element of show_menu")
  }
  
  in_tool_menu = as.integer(in_tool_menu) 
  
  if(length(in_tool_menu) > 1) {
    warning("using only the first element of in_tool_menu")
  }
  
  make_default = as.integer(make_default) 
  
  if(length(make_default) > 1) {
    warning("using only the first element of make_default")
  }
  
  .Call('R_swig_tc_addFunction', f, title, description, category, iconFile, target_family, show_menu, in_tool_menu, make_default, PACKAGE='tinkercell')
  
}

attr(`tc_addFunction`, 'returnType') = 'void'
attr(`tc_addFunction`, "inputTypes") = c('_p_f___void', 'character', 'character', 'character', 'character', 'character', 'integer', 'integer', 'integer')
class(`tc_addFunction`) = c("SWIGFunction", class('tc_addFunction'))

# Start of tc_runOctaveCode

`tc_runOctaveCode` = function(code)
{
  code = as(code, "character") 
  .Call('R_swig_tc_runOctaveCode', code, PACKAGE='tinkercell')
  
}

attr(`tc_runOctaveCode`, 'returnType') = 'void'
attr(`tc_runOctaveCode`, "inputTypes") = c('character')
class(`tc_runOctaveCode`) = c("SWIGFunction", class('tc_runOctaveCode'))

# Start of tc_runOctaveFile

`tc_runOctaveFile` = function(filename)
{
  filename = as(filename, "character") 
  .Call('R_swig_tc_runOctaveFile', filename, PACKAGE='tinkercell')
  
}

attr(`tc_runOctaveFile`, 'returnType') = 'void'
attr(`tc_runOctaveFile`, "inputTypes") = c('character')
class(`tc_runOctaveFile`) = c("SWIGFunction", class('tc_runOctaveFile'))

# Start of tc_addOctavePlugin

`tc_addOctavePlugin` = function(file, name, description, category, icon)
{
  file = as(file, "character") 
  name = as(name, "character") 
  description = as(description, "character") 
  category = as(category, "character") 
  icon = as(icon, "character") 
  .Call('R_swig_tc_addOctavePlugin', file, name, description, category, icon, PACKAGE='tinkercell')
  
}

attr(`tc_addOctavePlugin`, 'returnType') = 'void'
attr(`tc_addOctavePlugin`, "inputTypes") = c('character', 'character', 'character', 'character', 'character')
class(`tc_addOctavePlugin`) = c("SWIGFunction", class('tc_addOctavePlugin'))

# Start of tc_DynamicLibraryMenu_api

`tc_DynamicLibraryMenu_api` = function(callFunction)
{
  if(is.function(callFunction)) {
    assert('...' %in% names(formals(callFunction)) || length(formals(callFunction)) >= 0)
  } else {
    if(is.character(callFunction)) {
      callFunction = getNativeSymbolInfo(callFunction)
    }
    if(is(callFunction, "NativeSymbolInfo")) {
      callFunction = callFunction$address
    }
  }
  .Call('R_swig_tc_DynamicLibraryMenu_api', callFunction, PACKAGE='tinkercell')
  
}

attr(`tc_DynamicLibraryMenu_api`, 'returnType') = 'void'
attr(`tc_DynamicLibraryMenu_api`, "inputTypes") = c('_p_f_p_q_const__char__void')
class(`tc_DynamicLibraryMenu_api`) = c("SWIGFunction", class('tc_DynamicLibraryMenu_api'))

# Start of tc_LoadCLibraries_api

`tc_LoadCLibraries_api` = function(compileAndRun, compileBuildLoad, compileBuildLoadSliders, loadLibrary, addFunction)
{
  if(is.function(compileAndRun)) {
    assert('...' %in% names(formals(compileAndRun)) || length(formals(compileAndRun)) >= 2)
  } else {
    if(is.character(compileAndRun)) {
      compileAndRun = getNativeSymbolInfo(compileAndRun)
    }
    if(is(compileAndRun, "NativeSymbolInfo")) {
      compileAndRun = compileAndRun$address
    }
  }
  if(is.function(compileBuildLoad)) {
    assert('...' %in% names(formals(compileBuildLoad)) || length(formals(compileBuildLoad)) >= 3)
  } else {
    if(is.character(compileBuildLoad)) {
      compileBuildLoad = getNativeSymbolInfo(compileBuildLoad)
    }
    if(is(compileBuildLoad, "NativeSymbolInfo")) {
      compileBuildLoad = compileBuildLoad$address
    }
  }
  if(is.function(compileBuildLoadSliders)) {
    assert('...' %in% names(formals(compileBuildLoadSliders)) || length(formals(compileBuildLoadSliders)) >= 4)
  } else {
    if(is.character(compileBuildLoadSliders)) {
      compileBuildLoadSliders = getNativeSymbolInfo(compileBuildLoadSliders)
    }
    if(is(compileBuildLoadSliders, "NativeSymbolInfo")) {
      compileBuildLoadSliders = compileBuildLoadSliders$address
    }
  }
  if(is.function(loadLibrary)) {
    assert('...' %in% names(formals(loadLibrary)) || length(formals(loadLibrary)) >= 0)
  } else {
    if(is.character(loadLibrary)) {
      loadLibrary = getNativeSymbolInfo(loadLibrary)
    }
    if(is(loadLibrary, "NativeSymbolInfo")) {
      loadLibrary = loadLibrary$address
    }
  }
  if(is.function(addFunction)) {
    assert('...' %in% names(formals(addFunction)) || length(formals(addFunction)) >= 9)
  } else {
    if(is.character(addFunction)) {
      addFunction = getNativeSymbolInfo(addFunction)
    }
    if(is(addFunction, "NativeSymbolInfo")) {
      addFunction = addFunction$address
    }
  }
  .Call('R_swig_tc_LoadCLibraries_api', compileAndRun, compileBuildLoad, compileBuildLoadSliders, loadLibrary, addFunction, PACKAGE='tinkercell')
  
}

attr(`tc_LoadCLibraries_api`, 'returnType') = 'void'
attr(`tc_LoadCLibraries_api`, "inputTypes") = c('_p_f_p_q_const__char_p_q_const__char__int', '_p_f_p_q_const__char_p_q_const__char_p_q_const__char__int', '_p_f_p_q_const__char_p_q_const__char_p_q_const__char_tc_matrix__int', '_p_f_p_q_const__char__void', '_p_f_p_f___void_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_int_int_int__void')
class(`tc_LoadCLibraries_api`) = c("SWIGFunction", class('tc_LoadCLibraries_api'))

# Start of tc_PythonTool_api

`tc_PythonTool_api` = function(runPythonCode, runPythonFile, addPythonPlugin)
{
  if(is.function(runPythonCode)) {
    assert('...' %in% names(formals(runPythonCode)) || length(formals(runPythonCode)) >= 0)
  } else {
    if(is.character(runPythonCode)) {
      runPythonCode = getNativeSymbolInfo(runPythonCode)
    }
    if(is(runPythonCode, "NativeSymbolInfo")) {
      runPythonCode = runPythonCode$address
    }
  }
  if(is.function(runPythonFile)) {
    assert('...' %in% names(formals(runPythonFile)) || length(formals(runPythonFile)) >= 0)
  } else {
    if(is.character(runPythonFile)) {
      runPythonFile = getNativeSymbolInfo(runPythonFile)
    }
    if(is(runPythonFile, "NativeSymbolInfo")) {
      runPythonFile = runPythonFile$address
    }
  }
  if(is.function(addPythonPlugin)) {
    assert('...' %in% names(formals(addPythonPlugin)) || length(formals(addPythonPlugin)) >= 0)
  } else {
    if(is.character(addPythonPlugin)) {
      addPythonPlugin = getNativeSymbolInfo(addPythonPlugin)
    }
    if(is(addPythonPlugin, "NativeSymbolInfo")) {
      addPythonPlugin = addPythonPlugin$address
    }
  }
  .Call('R_swig_tc_PythonTool_api', runPythonCode, runPythonFile, addPythonPlugin, PACKAGE='tinkercell')
  
}

attr(`tc_PythonTool_api`, 'returnType') = 'void'
attr(`tc_PythonTool_api`, "inputTypes") = c('_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char_p_q_const__char__void')
class(`tc_PythonTool_api`) = c("SWIGFunction", class('tc_PythonTool_api'))

# Start of tc_surface

`tc_surface` = function(z, title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_surface', z, title, PACKAGE='tinkercell')
  
}

attr(`tc_surface`, 'returnType') = 'void'
attr(`tc_surface`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_surface`) = c("SWIGFunction", class('tc_surface'))

# Start of tc_plot

`tc_plot` = function(data, title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_plot', data, title, PACKAGE='tinkercell')
  
}

attr(`tc_plot`, 'returnType') = 'void'
attr(`tc_plot`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_plot`) = c("SWIGFunction", class('tc_plot'))

# Start of tc_scatterplot

`tc_scatterplot` = function(data, title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_scatterplot', data, title, PACKAGE='tinkercell')
  
}

attr(`tc_scatterplot`, 'returnType') = 'void'
attr(`tc_scatterplot`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_scatterplot`) = c("SWIGFunction", class('tc_scatterplot'))

# Start of tc_errorBars

`tc_errorBars` = function(data, title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_errorBars', data, title, PACKAGE='tinkercell')
  
}

attr(`tc_errorBars`, 'returnType') = 'void'
attr(`tc_errorBars`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_errorBars`) = c("SWIGFunction", class('tc_errorBars'))

# Start of tc_hist

`tc_hist` = function(data, title)
{
  title = as(title, "character") 
  .Call('R_swig_tc_hist', data, title, PACKAGE='tinkercell')
  
}

attr(`tc_hist`, 'returnType') = 'void'
attr(`tc_hist`, "inputTypes") = c('_p_tc_matrix', 'character')
class(`tc_hist`) = c("SWIGFunction", class('tc_hist'))

# Start of tc_multiplot

`tc_multiplot` = function(rows, cols)
{
  rows = as.integer(rows) 
  
  if(length(rows) > 1) {
    warning("using only the first element of rows")
  }
  
  cols = as.integer(cols) 
  
  if(length(cols) > 1) {
    warning("using only the first element of cols")
  }
  
  .Call('R_swig_tc_multiplot', rows, cols, PACKAGE='tinkercell')
  
}

attr(`tc_multiplot`, 'returnType') = 'void'
attr(`tc_multiplot`, "inputTypes") = c('integer', 'integer')
class(`tc_multiplot`) = c("SWIGFunction", class('tc_multiplot'))

# Start of tc_holdPlot

`tc_holdPlot` = function(on)
{
  on = as.integer(on) 
  
  if(length(on) > 1) {
    warning("using only the first element of on")
  }
  
  .Call('R_swig_tc_holdPlot', on, PACKAGE='tinkercell')
  
}

attr(`tc_holdPlot`, 'returnType') = 'void'
attr(`tc_holdPlot`, "inputTypes") = c('integer')
class(`tc_holdPlot`) = c("SWIGFunction", class('tc_holdPlot'))

# Start of tc_clusterPlots

`tc_clusterPlots` = function(clusters)
{
  clusters = as.integer(clusters) 
  
  if(length(clusters) > 1) {
    warning("using only the first element of clusters")
  }
  
  .Call('R_swig_tc_clusterPlots', clusters, PACKAGE='tinkercell')
  
}

attr(`tc_clusterPlots`, 'returnType') = 'void'
attr(`tc_clusterPlots`, "inputTypes") = c('integer')
class(`tc_clusterPlots`) = c("SWIGFunction", class('tc_clusterPlots'))

# Start of tc_getPlotData

`tc_getPlotData` = function(whichPlot, .copy = FALSE)
{
  whichPlot = as.integer(whichPlot) 
  
  if(length(whichPlot) > 1) {
    warning("using only the first element of whichPlot")
  }
  
  ans = .Call('R_swig_tc_getPlotData', whichPlot, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getPlotData`, 'returnType') = '_p_tc_matrix'
attr(`tc_getPlotData`, "inputTypes") = c('integer')
class(`tc_getPlotData`) = c("SWIGFunction", class('tc_getPlotData'))

# Start of tc_gnuplot

`tc_gnuplot` = function(s_arg1)
{
  s_arg1 = as(s_arg1, "character") 
  .Call('R_swig_tc_gnuplot', s_arg1, PACKAGE='tinkercell')
  
}

attr(`tc_gnuplot`, 'returnType') = 'void'
attr(`tc_gnuplot`, "inputTypes") = c('character')
class(`tc_gnuplot`) = c("SWIGFunction", class('tc_gnuplot'))

# Start of tc_savePlot

`tc_savePlot` = function(filename)
{
  filename = as(filename, "character") 
  .Call('R_swig_tc_savePlot', filename, PACKAGE='tinkercell')
  
}

attr(`tc_savePlot`, 'returnType') = 'void'
attr(`tc_savePlot`, "inputTypes") = c('character')
class(`tc_savePlot`) = c("SWIGFunction", class('tc_savePlot'))

# Start of tc_setLogScale

`tc_setLogScale` = function(s_arg1)
{
  s_arg1 = as.integer(s_arg1) 
  
  if(length(s_arg1) > 1) {
    warning("using only the first element of s_arg1")
  }
  
  .Call('R_swig_tc_setLogScale', s_arg1, PACKAGE='tinkercell')
  
}

attr(`tc_setLogScale`, 'returnType') = 'void'
attr(`tc_setLogScale`, "inputTypes") = c('integer')
class(`tc_setLogScale`) = c("SWIGFunction", class('tc_setLogScale'))

# Start of tc_PlotTool_api

`tc_PlotTool_api` = function(plot, surface, hist, errorBars, scatterplot, multiplot, hold, enableClustering, plotData, gnuplot, savePlotImage, logscale)
{
  if(is.function(plot)) {
    assert('...' %in% names(formals(plot)) || length(formals(plot)) >= 2)
  } else {
    if(is.character(plot)) {
      plot = getNativeSymbolInfo(plot)
    }
    if(is(plot, "NativeSymbolInfo")) {
      plot = plot$address
    }
  }
  if(is.function(surface)) {
    assert('...' %in% names(formals(surface)) || length(formals(surface)) >= 0)
  } else {
    if(is.character(surface)) {
      surface = getNativeSymbolInfo(surface)
    }
    if(is(surface, "NativeSymbolInfo")) {
      surface = surface$address
    }
  }
  if(is.function(hist)) {
    assert('...' %in% names(formals(hist)) || length(formals(hist)) >= 0)
  } else {
    if(is.character(hist)) {
      hist = getNativeSymbolInfo(hist)
    }
    if(is(hist, "NativeSymbolInfo")) {
      hist = hist$address
    }
  }
  if(is.function(errorBars)) {
    assert('...' %in% names(formals(errorBars)) || length(formals(errorBars)) >= 0)
  } else {
    if(is.character(errorBars)) {
      errorBars = getNativeSymbolInfo(errorBars)
    }
    if(is(errorBars, "NativeSymbolInfo")) {
      errorBars = errorBars$address
    }
  }
  if(is.function(scatterplot)) {
    assert('...' %in% names(formals(scatterplot)) || length(formals(scatterplot)) >= 0)
  } else {
    if(is.character(scatterplot)) {
      scatterplot = getNativeSymbolInfo(scatterplot)
    }
    if(is(scatterplot, "NativeSymbolInfo")) {
      scatterplot = scatterplot$address
    }
  }
  if(is.function(multiplot)) {
    assert('...' %in% names(formals(multiplot)) || length(formals(multiplot)) >= 2)
  } else {
    if(is.character(multiplot)) {
      multiplot = getNativeSymbolInfo(multiplot)
    }
    if(is(multiplot, "NativeSymbolInfo")) {
      multiplot = multiplot$address
    }
  }
  if(is.function(hold)) {
    assert('...' %in% names(formals(hold)) || length(formals(hold)) >= 0)
  } else {
    if(is.character(hold)) {
      hold = getNativeSymbolInfo(hold)
    }
    if(is(hold, "NativeSymbolInfo")) {
      hold = hold$address
    }
  }
  if(is.function(enableClustering)) {
    assert('...' %in% names(formals(enableClustering)) || length(formals(enableClustering)) >= 0)
  } else {
    if(is.character(enableClustering)) {
      enableClustering = getNativeSymbolInfo(enableClustering)
    }
    if(is(enableClustering, "NativeSymbolInfo")) {
      enableClustering = enableClustering$address
    }
  }
  if(is.function(plotData)) {
    assert('...' %in% names(formals(plotData)) || length(formals(plotData)) >= 1)
  } else {
    if(is.character(plotData)) {
      plotData = getNativeSymbolInfo(plotData)
    }
    if(is(plotData, "NativeSymbolInfo")) {
      plotData = plotData$address
    }
  }
  if(is.function(gnuplot)) {
    assert('...' %in% names(formals(gnuplot)) || length(formals(gnuplot)) >= 0)
  } else {
    if(is.character(gnuplot)) {
      gnuplot = getNativeSymbolInfo(gnuplot)
    }
    if(is(gnuplot, "NativeSymbolInfo")) {
      gnuplot = gnuplot$address
    }
  }
  if(is.function(savePlotImage)) {
    assert('...' %in% names(formals(savePlotImage)) || length(formals(savePlotImage)) >= 0)
  } else {
    if(is.character(savePlotImage)) {
      savePlotImage = getNativeSymbolInfo(savePlotImage)
    }
    if(is(savePlotImage, "NativeSymbolInfo")) {
      savePlotImage = savePlotImage$address
    }
  }
  if(is.function(logscale)) {
    assert('...' %in% names(formals(logscale)) || length(formals(logscale)) >= 0)
  } else {
    if(is.character(logscale)) {
      logscale = getNativeSymbolInfo(logscale)
    }
    if(is(logscale, "NativeSymbolInfo")) {
      logscale = logscale$address
    }
  }
  .Call('R_swig_tc_PlotTool_api', plot, surface, hist, errorBars, scatterplot, multiplot, hold, enableClustering, plotData, gnuplot, savePlotImage, logscale, PACKAGE='tinkercell')
  
}

attr(`tc_PlotTool_api`, 'returnType') = 'void'
attr(`tc_PlotTool_api`, "inputTypes") = c('_p_f_tc_matrix_p_q_const__char__void', '_p_f_tc_matrix_p_q_const__char__void', '_p_f_tc_matrix_p_q_const__char__void', '_p_f_tc_matrix_p_q_const__char__void', '_p_f_tc_matrix_p_q_const__char__void', '_p_f_int_int__void', '_p_f_int__void', '_p_f_int__void', '_p_f_int__tc_matrix', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_int__void')
class(`tc_PlotTool_api`) = c("SWIGFunction", class('tc_PlotTool_api'))

# Start of tc_writeModel

`tc_writeModel` = function(file, items, .copy = FALSE)
{
  file = as(file, "character") 
  
  .Call('R_swig_tc_writeModel', file, items, as.logical(.copy), PACKAGE='tinkercell')
  
}

attr(`tc_writeModel`, 'returnType') = 'integer'
attr(`tc_writeModel`, "inputTypes") = c('character', '_p_tc_items')
class(`tc_writeModel`) = c("SWIGFunction", class('tc_writeModel'))

# Start of tc_ModelFileGenerator_api

`tc_ModelFileGenerator_api` = function(modelgen)
{
  if(is.function(modelgen)) {
    assert('...' %in% names(formals(modelgen)) || length(formals(modelgen)) >= 2)
  } else {
    if(is.character(modelgen)) {
      modelgen = getNativeSymbolInfo(modelgen)
    }
    if(is(modelgen, "NativeSymbolInfo")) {
      modelgen = modelgen$address
    }
  }
  .Call('R_swig_tc_ModelFileGenerator_api', modelgen, PACKAGE='tinkercell')
  
}

attr(`tc_ModelFileGenerator_api`, 'returnType') = 'void'
attr(`tc_ModelFileGenerator_api`, "inputTypes") = c('_p_f_p_q_const__char_tc_items__int')
class(`tc_ModelFileGenerator_api`) = c("SWIGFunction", class('tc_ModelFileGenerator_api'))

# Start of tc_getEventTriggers

`tc_getEventTriggers` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getEventTriggers', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getEventTriggers`, 'returnType') = '_p_tc_strings'
class(`tc_getEventTriggers`) = c("SWIGFunction", class('tc_getEventTriggers'))

# Start of tc_getEventResponses

`tc_getEventResponses` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getEventResponses', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getEventResponses`, 'returnType') = '_p_tc_strings'
class(`tc_getEventResponses`) = c("SWIGFunction", class('tc_getEventResponses'))

# Start of tc_addEvent

`tc_addEvent` = function(trigger, event)
{
  trigger = as(trigger, "character") 
  event = as(event, "character") 
  .Call('R_swig_tc_addEvent', trigger, event, PACKAGE='tinkercell')
  
}

attr(`tc_addEvent`, 'returnType') = 'void'
attr(`tc_addEvent`, "inputTypes") = c('character', 'character')
class(`tc_addEvent`) = c("SWIGFunction", class('tc_addEvent'))

# Start of tc_SimulationEventsTool_api

`tc_SimulationEventsTool_api` = function(getEventTriggers, getEventResponses, addEvent)
{
  if(is.function(getEventTriggers)) {
    assert('...' %in% names(formals(getEventTriggers)) || length(formals(getEventTriggers)) >= 0)
  } else {
    if(is.character(getEventTriggers)) {
      getEventTriggers = getNativeSymbolInfo(getEventTriggers)
    }
    if(is(getEventTriggers, "NativeSymbolInfo")) {
      getEventTriggers = getEventTriggers$address
    }
  }
  if(is.function(getEventResponses)) {
    assert('...' %in% names(formals(getEventResponses)) || length(formals(getEventResponses)) >= 0)
  } else {
    if(is.character(getEventResponses)) {
      getEventResponses = getNativeSymbolInfo(getEventResponses)
    }
    if(is(getEventResponses, "NativeSymbolInfo")) {
      getEventResponses = getEventResponses$address
    }
  }
  if(is.function(addEvent)) {
    assert('...' %in% names(formals(addEvent)) || length(formals(addEvent)) >= 0)
  } else {
    if(is.character(addEvent)) {
      addEvent = getNativeSymbolInfo(addEvent)
    }
    if(is(addEvent, "NativeSymbolInfo")) {
      addEvent = addEvent$address
    }
  }
  .Call('R_swig_tc_SimulationEventsTool_api', getEventTriggers, getEventResponses, addEvent, PACKAGE='tinkercell')
  
}

attr(`tc_SimulationEventsTool_api`, 'returnType') = 'void'
attr(`tc_SimulationEventsTool_api`, "inputTypes") = c('_p_f___tc_strings', '_p_f___tc_strings', '_p_f_p_q_const__char_p_q_const__char__void')
class(`tc_SimulationEventsTool_api`) = c("SWIGFunction", class('tc_SimulationEventsTool_api'))

# Start of tc_getForcingFunctionNames

`tc_getForcingFunctionNames` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getForcingFunctionNames', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getForcingFunctionNames`, 'returnType') = '_p_tc_strings'
attr(`tc_getForcingFunctionNames`, "inputTypes") = c('_p_tc_items')
class(`tc_getForcingFunctionNames`) = c("SWIGFunction", class('tc_getForcingFunctionNames'))

# Start of tc_getForcingFunctionAssignments

`tc_getForcingFunctionAssignments` = function(a, .copy = FALSE)
{
  ans = .Call('R_swig_tc_getForcingFunctionAssignments', a, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_getForcingFunctionAssignments`, 'returnType') = '_p_tc_strings'
attr(`tc_getForcingFunctionAssignments`, "inputTypes") = c('_p_tc_items')
class(`tc_getForcingFunctionAssignments`) = c("SWIGFunction", class('tc_getForcingFunctionAssignments'))

# Start of tc_addForcingFunction

`tc_addForcingFunction` = function(item, variable, formula)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  variable = as(variable, "character") 
  formula = as(formula, "character") 
  .Call('R_swig_tc_addForcingFunction', item, variable, formula, PACKAGE='tinkercell')
  
}

attr(`tc_addForcingFunction`, 'returnType') = 'void'
attr(`tc_addForcingFunction`, "inputTypes") = c('integer', 'character', 'character')
class(`tc_addForcingFunction`) = c("SWIGFunction", class('tc_addForcingFunction'))

# Start of tc_AssignmentFunctionsTool_api

`tc_AssignmentFunctionsTool_api` = function(getForcingFunctionNames, getForcingFunctionAssignments, addForcingFunction)
{
  if(is.function(getForcingFunctionNames)) {
    assert('...' %in% names(formals(getForcingFunctionNames)) || length(formals(getForcingFunctionNames)) >= 0)
  } else {
    if(is.character(getForcingFunctionNames)) {
      getForcingFunctionNames = getNativeSymbolInfo(getForcingFunctionNames)
    }
    if(is(getForcingFunctionNames, "NativeSymbolInfo")) {
      getForcingFunctionNames = getForcingFunctionNames$address
    }
  }
  if(is.function(getForcingFunctionAssignments)) {
    assert('...' %in% names(formals(getForcingFunctionAssignments)) || length(formals(getForcingFunctionAssignments)) >= 0)
  } else {
    if(is.character(getForcingFunctionAssignments)) {
      getForcingFunctionAssignments = getNativeSymbolInfo(getForcingFunctionAssignments)
    }
    if(is(getForcingFunctionAssignments, "NativeSymbolInfo")) {
      getForcingFunctionAssignments = getForcingFunctionAssignments$address
    }
  }
  if(is.function(addForcingFunction)) {
    assert('...' %in% names(formals(addForcingFunction)) || length(formals(addForcingFunction)) >= 0)
  } else {
    if(is.character(addForcingFunction)) {
      addForcingFunction = getNativeSymbolInfo(addForcingFunction)
    }
    if(is(addForcingFunction, "NativeSymbolInfo")) {
      addForcingFunction = addForcingFunction$address
    }
  }
  .Call('R_swig_tc_AssignmentFunctionsTool_api', getForcingFunctionNames, getForcingFunctionAssignments, addForcingFunction, PACKAGE='tinkercell')
  
}

attr(`tc_AssignmentFunctionsTool_api`, 'returnType') = 'void'
attr(`tc_AssignmentFunctionsTool_api`, "inputTypes") = c('_p_f_tc_items__tc_strings', '_p_f_tc_items__tc_strings', '_p_f_long_p_q_const__char_p_q_const__char__void')
class(`tc_AssignmentFunctionsTool_api`) = c("SWIGFunction", class('tc_AssignmentFunctionsTool_api'))

# Start of tc_partsIn

`tc_partsIn` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_partsIn', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_partsIn`, 'returnType') = '_p_tc_items'
attr(`tc_partsIn`, "inputTypes") = c('integer')
class(`tc_partsIn`) = c("SWIGFunction", class('tc_partsIn'))

# Start of tc_partsUpstream

`tc_partsUpstream` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_partsUpstream', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_partsUpstream`, 'returnType') = '_p_tc_items'
attr(`tc_partsUpstream`, "inputTypes") = c('integer')
class(`tc_partsUpstream`) = c("SWIGFunction", class('tc_partsUpstream'))

# Start of tc_partsDownstream

`tc_partsDownstream` = function(o, .copy = FALSE)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  ans = .Call('R_swig_tc_partsDownstream', o, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_items"
  
  ans
  
}

attr(`tc_partsDownstream`, 'returnType') = '_p_tc_items'
attr(`tc_partsDownstream`, "inputTypes") = c('integer')
class(`tc_partsDownstream`) = c("SWIGFunction", class('tc_partsDownstream'))

# Start of tc_alignParts

`tc_alignParts` = function(a)
{
  .Call('R_swig_tc_alignParts', a, PACKAGE='tinkercell')
  
}

attr(`tc_alignParts`, 'returnType') = 'void'
attr(`tc_alignParts`, "inputTypes") = c('_p_tc_items')
class(`tc_alignParts`) = c("SWIGFunction", class('tc_alignParts'))

# Start of tc_alignPartsOnPlasmid

`tc_alignPartsOnPlasmid` = function(s_arg1, s_arg2)
{
  s_arg1 = as.integer(s_arg1) 
  
  if(length(s_arg1) > 1) {
    warning("using only the first element of s_arg1")
  }
  
  
  .Call('R_swig_tc_alignPartsOnPlasmid', s_arg1, s_arg2, PACKAGE='tinkercell')
  
}

attr(`tc_alignPartsOnPlasmid`, 'returnType') = 'void'
attr(`tc_alignPartsOnPlasmid`, "inputTypes") = c('integer', '_p_tc_items')
class(`tc_alignPartsOnPlasmid`) = c("SWIGFunction", class('tc_alignPartsOnPlasmid'))

# Start of tc_setSequence

`tc_setSequence` = function(o, s_arg2)
{
  o = as.integer(o) 
  
  if(length(o) > 1) {
    warning("using only the first element of o")
  }
  
  s_arg2 = as(s_arg2, "character") 
  .Call('R_swig_tc_setSequence', o, s_arg2, PACKAGE='tinkercell')
  
}

attr(`tc_setSequence`, 'returnType') = 'void'
attr(`tc_setSequence`, "inputTypes") = c('integer', 'character')
class(`tc_setSequence`) = c("SWIGFunction", class('tc_setSequence'))

# Start of tc_AutoGeneRegulatoryTool_api

`tc_AutoGeneRegulatoryTool_api` = function(f1, f2, f3, f4, f5)
{
  if(is.function(f1)) {
    assert('...' %in% names(formals(f1)) || length(formals(f1)) >= 0)
  } else {
    if(is.character(f1)) {
      f1 = getNativeSymbolInfo(f1)
    }
    if(is(f1, "NativeSymbolInfo")) {
      f1 = f1$address
    }
  }
  if(is.function(f2)) {
    assert('...' %in% names(formals(f2)) || length(formals(f2)) >= 0)
  } else {
    if(is.character(f2)) {
      f2 = getNativeSymbolInfo(f2)
    }
    if(is(f2, "NativeSymbolInfo")) {
      f2 = f2$address
    }
  }
  if(is.function(f3)) {
    assert('...' %in% names(formals(f3)) || length(formals(f3)) >= 0)
  } else {
    if(is.character(f3)) {
      f3 = getNativeSymbolInfo(f3)
    }
    if(is(f3, "NativeSymbolInfo")) {
      f3 = f3$address
    }
  }
  if(is.function(f4)) {
    assert('...' %in% names(formals(f4)) || length(formals(f4)) >= 0)
  } else {
    if(is.character(f4)) {
      f4 = getNativeSymbolInfo(f4)
    }
    if(is(f4, "NativeSymbolInfo")) {
      f4 = f4$address
    }
  }
  if(is.function(f5)) {
    assert('...' %in% names(formals(f5)) || length(formals(f5)) >= 2)
  } else {
    if(is.character(f5)) {
      f5 = getNativeSymbolInfo(f5)
    }
    if(is(f5, "NativeSymbolInfo")) {
      f5 = f5$address
    }
  }
  .Call('R_swig_tc_AutoGeneRegulatoryTool_api', f1, f2, f3, f4, f5, PACKAGE='tinkercell')
  
}

attr(`tc_AutoGeneRegulatoryTool_api`, 'returnType') = 'void'
attr(`tc_AutoGeneRegulatoryTool_api`, "inputTypes") = c('_p_f_long__tc_items', '_p_f_long__tc_items', '_p_f_long__tc_items', '_p_f_tc_items__void', '_p_f_long_tc_items__void')
class(`tc_AutoGeneRegulatoryTool_api`) = c("SWIGFunction", class('tc_AutoGeneRegulatoryTool_api'))

# Start of tc_exportSBML

`tc_exportSBML` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_exportSBML', file, PACKAGE='tinkercell')
  
}

attr(`tc_exportSBML`, 'returnType') = 'void'
attr(`tc_exportSBML`, "inputTypes") = c('character')
class(`tc_exportSBML`) = c("SWIGFunction", class('tc_exportSBML'))

# Start of tc_importSBML

`tc_importSBML` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_importSBML', file, PACKAGE='tinkercell')
  
}

attr(`tc_importSBML`, 'returnType') = 'void'
attr(`tc_importSBML`, "inputTypes") = c('character')
class(`tc_importSBML`) = c("SWIGFunction", class('tc_importSBML'))

# Start of tc_exportText

`tc_exportText` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_exportText', file, PACKAGE='tinkercell')
  
}

attr(`tc_exportText`, 'returnType') = 'void'
attr(`tc_exportText`, "inputTypes") = c('character')
class(`tc_exportText`) = c("SWIGFunction", class('tc_exportText'))

# Start of tc_importText

`tc_importText` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_importText', file, PACKAGE='tinkercell')
  
}

attr(`tc_importText`, 'returnType') = 'void'
attr(`tc_importText`, "inputTypes") = c('character')
class(`tc_importText`) = c("SWIGFunction", class('tc_importText'))

# Start of tc_exportMatlab

`tc_exportMatlab` = function(file)
{
  file = as(file, "character") 
  .Call('R_swig_tc_exportMatlab', file, PACKAGE='tinkercell')
  
}

attr(`tc_exportMatlab`, 'returnType') = 'void'
attr(`tc_exportMatlab`, "inputTypes") = c('character')
class(`tc_exportMatlab`) = c("SWIGFunction", class('tc_exportMatlab'))

# Start of tc_SBML_api

`tc_SBML_api` = function(exportSBML, importSBML, exportText, importText, exportMath)
{
  if(is.function(exportSBML)) {
    assert('...' %in% names(formals(exportSBML)) || length(formals(exportSBML)) >= 0)
  } else {
    if(is.character(exportSBML)) {
      exportSBML = getNativeSymbolInfo(exportSBML)
    }
    if(is(exportSBML, "NativeSymbolInfo")) {
      exportSBML = exportSBML$address
    }
  }
  if(is.function(importSBML)) {
    assert('...' %in% names(formals(importSBML)) || length(formals(importSBML)) >= 0)
  } else {
    if(is.character(importSBML)) {
      importSBML = getNativeSymbolInfo(importSBML)
    }
    if(is(importSBML, "NativeSymbolInfo")) {
      importSBML = importSBML$address
    }
  }
  if(is.function(exportText)) {
    assert('...' %in% names(formals(exportText)) || length(formals(exportText)) >= 0)
  } else {
    if(is.character(exportText)) {
      exportText = getNativeSymbolInfo(exportText)
    }
    if(is(exportText, "NativeSymbolInfo")) {
      exportText = exportText$address
    }
  }
  if(is.function(importText)) {
    assert('...' %in% names(formals(importText)) || length(formals(importText)) >= 0)
  } else {
    if(is.character(importText)) {
      importText = getNativeSymbolInfo(importText)
    }
    if(is(importText, "NativeSymbolInfo")) {
      importText = importText$address
    }
  }
  if(is.function(exportMath)) {
    assert('...' %in% names(formals(exportMath)) || length(formals(exportMath)) >= 0)
  } else {
    if(is.character(exportMath)) {
      exportMath = getNativeSymbolInfo(exportMath)
    }
    if(is(exportMath, "NativeSymbolInfo")) {
      exportMath = exportMath$address
    }
  }
  .Call('R_swig_tc_SBML_api', exportSBML, importSBML, exportText, importText, exportMath, PACKAGE='tinkercell')
  
}

attr(`tc_SBML_api`, 'returnType') = 'void'
attr(`tc_SBML_api`, "inputTypes") = c('_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void', '_p_f_p_q_const__char__void')
class(`tc_SBML_api`) = c("SWIGFunction", class('tc_SBML_api'))

# Start of tc_simulateDeterministic

`tc_simulateDeterministic` = function(startTime, endTime, numSteps, .copy = FALSE)
{
  numSteps = as.integer(numSteps) 
  
  if(length(numSteps) > 1) {
    warning("using only the first element of numSteps")
  }
  
  ans = .Call('R_swig_tc_simulateDeterministic', startTime, endTime, numSteps, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_simulateDeterministic`, 'returnType') = '_p_tc_matrix'
attr(`tc_simulateDeterministic`, "inputTypes") = c('numeric', 'numeric', 'integer')
class(`tc_simulateDeterministic`) = c("SWIGFunction", class('tc_simulateDeterministic'))

# Start of tc_simulateStochastic

`tc_simulateStochastic` = function(startTime, endTime, numSteps, .copy = FALSE)
{
  numSteps = as.integer(numSteps) 
  
  if(length(numSteps) > 1) {
    warning("using only the first element of numSteps")
  }
  
  ans = .Call('R_swig_tc_simulateStochastic', startTime, endTime, numSteps, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_simulateStochastic`, 'returnType') = '_p_tc_matrix'
attr(`tc_simulateStochastic`, "inputTypes") = c('numeric', 'numeric', 'integer')
class(`tc_simulateStochastic`) = c("SWIGFunction", class('tc_simulateStochastic'))

# Start of tc_simulateHybrid

`tc_simulateHybrid` = function(startTime, endTime, numSteps, .copy = FALSE)
{
  numSteps = as.integer(numSteps) 
  
  if(length(numSteps) > 1) {
    warning("using only the first element of numSteps")
  }
  
  ans = .Call('R_swig_tc_simulateHybrid', startTime, endTime, numSteps, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_simulateHybrid`, 'returnType') = '_p_tc_matrix'
attr(`tc_simulateHybrid`, "inputTypes") = c('numeric', 'numeric', 'integer')
class(`tc_simulateHybrid`) = c("SWIGFunction", class('tc_simulateHybrid'))

# Start of tc_simulateTauLeap

`tc_simulateTauLeap` = function(startTime, endTime, numSteps, .copy = FALSE)
{
  numSteps = as.integer(numSteps) 
  
  if(length(numSteps) > 1) {
    warning("using only the first element of numSteps")
  }
  
  ans = .Call('R_swig_tc_simulateTauLeap', startTime, endTime, numSteps, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_simulateTauLeap`, 'returnType') = '_p_tc_matrix'
attr(`tc_simulateTauLeap`, "inputTypes") = c('numeric', 'numeric', 'integer')
class(`tc_simulateTauLeap`) = c("SWIGFunction", class('tc_simulateTauLeap'))

# Start of tc_getSteadyState

`tc_getSteadyState` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getSteadyState', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getSteadyState`, 'returnType') = '_p_tc_matrix'
class(`tc_getSteadyState`) = c("SWIGFunction", class('tc_getSteadyState'))

# Start of tc_steadyStateScan

`tc_steadyStateScan` = function(param, start, end, numSteps, .copy = FALSE)
{
  param = as(param, "character") 
  
  
  numSteps = as.integer(numSteps) 
  
  if(length(numSteps) > 1) {
    warning("using only the first element of numSteps")
  }
  
  ans = .Call('R_swig_tc_steadyStateScan', param, start, end, numSteps, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_steadyStateScan`, 'returnType') = '_p_tc_matrix'
attr(`tc_steadyStateScan`, "inputTypes") = c('character', 'numeric', 'numeric', 'integer')
class(`tc_steadyStateScan`) = c("SWIGFunction", class('tc_steadyStateScan'))

# Start of tc_steadyStateScan2D

`tc_steadyStateScan2D` = function(param1, start1, end1, numSteps1, param2, start2, end2, numSteps2, .copy = FALSE)
{
  param1 = as(param1, "character") 
  
  
  numSteps1 = as.integer(numSteps1) 
  
  if(length(numSteps1) > 1) {
    warning("using only the first element of numSteps1")
  }
  
  param2 = as(param2, "character") 
  
  
  numSteps2 = as.integer(numSteps2) 
  
  if(length(numSteps2) > 1) {
    warning("using only the first element of numSteps2")
  }
  
  ans = .Call('R_swig_tc_steadyStateScan2D', param1, start1, end1, numSteps1, param2, start2, end2, numSteps2, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_steadyStateScan2D`, 'returnType') = '_p_tc_matrix'
attr(`tc_steadyStateScan2D`, "inputTypes") = c('character', 'numeric', 'numeric', 'integer', 'character', 'numeric', 'numeric', 'integer')
class(`tc_steadyStateScan2D`) = c("SWIGFunction", class('tc_steadyStateScan2D'))

# Start of tc_getJacobian

`tc_getJacobian` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getJacobian', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getJacobian`, 'returnType') = '_p_tc_matrix'
class(`tc_getJacobian`) = c("SWIGFunction", class('tc_getJacobian'))

# Start of tc_getEigenvalues

`tc_getEigenvalues` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getEigenvalues', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getEigenvalues`, 'returnType') = '_p_tc_matrix'
class(`tc_getEigenvalues`) = c("SWIGFunction", class('tc_getEigenvalues'))

# Start of tc_getUnscaledElasticities

`tc_getUnscaledElasticities` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getUnscaledElasticities', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getUnscaledElasticities`, 'returnType') = '_p_tc_matrix'
class(`tc_getUnscaledElasticities`) = c("SWIGFunction", class('tc_getUnscaledElasticities'))

# Start of tc_getUnscaledConcentrationCC

`tc_getUnscaledConcentrationCC` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getUnscaledConcentrationCC', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getUnscaledConcentrationCC`, 'returnType') = '_p_tc_matrix'
class(`tc_getUnscaledConcentrationCC`) = c("SWIGFunction", class('tc_getUnscaledConcentrationCC'))

# Start of tc_getUnscaledFluxCC

`tc_getUnscaledFluxCC` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getUnscaledFluxCC', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getUnscaledFluxCC`, 'returnType') = '_p_tc_matrix'
class(`tc_getUnscaledFluxCC`) = c("SWIGFunction", class('tc_getUnscaledFluxCC'))

# Start of tc_getScaledElasticities

`tc_getScaledElasticities` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getScaledElasticities', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getScaledElasticities`, 'returnType') = '_p_tc_matrix'
class(`tc_getScaledElasticities`) = c("SWIGFunction", class('tc_getScaledElasticities'))

# Start of tc_getScaledConcentrationCC

`tc_getScaledConcentrationCC` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getScaledConcentrationCC', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getScaledConcentrationCC`, 'returnType') = '_p_tc_matrix'
class(`tc_getScaledConcentrationCC`) = c("SWIGFunction", class('tc_getScaledConcentrationCC'))

# Start of tc_getScaledFluxCC

`tc_getScaledFluxCC` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_getScaledFluxCC', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_getScaledFluxCC`, 'returnType') = '_p_tc_matrix'
class(`tc_getScaledFluxCC`) = c("SWIGFunction", class('tc_getScaledFluxCC'))

# Start of tc_reducedStoichiometry

`tc_reducedStoichiometry` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_reducedStoichiometry', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_reducedStoichiometry`, 'returnType') = '_p_tc_matrix'
class(`tc_reducedStoichiometry`) = c("SWIGFunction", class('tc_reducedStoichiometry'))

# Start of tc_elementaryFluxModes

`tc_elementaryFluxModes` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_elementaryFluxModes', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_elementaryFluxModes`, 'returnType') = '_p_tc_matrix'
class(`tc_elementaryFluxModes`) = c("SWIGFunction", class('tc_elementaryFluxModes'))

# Start of tc_LMatrix

`tc_LMatrix` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_LMatrix', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_LMatrix`, 'returnType') = '_p_tc_matrix'
class(`tc_LMatrix`) = c("SWIGFunction", class('tc_LMatrix'))

# Start of tc_KMatrix

`tc_KMatrix` = function(.copy = FALSE)
{
  ans = .Call('R_swig_tc_KMatrix', as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_KMatrix`, 'returnType') = '_p_tc_matrix'
class(`tc_KMatrix`) = c("SWIGFunction", class('tc_KMatrix'))

# Start of tc_updateParameters

`tc_updateParameters` = function(params)
{
  .Call('R_swig_tc_updateParameters', params, PACKAGE='tinkercell')
  
}

attr(`tc_updateParameters`, 'returnType') = 'void'
attr(`tc_updateParameters`, "inputTypes") = c('_p_tc_matrix')
class(`tc_updateParameters`) = c("SWIGFunction", class('tc_updateParameters'))

# Start of tc_optimize

`tc_optimize` = function(formulaOrFile, .copy = FALSE)
{
  formulaOrFile = as(formulaOrFile, "character") 
  ans = .Call('R_swig_tc_optimize', formulaOrFile, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_matrix"
  
  ans
  
}

attr(`tc_optimize`, 'returnType') = '_p_tc_matrix'
attr(`tc_optimize`, "inputTypes") = c('character')
class(`tc_optimize`) = c("SWIGFunction", class('tc_optimize'))

# Start of tc_COPASI_api

`tc_COPASI_api` = function(simulateDeterministic, simulateStochastic, simulateHybrid, simulateTauLeap, getSteadyState, steadyStateScan, steadyStateScan2D, getJacobian, getEigenvalues, getUnscaledElasticities, getUnscaledConcentrationCC, getUnscaledFluxCC, getScaledElasticities, getScaledConcentrationCC, getScaledFluxCC, tc_reducedStoichiometry, tc_emf, tc_Lmat, tc_Kmat, gaoptim, update)
{
  if(is.function(simulateDeterministic)) {
    assert('...' %in% names(formals(simulateDeterministic)) || length(formals(simulateDeterministic)) >= 3)
  } else {
    if(is.character(simulateDeterministic)) {
      simulateDeterministic = getNativeSymbolInfo(simulateDeterministic)
    }
    if(is(simulateDeterministic, "NativeSymbolInfo")) {
      simulateDeterministic = simulateDeterministic$address
    }
  }
  if(is.function(simulateStochastic)) {
    assert('...' %in% names(formals(simulateStochastic)) || length(formals(simulateStochastic)) >= 0)
  } else {
    if(is.character(simulateStochastic)) {
      simulateStochastic = getNativeSymbolInfo(simulateStochastic)
    }
    if(is(simulateStochastic, "NativeSymbolInfo")) {
      simulateStochastic = simulateStochastic$address
    }
  }
  if(is.function(simulateHybrid)) {
    assert('...' %in% names(formals(simulateHybrid)) || length(formals(simulateHybrid)) >= 0)
  } else {
    if(is.character(simulateHybrid)) {
      simulateHybrid = getNativeSymbolInfo(simulateHybrid)
    }
    if(is(simulateHybrid, "NativeSymbolInfo")) {
      simulateHybrid = simulateHybrid$address
    }
  }
  if(is.function(simulateTauLeap)) {
    assert('...' %in% names(formals(simulateTauLeap)) || length(formals(simulateTauLeap)) >= 0)
  } else {
    if(is.character(simulateTauLeap)) {
      simulateTauLeap = getNativeSymbolInfo(simulateTauLeap)
    }
    if(is(simulateTauLeap, "NativeSymbolInfo")) {
      simulateTauLeap = simulateTauLeap$address
    }
  }
  if(is.function(getSteadyState)) {
    assert('...' %in% names(formals(getSteadyState)) || length(formals(getSteadyState)) >= 0)
  } else {
    if(is.character(getSteadyState)) {
      getSteadyState = getNativeSymbolInfo(getSteadyState)
    }
    if(is(getSteadyState, "NativeSymbolInfo")) {
      getSteadyState = getSteadyState$address
    }
  }
  if(is.function(steadyStateScan)) {
    assert('...' %in% names(formals(steadyStateScan)) || length(formals(steadyStateScan)) >= 4)
  } else {
    if(is.character(steadyStateScan)) {
      steadyStateScan = getNativeSymbolInfo(steadyStateScan)
    }
    if(is(steadyStateScan, "NativeSymbolInfo")) {
      steadyStateScan = steadyStateScan$address
    }
  }
  if(is.function(steadyStateScan2D)) {
    assert('...' %in% names(formals(steadyStateScan2D)) || length(formals(steadyStateScan2D)) >= 8)
  } else {
    if(is.character(steadyStateScan2D)) {
      steadyStateScan2D = getNativeSymbolInfo(steadyStateScan2D)
    }
    if(is(steadyStateScan2D, "NativeSymbolInfo")) {
      steadyStateScan2D = steadyStateScan2D$address
    }
  }
  if(is.function(getJacobian)) {
    assert('...' %in% names(formals(getJacobian)) || length(formals(getJacobian)) >= 0)
  } else {
    if(is.character(getJacobian)) {
      getJacobian = getNativeSymbolInfo(getJacobian)
    }
    if(is(getJacobian, "NativeSymbolInfo")) {
      getJacobian = getJacobian$address
    }
  }
  if(is.function(getEigenvalues)) {
    assert('...' %in% names(formals(getEigenvalues)) || length(formals(getEigenvalues)) >= 0)
  } else {
    if(is.character(getEigenvalues)) {
      getEigenvalues = getNativeSymbolInfo(getEigenvalues)
    }
    if(is(getEigenvalues, "NativeSymbolInfo")) {
      getEigenvalues = getEigenvalues$address
    }
  }
  if(is.function(getUnscaledElasticities)) {
    assert('...' %in% names(formals(getUnscaledElasticities)) || length(formals(getUnscaledElasticities)) >= 0)
  } else {
    if(is.character(getUnscaledElasticities)) {
      getUnscaledElasticities = getNativeSymbolInfo(getUnscaledElasticities)
    }
    if(is(getUnscaledElasticities, "NativeSymbolInfo")) {
      getUnscaledElasticities = getUnscaledElasticities$address
    }
  }
  if(is.function(getUnscaledConcentrationCC)) {
    assert('...' %in% names(formals(getUnscaledConcentrationCC)) || length(formals(getUnscaledConcentrationCC)) >= 0)
  } else {
    if(is.character(getUnscaledConcentrationCC)) {
      getUnscaledConcentrationCC = getNativeSymbolInfo(getUnscaledConcentrationCC)
    }
    if(is(getUnscaledConcentrationCC, "NativeSymbolInfo")) {
      getUnscaledConcentrationCC = getUnscaledConcentrationCC$address
    }
  }
  if(is.function(getUnscaledFluxCC)) {
    assert('...' %in% names(formals(getUnscaledFluxCC)) || length(formals(getUnscaledFluxCC)) >= 0)
  } else {
    if(is.character(getUnscaledFluxCC)) {
      getUnscaledFluxCC = getNativeSymbolInfo(getUnscaledFluxCC)
    }
    if(is(getUnscaledFluxCC, "NativeSymbolInfo")) {
      getUnscaledFluxCC = getUnscaledFluxCC$address
    }
  }
  if(is.function(getScaledElasticities)) {
    assert('...' %in% names(formals(getScaledElasticities)) || length(formals(getScaledElasticities)) >= 0)
  } else {
    if(is.character(getScaledElasticities)) {
      getScaledElasticities = getNativeSymbolInfo(getScaledElasticities)
    }
    if(is(getScaledElasticities, "NativeSymbolInfo")) {
      getScaledElasticities = getScaledElasticities$address
    }
  }
  if(is.function(getScaledConcentrationCC)) {
    assert('...' %in% names(formals(getScaledConcentrationCC)) || length(formals(getScaledConcentrationCC)) >= 0)
  } else {
    if(is.character(getScaledConcentrationCC)) {
      getScaledConcentrationCC = getNativeSymbolInfo(getScaledConcentrationCC)
    }
    if(is(getScaledConcentrationCC, "NativeSymbolInfo")) {
      getScaledConcentrationCC = getScaledConcentrationCC$address
    }
  }
  if(is.function(getScaledFluxCC)) {
    assert('...' %in% names(formals(getScaledFluxCC)) || length(formals(getScaledFluxCC)) >= 0)
  } else {
    if(is.character(getScaledFluxCC)) {
      getScaledFluxCC = getNativeSymbolInfo(getScaledFluxCC)
    }
    if(is(getScaledFluxCC, "NativeSymbolInfo")) {
      getScaledFluxCC = getScaledFluxCC$address
    }
  }
  if(is.function(tc_reducedStoichiometry)) {
    assert('...' %in% names(formals(tc_reducedStoichiometry)) || length(formals(tc_reducedStoichiometry)) >= 0)
  } else {
    if(is.character(tc_reducedStoichiometry)) {
      tc_reducedStoichiometry = getNativeSymbolInfo(tc_reducedStoichiometry)
    }
    if(is(tc_reducedStoichiometry, "NativeSymbolInfo")) {
      tc_reducedStoichiometry = tc_reducedStoichiometry$address
    }
  }
  if(is.function(tc_emf)) {
    assert('...' %in% names(formals(tc_emf)) || length(formals(tc_emf)) >= 0)
  } else {
    if(is.character(tc_emf)) {
      tc_emf = getNativeSymbolInfo(tc_emf)
    }
    if(is(tc_emf, "NativeSymbolInfo")) {
      tc_emf = tc_emf$address
    }
  }
  if(is.function(tc_Lmat)) {
    assert('...' %in% names(formals(tc_Lmat)) || length(formals(tc_Lmat)) >= 0)
  } else {
    if(is.character(tc_Lmat)) {
      tc_Lmat = getNativeSymbolInfo(tc_Lmat)
    }
    if(is(tc_Lmat, "NativeSymbolInfo")) {
      tc_Lmat = tc_Lmat$address
    }
  }
  if(is.function(tc_Kmat)) {
    assert('...' %in% names(formals(tc_Kmat)) || length(formals(tc_Kmat)) >= 0)
  } else {
    if(is.character(tc_Kmat)) {
      tc_Kmat = getNativeSymbolInfo(tc_Kmat)
    }
    if(is(tc_Kmat, "NativeSymbolInfo")) {
      tc_Kmat = tc_Kmat$address
    }
  }
  if(is.function(gaoptim)) {
    assert('...' %in% names(formals(gaoptim)) || length(formals(gaoptim)) >= 1)
  } else {
    if(is.character(gaoptim)) {
      gaoptim = getNativeSymbolInfo(gaoptim)
    }
    if(is(gaoptim, "NativeSymbolInfo")) {
      gaoptim = gaoptim$address
    }
  }
  if(is.function(update)) {
    assert('...' %in% names(formals(update)) || length(formals(update)) >= 0)
  } else {
    if(is.character(update)) {
      update = getNativeSymbolInfo(update)
    }
    if(is(update, "NativeSymbolInfo")) {
      update = update$address
    }
  }
  .Call('R_swig_tc_COPASI_api', simulateDeterministic, simulateStochastic, simulateHybrid, simulateTauLeap, getSteadyState, steadyStateScan, steadyStateScan2D, getJacobian, getEigenvalues, getUnscaledElasticities, getUnscaledConcentrationCC, getUnscaledFluxCC, getScaledElasticities, getScaledConcentrationCC, getScaledFluxCC, tc_reducedStoichiometry, tc_emf, tc_Lmat, tc_Kmat, gaoptim, update, PACKAGE='tinkercell')
  
}

attr(`tc_COPASI_api`, 'returnType') = 'void'
attr(`tc_COPASI_api`, "inputTypes") = c('_p_f_double_double_int__tc_matrix', '_p_f_double_double_int__tc_matrix', '_p_f_double_double_int__tc_matrix', '_p_f_double_double_int__tc_matrix', '_p_f___tc_matrix', '_p_f_p_q_const__char_double_double_int__tc_matrix', '_p_f_p_q_const__char_double_double_int_p_q_const__char_double_double_int__tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f___tc_matrix', '_p_f_p_q_const__char__tc_matrix', '_p_f_tc_matrix__void')
class(`tc_COPASI_api`) = c("SWIGFunction", class('tc_COPASI_api'))

# Start of tc_substituteModel

`tc_substituteModel` = function(item, filename)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  filename = as(filename, "character") 
  .Call('R_swig_tc_substituteModel', item, filename, PACKAGE='tinkercell')
  
}

attr(`tc_substituteModel`, 'returnType') = 'void'
attr(`tc_substituteModel`, "inputTypes") = c('integer', 'character')
class(`tc_substituteModel`) = c("SWIGFunction", class('tc_substituteModel'))

# Start of tc_substituteEmptyModel

`tc_substituteEmptyModel` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_substituteEmptyModel', item, PACKAGE='tinkercell')
  
}

attr(`tc_substituteEmptyModel`, 'returnType') = 'void'
attr(`tc_substituteEmptyModel`, "inputTypes") = c('integer')
class(`tc_substituteEmptyModel`) = c("SWIGFunction", class('tc_substituteEmptyModel'))

# Start of tc_substituteOriginalModel

`tc_substituteOriginalModel` = function(item)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  .Call('R_swig_tc_substituteOriginalModel', item, PACKAGE='tinkercell')
  
}

attr(`tc_substituteOriginalModel`, 'returnType') = 'void'
attr(`tc_substituteOriginalModel`, "inputTypes") = c('integer')
class(`tc_substituteOriginalModel`) = c("SWIGFunction", class('tc_substituteOriginalModel'))

# Start of tc_listOfPossibleModels

`tc_listOfPossibleModels` = function(item, .copy = FALSE)
{
  item = as.integer(item) 
  
  if(length(item) > 1) {
    warning("using only the first element of item")
  }
  
  ans = .Call('R_swig_tc_listOfPossibleModels', item, as.logical(.copy), PACKAGE='tinkercell')
  class(ans) <- "_p_tc_strings"
  
  ans
  
}

attr(`tc_listOfPossibleModels`, 'returnType') = '_p_tc_strings'
attr(`tc_listOfPossibleModels`, "inputTypes") = c('integer')
class(`tc_listOfPossibleModels`) = c("SWIGFunction", class('tc_listOfPossibleModels'))

# Start of tc_ModuleTool_api

`tc_ModuleTool_api` = function(substituteModel, listOfModels)
{
  if(is.function(substituteModel)) {
    assert('...' %in% names(formals(substituteModel)) || length(formals(substituteModel)) >= 0)
  } else {
    if(is.character(substituteModel)) {
      substituteModel = getNativeSymbolInfo(substituteModel)
    }
    if(is(substituteModel, "NativeSymbolInfo")) {
      substituteModel = substituteModel$address
    }
  }
  if(is.function(listOfModels)) {
    assert('...' %in% names(formals(listOfModels)) || length(formals(listOfModels)) >= 0)
  } else {
    if(is.character(listOfModels)) {
      listOfModels = getNativeSymbolInfo(listOfModels)
    }
    if(is(listOfModels, "NativeSymbolInfo")) {
      listOfModels = listOfModels$address
    }
  }
  .Call('R_swig_tc_ModuleTool_api', substituteModel, listOfModels, PACKAGE='tinkercell')
  
}

attr(`tc_ModuleTool_api`, 'returnType') = 'void'
attr(`tc_ModuleTool_api`, "inputTypes") = c('_p_f_long_p_q_const__char__void', '_p_f_long__tc_strings')
class(`tc_ModuleTool_api`) = c("SWIGFunction", class('tc_ModuleTool_api'))


