FILE(GLOB PYTHONPLUGINS python/*.py)
INSTALL(FILES ${PYTHONPLUGINS} DESTINATION python)

FILE(GLOB OCTAVEPLUGINS octave/*.m)
INSTALL(FILES ${OCTAVEPLUGINS} DESTINATION python)

IF (APPLE)
	INSTALL(DIRECTORY Lab DESTINATION ${TINKERCELL_EXE}.app/Contents/MacOS
		      PATTERN ".svn" EXCLUDE)

	INSTALL(DIRECTORY Modules DESTINATION ${TINKERCELL_EXE}.app/Contents/MacOS
		      PATTERN ".svn" EXCLUDE)
ELSE (APPLE)
	INSTALL(DIRECTORY Lab DESTINATION bin/../
		      PATTERN ".svn" EXCLUDE)

	INSTALL(DIRECTORY Modules DESTINATION bin/../
		      PATTERN ".svn" EXCLUDE)
ENDIF (APPLE)
