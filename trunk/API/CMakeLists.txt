SET(LIBRARY_OUTPUT_PATH ${EXECUTABLE_OUTPUT_PATH})

FILE(GLOB API_SRC *.c)
ADD_DEFINITIONS(-DTC_EXPORTS)

ADD_LIBRARY( tinkercellapi
	STATIC
#    SHARED
	${API_SRC}
)

INSTALL(TARGETS tinkercellapi
	RUNTIME DESTINATION ${TINKERCELL_PACKAGE_ROOT_DIR}
	LIBRARY DESTINATION ${TINKERCELL_PACKAGE_ROOT_DIR}
	ARCHIVE DESTINATION ${TINKERCELL_PACKAGE_ROOT_DIR}
)

FILE( GLOB API_SRC "*.h" "*.c")
INSTALL( FILES ${API_SRC} DESTINATION ${TINKERCELL_PACKAGE_ROOT_DIR}/c )

