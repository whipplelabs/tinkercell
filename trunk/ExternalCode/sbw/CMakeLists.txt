INCLUDE_DIRECTORIES( 
	BEFORE
	${TINKERCELL_SOURCE_DIR}/ExternalCode/sbw
	${TINKERCELL_SOURCE_DIR}/ExternalCode/sbw/include
	${TINKERCELL_SOURCE_DIR}/ExternalCode/sbw/include/SBW
	${TINKERCELL_SOURCE_DIR}/ExternalCode/sbw/include/libxml
)

IF (WIN32)
  IF( MINGW )
    ADD_DEFINITIONS(-DMINGW)
	IF (NOT USE_STDCALL)
		ADD_DEFINITIONS(-DSTDCALL)
	ENDIF(NOT USE_STDCALL)
  ENDIF(MINGW)
  ADD_DEFINITIONS(-DWIN32)
ELSE (WIN32)
  ADD_DEFINITIONS(-DLINUX)
  IF (APPLE)
     ADD_DEFINITIONS(-DDARWIN)
  ENDIF (APPLE)
ENDIF (WIN32)

ADD_DEFINITIONS(-DSBW_EXPORTS)
ADD_DEFINITIONS(-DLIBLAX_EXPORTS)
ADD_SUBDIRECTORY( SBWCore )

