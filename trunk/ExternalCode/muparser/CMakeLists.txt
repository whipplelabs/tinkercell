SET(LIBRARY_OUTPUT_PATH ${EXECUTABLE_OUTPUT_PATH})

FILE( GLOB MUPARSER_SRC *.cpp )

ADD_LIBRARY( muparser 
#  SHARED
   STATIC
  ${MUPARSER_SRC} 
)

IF ( WIN32 )
  INSTALL(TARGETS muparser DESTINATION bin/../)
ELSE ( WIN32 )
  INSTALL(TARGETS muparser LIBRARY DESTINATION bin/../)
ENDIF( WIN32 )

